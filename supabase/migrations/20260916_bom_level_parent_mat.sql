-- 🌳 BOM หลายชั้นที่ "แก้ชั้นได้จริง" + ผูกขั้นตอน (PFC/OP)  (DR project — eyhclzkifitbhbljgoav)
--
-- คำสั่ง user 2026-09-16:
--   "เรื่อง bom level ตอนนี้ เหมือนปรับแก้อะไรไม่ได้ เหมือน SAP เพราะ SAP จะมีเลเวลควบคุม หรือเราตกเรื่องนี้ไป"
--   "ถ้าทำเลเวล bom ได้แบบ pfc มันจะแก้หลายจุดเลย แถมเหนือกว่า sap ตรงที่เรามี OP ด้วย"
--
-- ── ปัญหาที่วัดได้ (ตรวจ 16/09) ──────────────────────────────────────────────────────
-- `bom_items.product_id` ชี้ `dr_products.id` เท่านั้น ⇒ **ของที่จะมีลูกได้ ต้องเป็นแถวใน dr_products**
-- ของที่อยู่แค่ `parts_master` = ใบไม้ตลอดกาล ผูกลูกต่อไม่ได้เลย
--   เคสจริง `10100817`: ลูก 5 ตัว มีแค่ `20067039` ที่เป็น product · อีก 4 ตัว (รวมวัตถุดิบ `50028183`)
--   ต่อชั้นไม่ได้ ⇒ จอถึงเตือน "5xx ปนอยู่กับ 2xx" แต่คนแก้ไม่ได้
--   ทั้งฐาน: 506 แถว / 98 สินค้า — มีแค่ **57 แถว (11%)** ที่ลูกมี BOM ของตัวเอง · ลูกที่เป็น product 26 ตัว
--   ⇒ ~90% ของโครงถูกตรึงชั้นเดียว **เพราะระบบไม่ให้สร้างชั้น ไม่ใช่เพราะของจริงมีชั้นเดียว**
--
-- ── สิ่งที่เพิ่ม (additive ล้วน · ถอยได้ · แถวเดิมไม่ถูกแตะ) ──────────────────────────
-- 1) `parent_mat` — ตัวแม่ของบรรทัดนี้เป็น **mat_no** (ใครก็เป็นแม่ได้ ไม่ต้องเป็น dr_products)
--    null = พฤติกรรมเดิมเป๊ะ (ตัวแม่ = `dr_products` ของ `product_id`)
--    ⚠️ `product_id` **ยังต้องมีเสมอ** = "บรรทัดนี้อยู่ในใบ BOM ของ FG ตัวไหน" (root ของต้นไม้)
--       ⇒ ทุกจอที่อ่านด้วย `product_id` วันนี้ยังได้ผลเท่าเดิมทั้งหมด ไม่ต้องแก้พร้อมกัน
-- 2) `op_no` — ขั้นตอนที่ชิ้นนี้ถูกใส่เข้าไป (เลข OP ตาม PFC เช่น 190, 200)
--    นี่คือจุดที่ **เหนือ SAP**: SAP แยก BOM (ต้นไม้วัสดุ) กับ Routing (ขั้นตอน) แล้วการผูก
--    component→operation เป็น optional ที่แทบไม่มีใคร maintain · ของเราบรรทัดเดียวตอบทั้งสองคำถาม
--    ⇒ ของเสียหลุดขั้นไหนก็ตัดถูก · WIP ต่อขั้นถูก · part-call ส่งของถูกขั้น/ถูกไลน์
--    ทิศทาง: ต่อไปจะ sync กับ `pe_processes.child_parts` (PFC · Main project) ซึ่งกรอกไว้แล้ว
--    30/37 ขั้น (LH) · 21/28 ขั้น (RH) แต่ยังเป็น text ที่ไม่ได้คุยกับ BOM
--    ⚠️ `pe_doc_sets.mat_no` ว่างทั้ง 3 ใบ — ต้องผูก PFC↔MAT ก่อนถึง sync อัตโนมัติได้ (งานถัดไป)
--
-- ⚠️ **ไม่ลบ/ไม่ย้ายแถวไหนทั้งสิ้นในรอบนี้** — BOM ชุดนี้ถูก "แบน" มาจาก SAP (bomTree.js วัดไว้:
--    121/121 แถวหลานถูกใส่ซ้ำที่ชั้น 1 ด้วย) การตัดสินใจว่าจะลบแถวแบนหรือคงไว้ เป็นของ PE/Planning
--    เดาผิด = ความต้องการวัตถุดิบทั้งโรงงานเพี้ยน ย้อนยาก

alter table public.bom_items
  add column if not exists parent_mat text,
  add column if not exists op_no      text;

comment on column public.bom_items.parent_mat is
  'ตัวแม่ของบรรทัดนี้ (mat_no) — null = ตัวแม่คือ dr_products ของ product_id (พฤติกรรมเดิม) · ตั้งค่าเพื่อสร้างชั้นที่ 2+ โดยตัวแม่ไม่ต้องเป็น dr_products · product_id ยังเป็น "ใบ BOM ของ FG ตัวไหน" เสมอ';
comment on column public.bom_items.op_no is
  'ขั้นตอน (เลข OP ตาม PFC) ที่ชิ้นนี้ถูกใส่เข้าไป — ว่าง = ยังไม่ระบุ ห้ามเดา · จุดที่เหนือ SAP: BOM บรรทัดเดียวตอบทั้ง "ประกอบจากอะไร" และ "ใส่ตอนไหน"';

-- ไล่ต้นไม้เร็วขึ้นเมื่อมีชั้นเยอะ (ตอนนี้ 506 แถว ยังเล็ก แต่กันไว้ก่อนตอน scale ทั้งกลุ่ม)
create index if not exists bom_items_parent_mat_idx on public.bom_items (parent_mat) where parent_mat is not null;

-- ══ ตรวจผลหลังรัน ═══════════════════════════════════════════════════════════════════
--   select count(*) filter (where parent_mat is not null) as has_parent,
--          count(*) filter (where op_no is not null) as has_op, count(*) as total
--     from bom_items where is_active;   -- ต้องได้ 0 / 0 / 506 ทันทีหลัง apply (ยังไม่มีใครตั้ง)
--
-- ══ Rollback ════════════════════════════════════════════════════════════════════════
--   drop index if exists public.bom_items_parent_mat_idx;
--   alter table public.bom_items drop column if exists parent_mat, drop column if exists op_no;
--   (โค้ดฝั่งแอปถอยได้เอง: ตัวอ่าน BOM จับ 42703 แล้วกลับไปใช้ชุดคอลัมน์เดิม)
