# อัลกอริทึมทั้งหมดในระบบ ESM — ประมวลผลยังไง · ใช้ AI ไหม

> **สำหรับตอบคำถามตอนนำเสนอ** — ตรวจกับโค้ดจริง + รายการ edge function ที่ deploy อยู่ ณ **25 ส.ค. 2569**
> ทุกบรรทัดในเอกสารนี้ชี้ไปที่ไฟล์จริงได้ ไม่มีของที่ "กำลังจะทำ"

---

## 🎤 คำตอบสั้น — พูดได้ทันที 3 ประโยค

> **"ระบบนี้ไม่มี AI อยู่ในเส้นทางการตัดสินใจเลยครับ**
> ตัวเลขทุกตัวมาจาก**สูตรมาตรฐานอุตสาหกรรม** (OEE ตาม TPM · Cp/Cpk ตาม AIAG · Rank อะไหล่ตาม WI ของเราเอง)
> กับ**อัลกอริทึมคอมพิวเตอร์พื้นฐาน** ที่มีมาหลายสิบปีแล้ว เช่นการหาเส้นทางสั้นสุด
>
> **ที่เลือกแบบนี้เพราะสายรถยนต์ต้องอธิบายที่มาของตัวเลขให้ auditor ฟังได้**
> ถ้าใช้โมเดลที่บอกไม่ได้ว่าคิดมายังไง เวลาลูกค้าถามว่า OEE 79.5% มาจากไหน เราจะตอบไม่ได้
>
> **AI เป็นก้าวถัดไป ไม่ใช่ตอนนี้ครับ** — ตอนนี้เรายังอยู่ขั้น 'ทำให้ข้อมูลครบและเชื่อถือได้ก่อน'
> ซึ่งเป็นเงื่อนไขบังคับ ถ้าข้อมูลยังไม่ครบ AI ก็ไม่มีอะไรให้เรียนรู้"

---

## 🤖 เรื่อง AI — สถานะจริง 3 ระดับ

| | สถานะ | รายละเอียด |
|---|---|---|
| **AI / LLM ในการตัดสินใจ** | 🔴 **ไม่มี** | ไม่มี dependency ของ OpenAI / Anthropic / TensorFlow / ONNX ในระบบที่ deploy อยู่ |
| **โมเดล ML ในเครื่อง** | 🟡 มี 1 จุด (ไม่แตะข้อมูลผลิต) | **MediaPipe Gesture Recognizer** — ควบคุมสไลด์ประชุมแถวเช้าด้วยท่ามือ |
| **AI ผ่าน API** | 🟡 เขียนโค้ดไว้ **แต่ยังไม่ deploy** | `telegram-webhook` (Claude Haiku ช่วยแยกฟิลด์ตอนพิมพ์แจ้ง downtime ในไลน์แชท) |

### 🟡 MediaPipe — ต้องอธิบายให้ถูก ถ้าเขาสังเกตเห็น

`src/components/GestureCam.jsx` · `@mediapipe/tasks-vision`

- ใช้แค่ **เปลี่ยนหน้าสไลด์ในโหมดประชุมแถวเช้า** (ชี้นิ้ว / ปัดมือ / ยกนิ้วโป้ง)
- **ประมวลผลในเครื่อง 100%** — โมเดลกับ WASM เก็บไว้ในเซิร์ฟเวอร์เราเอง (`/public/mediapipe`) ไม่เรียก CDN ไม่เรียก API
- **ภาพจากกล้องไม่ถูกส่งออกไปไหนเลย**
- **ต้องกดเปิดเอง (opt-in)** ไม่ได้เปิดค้าง · มีจุดแดง + ภาพพรีวิวบอกตลอดว่ากล้องทำงานอยู่
- **กฎในระบบ: ท่ามือผูกได้แค่ "เปลี่ยนหน้า / ออกจากโหมด" ห้ามผูกกับ action ที่แก้ข้อมูล**

> พูดสั้นๆ: *"มีโมเดลจดจำท่ามือตัวเดียว ไว้เปลี่ยนสไลด์ตอนประชุมเช้าครับ ทำงานในเครื่อง ไม่ส่งภาพออก และแตะข้อมูลการผลิตไม่ได้เลย"*

### 🟡 AI intake — ถ้าเขาถามว่า "แล้วอนาคตล่ะ"

โค้ดอยู่ที่ `supabase/functions/telegram-webhook/` — ให้พนักงานพิมพ์ในไลน์แชทว่า
`/dt RB80 โรบอทชนจิ๊ก 14.00-14.20` แล้ว AI แยกให้ว่า เครื่องไหน อาการอะไร ช่วงเวลาไหน

**ยังไม่ deploy** (ตรวจรายการ edge function 25 ส.ค. แล้ว ไม่มีตัวนี้) และถ้าเปิดใช้จริง กติกาที่วางไว้คือ:

- AI แค่ **เสนอ** ค่าที่แยกได้ → ขึ้นปุ่ม **[✅ บันทึก] [❌ ยกเลิก]** → **คนกดยืนยันเท่านั้นถึงเขียนฐานข้อมูล**
- ค่าที่ AI เสนอถูก **ground กับ master จริง** (ทะเบียนเครื่อง / ประเภท downtime / กะที่เปิดอยู่) ไม่ใช่ให้มันแต่งเอง
- **AI เขียนฐานข้อมูลเองไม่ได้** — เป็นกฎที่เขียนไว้ใน CLAUDE.md ตั้งแต่แรก

> พูดสั้นๆ: *"เขียนไว้แล้วแต่ยังไม่เปิดครับ และต่อให้เปิด AI ก็ทำได้แค่เสนอ คนต้องกดยืนยันก่อนเสมอ"*

---

## 📐 อัลกอริทึมทั้งหมด — แยกตามหมวด

**ประเภท:** 🏭 สูตรมาตรฐานอุตสาหกรรม · 🧮 อัลกอริทึมคอมพิวเตอร์คลาสสิก · 📋 กฎธุรกิจที่เราตั้งเอง

### A. OEE และการวัดผลผลิต — `src/utils/oee.js`

| # | อัลกอริทึม | วิธีคิด | ประเภท |
|---|---|---|---|
| 1 | **OEE = A × P × Q** | Availability × Performance × Quality | 🏭 TPM / JIPM |
| 2 | **ค่าเฉลี่ยถ่วงน้ำหนัก** (`wavg`) | A/OEE ถ่วงด้วยเวลารับภาระ · P ถ่วงเวลาเดินเครื่อง · Q ถ่วงจำนวนผลิต | 🏭 |
| 3 | **หัก downtime แบบทับซ้อนช่วงเวลา** | ไม่บวกนาทีตรงๆ แต่หาช่วงที่ทับกับเวลาที่พาร์ทนั้นวิ่งจริง | 📋 |
| 4 | **ไลน์เครื่องขนาน หัก 1/N** | เครื่อง 3 ตัวเสีย 1 ตัว ≠ ไลน์หยุดทั้งไลน์ | 📋 |
| 5 | **`busyMinutes`** | รวมช่วงเวลาแบบ union (ทับกัน = หลายเครื่องเดินพร้อมกัน) เป็นตัวหารของ P | 🧮 interval union |
| 6 | **OEE สดระหว่างกะ** | คำนวณจากข้อมูล ณ ตอนนี้ · **ประเมินไม่ได้ = คืน null ห้ามคืน 0** | 📋 |
| 7 | **OOE / TEEP** | เปลี่ยน "ฐานเวลา" — OOE ใช้เวลากะทั้งหมด · TEEP ใช้ปฏิทิน 24 ชม. | 🏭 |
| 8 | **"OEE จริง"** (strict) | นับหยุดตามแผนเป็นการสูญเสียด้วย → กันการติ๊ก "ในแผน" เพื่อดัน OEE | 📋 |
| 9 | **6 Big Losses / 8 Wastes** | จัดกลุ่มเวลาที่หายไป จาก master ที่หัวหน้าติ๊กเอง ไม่ใช่ระบบเดา | 🏭 TPM / Lean |

### B. การผลิตและการวางแผน

| # | อัลกอริทึม | วิธีคิด | ไฟล์ |
|---|---|---|---|
| 10 | **กำลังผลิตจริง** | **median** ของยอดดีต่อกะ 60 วันล่าสุด (median ตัดวันเทพ/วันหายนะออกเอง) + ช่วง P25–P75 บอกความเสี่ยง | `capacityModel.js` 🏭 |
| 11 | **แผนผลิตแบบ greedy ตามปฏิทิน** | เดินวันต่อวัน: กะเช้า → +กะดึก → +OT → วันหยุด ม.75 → OT วันหยุด (ตามลำดับต้นทุน) | `ProductionPlan` 📋 |
| 12 | **คำนวณจำนวนบัตรคัมบัง** | สูตรถอดจาก Excel เดิมของ planner (ปัดขึ้นเสมอ ⌈x⌉) แยกคัมบังเบิกถอน/สั่งผลิต | `kanbanCalc.js` 🏭 |
| 13 | **"ตอนนี้ควรผลิตได้เท่าไหร่"** | `min(เป้าจากใบที่เปิด, เวลาที่มีให้ผลิต ÷ CT × จำนวนเครื่องที่เดินจริง)` — หักพักและหยุดตามแผน **ไม่หักเครื่องเสีย** | `FactoryMap` 📋 |
| 14 | **งานคู่ LH/RH นับเป็น 1 จังหวะปั๊ม** | แม่พิมพ์คู่ปั๊มครั้งเดียวได้ 2 ชิ้น → ใช้ max ของสองข้าง ไม่บวกกัน | `pairTotals.js` 📋 |
| 15 | **ยุบชั้นขั้นตอน (OP) เข้าพาร์ทจริง** | กันนับซ้ำเมื่อชิ้นเดียวผ่านหลายสถานีที่เปิดใบแยกกัน | `pairTotals.js` 📋 |
| 16 | **WIP ค้างระหว่างขั้น** | `ค้าง = Σยอดสะสมผ่านขั้น − Σยอดสะสมปลายทาง` · เรียงลำดับการไหลจากยอดสะสม (ต้นทาง ≥ ปลายน้ำ) | `wipChain.js` 🧮 |
| 17 | **จำลองสต็อกรายวัน** | ไล่วันไปข้างหน้า หา "วันแรกที่ติดลบ" — ไม่ใช่ดูผลรวมทั้งเดือน | `demandSupply.js` 📋 |
| 18 | **ตรวจ "สแกนรวบ"** | เกณฑ์คิดจาก `qty × CT` ของใบนั้นเอง ไม่ใช่เวลาตายตัว → ปรับตามขนาดงานอัตโนมัติ | `OrderTrace` 📋 |

### C. งานกับข้อความ — ตรงนี้คนมักเข้าใจผิดว่าเป็น AI

| # | อัลกอริทึม | วิธีคิด | ไฟล์ |
|---|---|---|---|
| 19 | **จับกลุ่มหมายเหตุที่พนักงานพิมพ์เอง** | **character 3-gram + Jaccard similarity + containment → greedy clustering** | `textCluster.js` 🧮 |
| 20 | **จับคู่อาการเคลม ↔ บรรทัด PFMEA** | กลุ่มคำเป็น**ตัวกรอง** + n-gram เป็น**ตัวจัดอันดับ** (ไทย↔อังกฤษ n-gram เป็น 0 เสมอ ต้องมีกลุ่มคำช่วย) | `peLink.js` 🧮 |
| 21 | **ตรวจเคลมซ้ำ 2 ระดับ** | ซ้ำแน่ (แดง) vs อาจซ้ำ (เหลือง) — เกณฑ์เดียวแยกไม่ได้ วัดกับข้อความไทยจริงแล้ว | `peLink.js` 🧮 |
| 22 | **จับคู่เลขพาร์ทลูกค้า → เลข SAP** | normalize (ตัดขีด/ช่องว่าง) แล้วเทียบ index · **ชัดเจนตัวเดียวเท่านั้นถึงจับคู่ กำกวม = ไม่เดา** | `matResolve.js` 📋 |

> ⚠️ **ข้อ 19 คือจุดที่ต้องอธิบายให้ชัดถ้าถูกถาม**
> *"ระบบเอาหมายเหตุที่พนักงานพิมพ์มาจัดกลุ่มได้ — ใช้ AI หรือเปล่า"*
> → **ไม่ใช่ครับ** เราไม่ได้ตัดคำภาษาไทย (ซึ่งต้องใช้ dictionary หรือโมเดล) แต่ใช้วิธี
> **เทียบความคล้ายของตัวอักษรทีละ 3 ตัว** ซึ่งไม่ต้องรู้ว่าคำจบตรงไหน ทนคำสะกดต่างกันเล็กน้อยได้
> เป็นวิธีมาตรฐานที่ใช้กันมานาน ไม่ต้องเทรนอะไร ไม่มี dependency และ**ให้ผลเหมือนเดิมทุกครั้ง**

### D. เส้นทางและผัง

| # | อัลกอริทึม | ใช้ทำอะไร | ไฟล์ |
|---|---|---|---|
| 23 | **Dijkstra** | เส้นทางสั้นสุดบนกราฟถนนในโรงงาน | `transportGraph.js` 🧮 |
| 24 | **จัดลำดับจุดจอดสั้นสุด** | TSP แบบล็อกจุดเริ่ม (จุดจอดต่อรอบมีไม่มาก) | `transportGraph.js` 🧮 |
| 25 | **แทรกสี่แยกอัตโนมัติ** | ตรวจเส้นตัดกัน (segment intersection) แล้วแยกถนนให้เอง — ถ้าไม่ทำ กราฟจะเดินไม่ผ่าน | `transportGraph.js` 🧮 |
| 26 | **วางป้ายบนผังไม่ให้ทับกัน** | greedy + นับกรอบไลน์อื่นเป็นสิ่งกีดขวาง · ไม่มีที่ = ซ่อนแล้ว**บอกว่าซ่อนกี่ป้าย** | `FactoryMap` 🧮 |
| 27 | **Convex hull** | วาดกรอบกลุ่มไลน์อัตโนมัติจากกรอบไลน์ลูก | `FactoryMap` 🧮 |
| 28 | **เลือกสัดส่วนผังให้ตัวอักษรใหญ่สุด** | maximize `min(กว้างกระดาษ/กว้างผัง, สูงกระดาษ/สูงผัง)` | `PeFlowChart` 🧮 |

### E. ซ่อมบำรุง

| # | อัลกอริทึม | วิธีคิด | ไฟล์ |
|---|---|---|---|
| 29 | **Rank อะไหล่ A/B/C** | เมทริกซ์ **ใช้เฉลี่ย/เดือน × leadtime** ตาม **WI-JIG-010 Rev.00** ของเราเอง | `spareRank.js` 🏭 |
| 30 | **PM ล่วงหน้า + buffer** | `วันคาด = วันนี้ + (เกณฑ์ − shot สะสม) ÷ อัตราผลิต/วัน` · buffer = อัตรา/วัน × ชม.ที่หยุด ÷ 16 × (1+margin) | `pmPredictive.js` 🏭 |
| 31 | **"PM ค้างไหม ณ วันที่ผลิต"** | ย้อนจาก**ประวัติการตรวจ** ไม่อ่านค่าปัจจุบัน (ค่าปัจจุบันขยับไปแล้วถ้าช่างเพิ่งทำ PM) | `OrderTrace` 📋 |
| 32 | **คะแนนความน่าสงสัยของเครื่อง** | ยืนยันตัวจริง 100 · PM ค้าง 40 · เพิ่งซ่อมเสร็จ ≤7 วัน 30 · ไม่เคยตรวจ 20 · มี DT ในกะ 10 | `OrderTrace` 📋 |
| 33 | **เตือนเครื่องหยุดค้าง** | cron ฝั่ง server ทุก 5 นาที · ค้างเกินเกณฑ์ (ตั้งได้ default 15 นาที) · **กันหยุดตามแผนไม่ให้ยิง** | `downtime-open-scan` 📋 |
| 34 | **เครื่องหยุดซ้ำแต่ยังไม่มีใบซ่อม** | นับเครื่องที่หยุด ≥2 ครั้งใน 30 วัน แล้วเทียบกับใบ MO ที่เปิด | `DeptDashboard` 📋 |

### F. คุณภาพ

| # | อัลกอริทึม | วิธีคิด | ไฟล์ |
|---|---|---|---|
| 35 | **วัดประสิทธิผล 8D/CAPA** | ก่อน/หลังวันที่มาตรการมีผล **หารด้วยจำนวนวันที่ไลน์ผลิตจริง** (ไม่ใช่วันปฏิทิน) → 4 ระดับ | `capaEffect.js` 📋 |
| 36 | **FTT / PPM** | `ของดี ÷ (ดี + เสีย)` — **ไม่ใช่** `(ดี − เสีย) ÷ ดี` ที่หักซ้ำ | `QualityControl` 🏭 |
| 37 | **SPC** | X̄-R chart · Cp / Cpk / Pp / Ppk | `QualityControl` 🏭 AIAG |
| 38 | **Pareto + ABC** | 80/20 จัดกลุ่ม A/B/C ตามสัดส่วนสะสม | หลายหน้า 🏭 |
| 39 | **เรียก QA มาตรวจ (FME)** | derive หน่วยตรวจจาก **ไลน์ × วัน × กะ × รุ่น** — **ไม่ผูกกับเลขออเดอร์** (1 ลอตมี 50 ออเดอร์ได้) | `qa-fme-scan` 📋 |

### G. คนและทักษะ

| # | อัลกอริทึม | วิธีคิด | ที่อยู่ |
|---|---|---|---|
| 40 | **สะสม EXP รายวัน** | +1/วัน ต่อสกิลที่ใช้จริงที่จุดงาน · cap 3 ชั้น (เกณฑ์จุดงาน / เพดานขั้น / หยุดเมื่อรออนุมัติ) | SQL function 📋 |
| 41 | **ปรับรายสัปดาห์** | ทำงาน ≥3 วัน/สัปดาห์ → +2 · ไม่ได้ทำเลย → −2 (ต่ำสุด 25) · **idempotent** รันซ้ำไม่บวกซ้ำ | SQL function 📋 |
| 42 | **ด่านอนุมัติข้ามขั้น** | ชนเพดาน 24/49/74/99 → สร้างคำขอ → หัวหน้าอนุมัติถึงข้ามไป 25/50/75/100 | 📋 |
| 43 | **% ความเหมาะสมกับจุดงาน** | สัดส่วนสกิลที่ผ่านเกณฑ์ขั้นต่ำของจุดนั้น | `computeFit` 📋 |
| 44 | **ตัดสินว่าใครเข้ากะไหน** | ลำดับตายตัว: override รายคน → merge → ตารางกะไลน์ → ตารางกะหน่วยงาน | `shiftAssign.js` 📋 |

### H. ต้นทุน

| # | อัลกอริทึม | วิธีคิด | ไฟล์ |
|---|---|---|---|
| 45 | **ตีมูลค่าความสูญเสียเป็นเงิน** | activity rate ต่อ cost center (**DL / IDP / DP / OH** ตรงกับ SAP) × เวลาที่เสียไป | `costSaving.js` 🏭 standard costing |
| 46 | **ต้นทุนของเสียต่อชิ้น** | `standard_cost` ชนะ → ไม่มีค่อยใช้ `material_cost + (CT × rate)` · **ไม่รู้ต้นทุน = คืน null ห้ามเดาเป็น 0** | `costSaving.js` 📋 |

### I. เครื่องมือ Insight (rule-based ล้วน)

**`OeeInsightPanel`** — 7 กฎ วิเคราะห์อัตโนมัติจากกะที่ปิดแล้ว 14/30/60/90 วัน

1. แตกเป้าที่หายเป็นชิ้น → Downtime กี่ชิ้น / NG กี่ชิ้น / ความเร็วต่ำกว่า CT กี่ชิ้น
2. Downtime เรื้อรัง (ชนิด × เครื่อง ที่เกิด ≥3 กะ)
3. กะเช้า vs กะดึก ต่างกัน ≥5 จุด + ชี้ว่าต่างที่ A หรือ P
4. วันในสัปดาห์ที่ DT หนักผิดปกติ (> 1.6 เท่าของค่ากลาง)
5. คนขาด ↔ OEE — เทียบวันคนครบกับวันคนขาด
6. Product ที่วิ่งช้าซ้ำ (P<75 ใน ≥3 กะ) → ชี้ว่า CT master อาจตั้งเร็วเกิน
7. NG กระจุกประเภทเดียว (≥50% ของ NG รวม และ ≥20 ชิ้น)

> **ทุกข้อมีหลักฐานตัวเลขแนบ** และ **ข้อมูล <3 กะ = บอกว่า "ข้อมูลยังไม่พอ" ไม่เดา**
> นี่คือสิ่งที่คนมักเรียกว่า "AI" — จริงๆ คือ**กฎที่เราเขียนเองและอธิบายได้ทุกบรรทัด**

### J. เบ็ดเตล็ด

| # | อัลกอริทึม | หมายเหตุ |
|---|---|---|
| 47 | **อ่าน QR / บาร์โค้ด** | `BarcodeDetector` ที่ติดมากับเบราว์เซอร์ (ฟรี) · เครื่องที่ไม่มี fallback ไป `jsQR` | 🧮 |
| 48 | **เลขสุ่มแบบคงที่ (seeded)** | `hash32` — ใช้เฉพาะหน้า mockup สาธิต ให้เลขไม่ดิ้นทุกครั้งที่รีเฟรช · **ติดป้ายบอกว่าจำลองเสมอ** | 🧮 |
| 49 | **MediaPipe Gesture** | โมเดล ML ตัวเดียวในระบบ — ดูหัวข้อ AI ด้านบน | 🟡 |

---

## 🧭 หลักการที่ใช้ร่วมกันทุกอัลกอริทึม — ตรงนี้คือจุดขายจริง

ถ้าเขาถามลึกว่า *"แล้วมั่นใจได้ยังไงว่าตัวเลขถูก"* ให้ตอบด้วย 5 ข้อนี้:

| หลัก | ความหมาย | ตัวอย่าง |
|---|---|---|
| **1. "ประเมินไม่ได้" ≠ "เป็นศูนย์"** | ข้อมูลไม่พอ = คืนค่าว่าง + บอกเหตุผล **ห้ามแปลงเป็น 0** | ยังไม่ตั้ง CT → P ไม่ขึ้น 0% แต่ขึ้นว่า "ยังไม่ได้ตั้งรอบเวลา" |
| **2. ห้ามล้มเหลวเงียบ** | ทำไม่ได้ต้องบอกบนจอเสมอ | กดส่งของแล้วตัดสต็อกไม่ได้ → เด้งเตือน ไม่ขึ้นเขียวหลอก |
| **3. สูตรอยู่ที่เดียว** | OEE ทั้งระบบมาจากไฟล์เดียว (`oee.js`) | หัวหน้าไลน์กับผู้บริหารเห็นเลขเดียวกันเสมอ |
| **4. ค่าที่ปิดแล้วล็อก ไม่คำนวณใหม่** | ปิดกะแล้ว OEE ถูก stamp — เปลี่ยน master ทีหลังไม่ย้อนไปแก้ของเก่า | ประวัติไม่เปลี่ยนคำตอบเองเงียบๆ |
| **5. ระบบเสนอ คนตัดสิน** | ทุกจุดที่ระบบ "เดา" จะเสนอให้คนกดยืนยัน ไม่เขียนฐานเอง | ตัวเสนอแก้ PFMEA · จับคู่เลข SAP · AI intake (ถ้าเปิด) |

---

## ❓ คำถามที่น่าจะโดน + คำตอบ

**Q: ใช้ AI ไหม / เป็น Machine Learning หรือเปล่า**
> ไม่ใช่ครับ ตัวเลขทุกตัวมาจากสูตรมาตรฐานที่เปิดตำราตรวจได้ — OEE ตาม TPM, Cp/Cpk ตาม AIAG
> ที่ดูเหมือน AI คือส่วนวิเคราะห์อัตโนมัติ แต่จริงๆ เป็นกฎ 7 ข้อที่เราเขียนเอง อธิบายได้ทุกบรรทัด
> **ข้อดีคือผลลัพธ์เหมือนเดิมทุกครั้ง และ auditor ตรวจย้อนได้**

**Q: ทำไมไม่ใช้ AI ล่ะ**
> 3 เหตุผลครับ (1) สายรถยนต์ต้องอธิบายที่มาของตัวเลขให้ได้ (2) ข้อมูลเราเพิ่งครบ 2 เดือน
> AI ต้องการข้อมูลที่ครบและสะอาดก่อน ไม่งั้นมันเรียนรู้จากข้อมูลที่ผิด
> (3) โจทย์ตอนนี้ยังเป็น "ทำให้เห็น" ซึ่งสูตรธรรมดาทำได้ดีกว่าและถูกกว่า

**Q: OEE 79.5% คิดมายังไง**
> A × P × Q ครับ · A = เวลาเดินเครื่องจริง ÷ เวลาที่ตั้งใจจะเดิน · P = เวลามาตรฐานของงานที่ทำได้ ÷ เวลาเดินเครื่อง
> · Q = ของดี ÷ ของที่ผลิตทั้งหมด · **เฉลี่ยข้ามกะเป็นแบบถ่วงน้ำหนัก ไม่ใช่บวกแล้วหาร**
> — กะที่เดิน 2 ชั่วโมงกับกะเต็ม ถ่วงไม่เท่ากัน · ในระบบมีปุ่ม ⓘ กางตารางให้ดูทีละกะได้เลยครับ

**Q: ข้อมูลเราออกไปนอกบริษัทไหม**
> ไม่ครับ อยู่บนฐานข้อมูลของเราเอง 2 ชุด · ไม่มีการส่งข้อมูลไปให้ AI หรือบริการภายนอกวิเคราะห์
> แม้แต่กล้องในโหมดประชุมก็ประมวลผลในเครื่อง ภาพไม่ออกไปไหน

**Q: ถ้าอยากได้ AI ต้องทำอะไรก่อน**
> ต้องมีข้อมูลครบก่อนครับ — ตอนนี้ฝ่ายผลิตครบแล้ว แต่ใบซ่อม/ใบตรวจคุณภาพยังเพิ่งเริ่ม
> พอครบทุกส่วนงานสิ้นตุลา เราจะมีข้อมูลที่เชื่อมกันจริงๆ ซึ่งเป็นวัตถุดิบของ AI
> **ลำดับคือ ข้อมูลครบ → เห็นภาพ → ทำนาย → ให้คำแนะนำ ข้ามขั้นไม่ได้**

**Q: ต่อกับเครื่องจักรโดยตรง (SCADA) ได้ไหม**
> ได้ครับ และวางแนวทางไว้แล้ว — แต่หลักคือ **ให้ SCADA เป็นเซ็นเซอร์ส่งข้อเท็จจริงมา
> ส่วนสูตรยังอยู่ในระบบเราที่เดียว** ไม่ให้ระบบภายนอกคำนวณ OEE เองแล้วส่งตัวเลขมา
> ไม่งั้นจะมี OEE สองชุดแล้วเถียงกันว่าเชื่อจอไหน · และเครื่องบอกได้แค่ว่าหยุดตอนไหน
> **บอกไม่ได้ว่าทำไม และไม่มีทางรู้ว่าของเสียกี่ชิ้น** — สองอย่างนั้นยังต้องมาจากคน

---

## 🔍 "หมวดสอบกลับ / วิเคราะห์ — ใช้ AI ค้นหาข้อมูลให้หรือเปล่า"

> **คำตอบที่ทำให้เขาเงียบ:**
> **"ไม่ได้ค้นหาครับ — มันไม่ต้องค้น"**

### ทำไมถึงไม่ต้องค้น

ทุกบันทึกในระบบ **ถูกผูกกับใบผลิตตั้งแต่วินาทีที่กดบันทึก** ไม่ใช่ไปตามหาทีหลัง
พนักงานลงเครื่องหยุด → แถวนั้นถือ `session_id` ของกะที่เปิดอยู่ทันที
ปิดใบผลิต → แถวสต็อกถือ `ref_order_id` ของใบนั้นทันที

**เพราะฉะนั้น "ย้อนกลับ" = เดินตามเส้นที่ผูกไว้แล้ว ไม่ใช่ไล่หาในกองข้อมูล**

| | ถ้าใช้ AI ค้นหา | ระบบที่เราทำ |
|---|---|---|
| วิธีตอบ | อ่านข้อมูลแล้วสรุปว่า "น่าจะใช่" | เดินตามความสัมพันธ์ที่ผูกไว้ตอนบันทึก |
| ข้อมูลโตขึ้น 100 เท่า | ช้าลงและแพงขึ้นตามปริมาณ | **แทบไม่ช้าลงเลย** |
| ความแน่นอน | ความน่าจะเป็น | **ชี้ตัวได้แน่นอน 100%** |
| auditor ยอมรับ | ❌ | ✅ |

### ตัวเลขที่เอาไปพูดได้เลย (วัดจริงจากฐานข้อมูลของเรา)

> **สอบกลับ 1 ใบผลิต จากทั้งหมด 9,684 ใบ ใช้เวลา 3 มิลลิวินาที**
> (วัดด้วย `EXPLAIN ANALYZE` บนฐานข้อมูลจริง &mdash; ไม่ใช่ตัวเลขในโบรชัวร์)

อัลกอริทึมเบื้องหลังคือ **B-tree index** &mdash; หลักการเดียวกับการเปิดพจนานุกรม
เปิดกลางเล่ม ดูว่าคำที่หาอยู่ซ้ายหรือขวา แล้วตัดอีกครึ่งทิ้ง ทำซ้ำ

| จำนวนใบผลิต | จำนวนครั้งที่ต้องเปิดดู (โดยประมาณ) |
|---|---|
| 9,684 ใบ (วันนี้) | ~13 ครั้ง |
| 100,000 ใบ | ~17 ครั้ง |
| **1,000,000 ใบ** | **~20 ครั้ง** |

> **ข้อมูลโตขึ้น 100 เท่า แต่งานที่ต้องทำเพิ่มขึ้นแค่ไม่ถึงเท่าตัว**
> นี่คือเหตุผลว่าทำไมระบบถึงตอบเร็วเท่าเดิมได้ตลอด แม้ใช้ไปอีกสิบปี

### ประโยคสำหรับพูดจริง

> "ลองนึกภาพห้องเก็บเอกสารครับ
> **ถ้าใช้ AI ค้น** ก็เหมือนจ้างคนอ่านทั้งห้องแล้วสรุปให้ฟัง &mdash; ทุกครั้งที่ถาม ต้องอ่านใหม่หมด
> เอกสารยิ่งเยอะยิ่งช้า และคำตอบที่ได้คือ 'น่าจะเป็นแฟ้มนี้'
>
> **ระบบเราทำอีกแบบ** &mdash; ตอนที่เอกสารทุกใบถูกสร้าง มันถูกผูกเชือกไว้กับเอกสารที่เกี่ยวข้องแล้ว
> เวลาจะย้อน เราแค่ **ดึงเชือกเส้นเดียว แล้วทั้งพวงตามมา**
> ใช้เวลา 3 มิลลิวินาที และตอบได้ว่า 'ใบนี้แน่นอน' ไม่ใช่ 'น่าจะใช่'
>
> **สำหรับงานสอบกลับในสายรถยนต์ ความแน่นอนสำคัญกว่าความฉลาดครับ**"

### ฝั่งวิเคราะห์ — "ไม่ได้อ่านแล้วสรุป แต่คำนวณ"

ถ้าเขาถามต่อว่าแล้วหมวดวิเคราะห์ล่ะ ให้ยกตัวอย่างที่ฟังแล้วเห็นภาพว่าเป็นคณิตศาสตร์ ไม่ใช่การเดา:

| สิ่งที่คนเห็นบนจอ | เบื้องหลังคืออะไร |
|---|---|
| "%P ของไลน์ที่มีเครื่อง 6 ตัว" | **รวมช่วงเวลาแบบ union** &mdash; ช่วงที่ทับกัน = หลายเครื่องเดินพร้อมกัน จึงไม่นับเวลาซ้ำ |
| "OEE เฉลี่ยของเดือน" | **ค่าเฉลี่ยถ่วงน้ำหนัก** &mdash; กะที่เดิน 2 ชม. ถ่วงไม่เท่ากะเต็ม (บวกแล้วหารคือคำตอบที่ผิด) |
| "มาตรการที่ปิด 8D ได้ผลไหม" | เทียบก่อน/หลัง **หารด้วยจำนวนวันที่ไลน์ผลิตจริง** &mdash; ไลน์หยุด 5 วันแล้วของเสียเป็น 0 ไม่ได้แปลว่าได้ผล |
| "จัดกลุ่มหมายเหตุที่พนักงานพิมพ์เอง" | **เทียบความคล้ายตัวอักษรทีละ 3 ตัว** (3-gram + Jaccard) ไม่ต้องตัดคำไทย ไม่ต้องเทรน |
| "เครื่องตัวไหนน่าสงสัยที่สุด" | **ให้คะแนนตามหลักฐาน** &mdash; ยืนยันตัวจริง 100 · PM ค้าง 40 · เพิ่งซ่อมเสร็จ 30 · ไม่เคยตรวจ 20 |

> **"ทุกตัวเลขที่เห็น เราชี้ได้ว่ามาจากบรรทัดไหนในสูตร**
> และถ้าข้อมูลไม่พอ ระบบจะบอกว่า **'ข้อมูลยังไม่พอ' แทนที่จะเดาให้ &mdash; นี่คือสิ่งที่ AI ทำไม่ได้"**

---

## ✍️ "พนักงานเขียนไม่เหมือนกัน — AI ช่วยได้ไหม หรืออัลกอริทึมที่มีพอแล้ว"

> **คำตอบสั้น: ปัญหามีจริงและวัดได้ · แต่ทางแก้ที่คุ้มที่สุด 3 ข้อแรก ไม่ใช่ AI**

### ปัญหาจริงที่วัดได้ (จาก 5,442 บันทึกที่มีหมายเหตุ)

**เขียนต่างกัน 1,685 แบบ** สำหรับเรื่องที่จริงๆ มีไม่กี่เรื่อง &mdash; ตัวอย่างจากของจริง:

| อาการ | ตัวอย่างที่เจอจริง |
|---|---|
| **สะกดต่างกัน** | `พาเลสเต็มราง` · `พาเลทเต็มราง` · `พาเลสเต๊มราง` · `พ่เลสเต็มราง` · `พาเลสเต็น็มราง` |
| **ไทยปนอังกฤษ ของเรื่องเดียวกัน** | `อารามTC` กับ `Alarm TC` &mdash; **ตัวเดียวกันแท้ๆ** |
| **พิมพ์ผิดภาษาแป้นพิมพ์** | `rkg]lg9H,ik'` = "พาเลสเต็มราง" ที่พิมพ์ตอนแป้นยังเป็นอังกฤษ |
| **หลายสาเหตุในบันทึกเดียว** | `รอไลน assy แก้ไขเครื่องจักร 54 นาที ชิ้นงานเต็มราง 80 นาที` = **134 นาที อยู่ในแถวเดียว** |
| **ใส่เวลาซ้ำกับช่องเวลา** | `Alarm feed nut sp-67 ไม่ไป 5 นาที` (ช่องนาทีก็กรอก 5 อยู่แล้ว) |

### อัลกอริทึมที่มีอยู่ จับได้แค่ไหน — วัดแล้ว

**จับคู่ข้อความที่คล้ายกันได้ 1,725 คู่** (ที่ความคล้าย ≥45%)

| | ผล |
|---|---|
| สะกดต่างกันเล็กน้อย | ✅ **จับได้ดี** &mdash; `พาเลส` ↔ `พาเลท` ↔ `พาเลสเต๊ม` รวมเป็นกลุ่มเดียว |
| ไทย ↔ อังกฤษ | ❌ **จับไม่ได้เลย** &mdash; `อาราม` กับ `alarm` ไม่มีตัวอักษรร่วมกันสักตัว |
| พิมพ์ผิดภาษาแป้น | ❌ จับไม่ได้ |
| แยกหลายสาเหตุในแถวเดียว | ❌ ทำไม่ได้โดยธรรมชาติของวิธีนี้ |

### 🎯 แต่ของจริงคือ — ปัญหา 3 ใน 4 แก้ได้โดยไม่ต้องใช้ AI

แยกดูว่า **"อื่นๆ" 26,322 นาที** จริงๆ แล้วเป็นเรื่องอะไรบ้าง:

| เรื่อง | นาที | % ของ "อื่นๆ" |
|---|---|---|
| รอกระบวนการ / รอของ (HDF · Bending · Laser · Store) | 8,586 | **32.6%** |
| พาเลท / รางเต็ม | 4,747 | **18.0%** |
| เครื่องแจ้งเตือน (Alarm) | 4,122 | **15.7%** |
| โรบอท-จิ๊ก วางงานไม่ลง | 1,073 | 4.1% |
| เคาะเศษ / สต๊อปเปอร์ | 1,058 | 4.0% |
| แก้ไขคุณภาพ | 592 | 2.2% |
| **รวม 6 เรื่องข้างบน** | **20,178** | **🔥 76.7%** |
| ที่เหลือกระจัดกระจายจริงๆ | 6,144 | 23.3% |

> **ตั้งประเภท downtime เพิ่ม 6 ตัว → "อื่นๆ" หายไป 3 ใน 4 ทันที**
> ไม่ต้องเขียนโค้ด ไม่ต้องจ่ายค่า AI ไม่ต้องรออะไร &mdash; ทำได้วันนี้ที่หน้า ⚙️ ตั้งค่า
>
> และ **ระบบชี้เรื่องนี้ให้เห็นอยู่แล้ว** &mdash; ตัวจับกลุ่มมีข้อความชวนว่า
> *"กลุ่มนี้กินเวลา ≥15% ของอื่นๆ ควรตั้งเป็นประเภทของตัวเอง"* แค่ยังไม่มีใครไปตั้ง

### ลำดับที่คุ้มที่สุด (เรียงตามความคุ้ม ไม่ใช่ตามความเท่)

| ลำดับ | ทำอะไร | ได้อะไร | ต้นทุน |
|---|---|---|---|
| **1** | **ตั้งประเภท downtime เพิ่ม 6 ตัว** | คืน **76.7%** ของ "อื่นๆ" | ⏱️ ครึ่งวัน · 0 บาท |
| **2** | แปลงข้อความที่พิมพ์ผิดแป้น (`rkg]lg` → `พาเลส`) | จับกลุ่มได้เพิ่ม | ~30 บรรทัด · แม่นยำ 100% |
| **3** | ขยายรายการคำพ้องไทย↔อังกฤษ (`อาราม`=`alarm`) | ปิดช่องโหว่ที่ n-gram ทำไม่ได้ | มีโครงอยู่แล้วใน `peLink.js` |
| **4** | ให้ฟอร์มลง **หลายสาเหตุในครั้งเดียว** ได้ | เลิกมี "134 นาทีในแถวเดียว" | แก้ฟอร์ม ไม่ใช่แก้อัลกอริทึม |
| **5** | **ค่อยพูดเรื่อง AI** | ช่วยตอนพิมพ์ (ต้นทาง) | ต้องมี API key + ค่าใช้จ่ายต่อครั้ง |

### แล้ว AI ช่วยตรงไหนได้จริง (ถ้าจะทำ)

**จุดที่ AI ชนะอัลกอริทึมชัดเจน คือ "ตอนพิมพ์" ไม่ใช่ "ตอนวิเคราะห์ย้อนหลัง"**

- ป้องกันที่ต้นทาง &mdash; พนักงานพิมพ์อิสระ แล้ว AI แปลงเป็นฟิลด์ให้ก่อนบันทึก
  (เครื่องไหน · อาการอะไร · กี่นาที) แล้ว **คนกดยืนยัน**
- แยกหลายสาเหตุออกจากประโยคเดียวได้ ซึ่งการจับกลุ่มทำไม่ได้
- เข้าใจไทยปนอังกฤษปนคำแสลงหน้างานโดยไม่ต้องมานั่งทำรายการคำพ้อง

**แต่ข้อควรระวังที่ต้องพูดให้ครบ:**

| | |
|---|---|
| ✅ **ต้องมีปุ่มยืนยันเสมอ** | AI เขียนฐานข้อมูลเองไม่ได้ &mdash; เป็นกฎที่ล็อกไว้ตั้งแต่ออกแบบ |
| ✅ **ต้อง ground กับ master จริง** | ให้เลือกจากทะเบียนเครื่อง/ประเภทที่มีอยู่ ไม่ใช่ให้มันแต่งชื่อเครื่องเอง |
| ⚠️ **มีค่าใช้จ่ายต่อครั้ง** | ต่างจากอัลกอริทึมที่รันฟรีไม่จำกัด |
| ⚠️ **ผลลัพธ์ไม่นิ่ง 100%** | ข้อความเดียวกันอาจได้ผลต่างกันเล็กน้อย &mdash; ยอมรับได้ตอน "ช่วยกรอก" แต่ยอมรับไม่ได้ตอน "คำนวณ OEE" |

> **ประโยคปิดที่ตอบได้ทั้ง 2 คำถาม:**
> "ปัญหาการเขียนไม่เหมือนกันมีจริงครับ เราวัดแล้ว &mdash; 1,685 แบบ จาก 5,442 บันทึก
> แต่พอแยกดู **3 ใน 4 เป็นแค่ 6 เรื่องที่ควรตั้งเป็นตัวเลือกให้กดตั้งแต่แรก**
> เพราะฉะนั้นสิ่งที่ควรทำก่อนไม่ใช่เอา AI มาตีความข้อความ
> **แต่คือทำให้พนักงานไม่ต้องพิมพ์ตั้งแต่แรก**
> AI จะมีประโยชน์ตอนที่เหลือ 23% ที่กระจัดกระจายจริงๆ ซึ่งเป็นก้าวถัดไปครับ"

---

## 📎 ที่มา

- โค้ดทั้งหมดอยู่ใน `src/utils/` และ `src/lib/` — ส่วนใหญ่เป็น pure function เทสแยกได้
- กติกาแต่ละข้อบันทึกไว้ใน `CLAUDE.md` (หัวข้อ OEE · Traceability · Closed-loop 8D)
- รายการ edge function ที่ deploy จริง ตรวจได้จาก Supabase dashboard

> ⚠️ **ก่อนเอาไปพูด ให้ตรวจ 2 อย่าง:** (1) `telegram-webhook` ยัง**ไม่**อยู่ในรายการ deploy
> (2) `package.json` ยังไม่มี dependency ของผู้ให้บริการ AI รายใด
> ถ้าวันหนึ่งเปิดใช้จริง **ต้องกลับมาแก้เอกสารนี้ทันที** — พูดว่า "ไม่ใช้ AI" ทั้งที่ใช้อยู่ เสียเครดิตกว่าไม่พูดเลย
