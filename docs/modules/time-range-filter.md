# ⏱️ ตัวกรองช่วงเวลามาตรฐาน — ใช้เหมือนกันทุกหน้า (2026-09-23 · คำสั่ง user)

> *"ระบบการกรองช่วงเวลา อยากไห้เป็นมาตรฐานเดียวกัน มีเหมือนกันทุกหน้า คือ เลือกสเกลที่จะดู
> รายวัน สัปดาห์ เดือน ปี และกรอบเวลา แบบนี้ และ scope 30วันย้อนหลัง 60 90 120 แบบนี้"*

## สภาพก่อนทำ — สำรวจทั้งรีโป 23/09

**18 ไฟล์มีตัวกรองเวลา แต่เป็น 4 แบบที่เข้ากันไม่ได้เลย**

| แบบ | หน้า | ปัญหา |
|---|---|---|
| ปุ่มสเกล `daily\|weekly\|monthly\|yearly` + from/to | `/oee-analytics` | ไม่มีปุ่มย้อนหลัง |
| `period` = `week\|month\|year` | จอ SQDCM | ไม่มี "วัน" · ไม่มี from/to |
| dropdown `days` = 30/60/90 | `/mtn-analysis` · MtnKpiPanel · MachineReliability · OeeInsightPanel | ไม่มีสเกล ไม่มี from/to |
| from/to เปล่า | QualityControl · WorkforceInsight · Energy · OrderTrace · ProductHistory · QualityBins · SymptomSearch · OrderIntakeLog · Report | ไม่มีสเกล |

🔴 **คำเดียวกันคนละความหมาย**: `period` ที่ `/oee-analytics` = *ขนาดถัง* · `period` ที่จอ SQDCM = *ช่วงที่ดู*
⇒ คนหน้างานย้ายหน้าทีก็ต้องเรียนรู้ตัวกรองใหม่ทุกครั้ง

## โครง 3 ชั้น — แยกกันชัดๆ ห้ามปนกัน

1. **สเกล (`scale`)** = ขนาด "ถัง" ที่เอาข้อมูลไปรวม — `day` / `week` / `month` / `year`
2. **กรอบเวลา (`from`–`to`)** = ช่วงที่ดึงข้อมูล — แก้เองได้เสมอ
3. **ปุ่มลัด** = เติม `from`–`to` ให้เร็วๆ (ย้อนหลัง **30/60/90/120 วัน**)

## ไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `src/utils/timeRange.js` | **pure · มีเทส 12 เคส** — `TIME_SCALES` · `LOOKBACK_DAYS` · `presetRange` · `rangeDays` · `bucketKey` · `bucketLabel` · `scaleWarning` · `isoWeek` |
| `src/utils/useTimeRange.js` | hook ผูกกับ URL (`?scale=&from=&to=`) ผ่าน `mergeParams` |
| `src/components/TimeRangeBar.jsx` | แถบบนจอ — สเกล · ปุ่มย้อนหลัง · from/to · `children` ของเฉพาะหน้า · ปุ่มโหลด |
| `src/utils/workDate.js` | **วันทำงานจุดเดียว** (`getWorkDate` · `getCurrentShift` · `localDateStr`) · มีเทส 4 เคส |

## 🔴 กฎที่ฝังไว้ อย่าไปรื้อ

1. **ปุ่มย้อนหลังไม่ใช่ "โหมด"** — กดแล้วเติมวันลงช่อง จากนั้นคนแก้วันต่อได้ทันที ·
   ไฮไลต์ปุ่มเฉพาะตอนช่วงตรงเป๊ะ (`matchPreset`) ⇒ แก้วันเองแล้วปุ่มเลิกไฮไลต์เอง
   **ห้ามทำเป็น state แยก** (จะเกิดคำถาม "ทำไมกดวันแล้วเด้งกลับ")
2. **สเกลไม่เข้ากับช่วง = เตือน ไม่บล็อก** (`scaleWarning`) — รายปี บน 30 วัน = แท่งเดียว ·
   รายวัน บน 2 ปี = 730 แท่ง · **ห้ามวาดกราฟแท่งเดียวเฉยๆ แล้วปล่อยให้เข้าใจว่านั่นคือแนวโน้ม**
3. **ส่วนที่หน้านั้นไม่ได้ใช้ ให้ซ่อน** (`scales={null}` / `presets={[]}`) — ปุ่มตายแย่กว่าไม่มีปุ่ม
4. **`rangeDays` / `presetRange` นับหัวนับท้าย** — "ย้อนหลัง 30 วัน" ต้องได้ **30 วันเป๊ะ**
   (บั๊กคลาสสิกคือลืม `-1` แล้วได้ 31 · มีเทสล็อกทุกปุ่ม)
5. **คีย์สัปดาห์ = "วันที่ของวันจันทร์" ไม่ใช่ `2026-W39`** โดยตั้งใจ — เอาไปบวกวันต่อได้ตรงๆ
   (โค้ดเติมช่องว่างบนแกนเดินทีละ 7 วันจากคีย์) และป้ายบอกช่วงวันจริง ("21–27/9")
   ซึ่งหน้างานอ่านรู้เรื่องกว่าเลขสัปดาห์ · ต้องการเลขสัปดาห์ ISO ใช้ `isoWeek()`
6. **ตัวแบ่งถัง/ป้ายแกนเวลา อยู่ใน `timeRange.js` ที่เดียว — ห้ามเขียนกลับมาในหน้า**
   (ไม่งั้นกลายเป็นสูตรคนละชุดอีกรอบ ซึ่งคือปัญหาที่งานนี้มาแก้)

## กับดักที่เจอจริงตอนทำ (อย่าพลาดซ้ำ)

- 🔴 **`isoWeek` เพี้ยนไป 1 สัปดาห์** — ใส่ออฟเซ็ต `+4` แทน `+3`
  (**1970-01-01 เป็นวันพฤหัส ไม่ใช่ศุกร์**) ⇒ วันจันทร์ถูกนับเป็นสัปดาห์ก่อนหน้าทั้งระบบ
  · เทสจับได้ทันที · ยืนยันซ้ำด้วยการเทียบกับอัลกอริทึมอ้างอิงครบทุกวัน **2019–2031 = ตรง 4,748/4,748**
- **`<input type="date">` ต้องกำหนด `width` เอง** — `index.css` ตั้ง `input{width:100%}` ทั้งแอป
  ไม่กำหนดแล้วมันกินเต็มแถวแล้วดันปุ่มแตกบรรทัด

## 🔴 หลักฐานว่าปัญหานี้ทำให้ตัวเลขไม่ตรงกันจริง — "30 วัน" คนละเลข

ตรวจ 23/09 ด้วยการรันสูตรของแต่ละหน้าบนวันเดียวกัน (23/09/2026):

| หน้า | โค้ดเดิม | ช่วงที่ได้ | **จำนวนวันจริง** |
|---|---|---|---|
| `/quality-control` ปุ่ม "30 วัน" | `daysAgoStr(30)` | 24/08 – 23/09 | **31 วัน** ❌ |
| `/workforce-insight` ค่าตั้งต้น | `daysAgoStr(29)` | 25/08 – 23/09 | 30 วัน ✅ |
| ของกลาง `presetRange(30)` | — | 25/08 – 23/09 | **30 วัน** ✅ |

⇒ 2 จอเขียนป้ายเหมือนกันว่า "30 วัน" แต่ดึงคนละช่วง · ตัวเลขที่เอาไปเทียบกันจึงไม่ตรงโดยที่ไม่มีใครรู้
**`presetRange()` นับหัวนับท้ายเสมอ + มีเทสล็อกทุกปุ่ม** (30/60/90/120 ต้องได้จำนวนวันตรงป้ายเป๊ะ)

## หน้าที่แปลงแล้ว

| หน้า | สเกล | ปุ่มย้อนหลัง | หมายเหตุ |
|---|---|---|---|
| `/oee-analytics` แท็บ 📊 แนวโน้ม | ✅ 4 ระดับ | ✅ | `defaultScale: 'month'` · `defaultDays: 90` — **คงพฤติกรรมเดิมของหน้านี้** ห้ามเปลี่ยนเป็น 30 ตามค่ากลาง · ลบ `fmtWeekKey/fmtMonthKey/fmtYearKey/fmtWeekLabel/fmtMonthLabel/fmtDayLabel` ในหน้าทิ้งแล้ว |
| `/mtn-analysis` แท็บ QC7 | ✅ | ✅ | เดิมมีแต่ dropdown "ย้อนหลัง N วัน" — **เลือกช่วงในอดีตไม่ได้เลย** · คิวรีเดิมมีแต่ `gte(since)` ไม่มีขอบบน ⇒ เพิ่ม `lt(until)` ให้ · แกนสัปดาห์เดิมนับถอยจาก "วันนี้" เปลี่ยนเป็นไล่จากช่วงที่เลือกจริง |
| `/quality-control` | — (`scales={null}`) | ✅ | แก้ off-by-one ข้างบน |
| `/workforce-insight` (2 แท็บ) | — (`scales={null}`) | ✅ | 2 แท็บใช้ `?from=&to=` ร่วมกัน ⇒ สลับแท็บแล้วช่วงเวลาไม่หาย |
| `/order-trace` | — | ✅ | หน้าสอบกลับ ไม่ได้แบ่งถังเวลา |
| `/product-history` | — | ✅ | `defaultDays: 90` คงพฤติกรรมเดิม |
| `/qa` แท็บถังเหลือง/ถังแดง (`QualityBins`) | — | ✅ | |
| `/order-trace` แท็บอาการ (`SymptomSearch`) | — | ✅ | `defaultDays` = ค่าเดิมของหน้า |
| `OrderIntakeLog` | — | ✅ | `defaultDays: 14` คงพฤติกรรมเดิม |
| `/report` 3 แผงช่วงเวลา (จุดงาน · ช่วงวัน · 4M) | — | ✅ | `defaultDays: 7` คงของเดิม · แผง 4M รับ deep-link `?from=` ได้เหมือนเดิม (hook อ่าน param เดียวกัน) |
| `MtnKpiPanel` · `MachineReliability` · `OeeInsightPanel` | — | ✅ | เดิมเป็น dropdown "N วันล่าสุด" · 🔴 **แก้ของจริงด้วย**: คิวรีเดิมนับถอยจาก "ตอนนี้" เสมอ ⇒ เลือกช่วงในอดีตแล้วจำนวนวันถูกแต่**หน้าต่างเวลาผิด** · เปลี่ยนไปยึด `from`–`to` จริงทั้ง 3 ตัว |
| `/obeya?tab=sqdcm` | — (ดูหมายเหตุ) | ✅ | **เก็บปุ่ม สัปดาห์/เดือน/ปี เดิมไว้ + เพิ่มกรอบวันที่/ปุ่มย้อนหลังทับได้** (คำสั่ง user 23/09) |

### 🖥️ จอ SQDCM — ทำไมไม่แปลงปุ่มเป็น "สเกล"

ปุ่ม สัปดาห์/เดือน/ปี ของจอนี้แปลว่า **"ดูช่วงไหน"** (สัปดาห์นี้ / เดือนนี้ / ปีนี้)
**ไม่ใช่ "ขนาดถัง"** แบบหน้าอื่น ⇒ แปลงตรงๆ = เปลี่ยนความหมายของจอที่หน้างานคุ้นอยู่แล้ว
user เคาะ 23/09: **เอาทั้งสองอย่าง** ⇒ เก็บปุ่มเดิม + เพิ่ม `custom` range ทับได้
- แก้วันเอง / กดปุ่มย้อนหลัง → `custom` ชนะ · ป้ายบนจอเปลี่ยนเป็น **"กำหนดเอง (N วัน)"**
- กดปุ่ม สัปดาห์/เดือน/ปี → ล้าง `custom` กลับโหมดเดิมทันที
- **ช่วงเทียบ (prev) ของกรอบกำหนดเอง = ช่วงยาวเท่ากันที่ติดกันข้างหน้า** (เทียบ "สัปดาห์ก่อน" ไม่ได้ เพราะกรอบเองไม่ใช่หน่วยปฏิทิน)
- **ไม่ผูก URL** ต่างจากหน้าอื่นโดยตั้งใจ — จอนี้เปิดค้างบน TV ด้วยลิงก์ `?tab=sqdcm`
  ถ้าเขียน `?from=&to=` ลงไปด้วย ลิงก์ที่แปะไว้บนจอจะค้างอยู่ช่วงเก่าตลอดไป
- แถบไม่โชว์ในโหมดจอ TV (ไม่มีคนกด)

> ทดสอบจริงด้วย Playwright: ค่าเริ่มต้น `รายเดือน` + `90` ไฮไลต์ · กด `60` แล้ววันที่เปลี่ยนเป็น
> 60 วันเป๊ะและปุ่มสลับไฮไลต์ · กด `รายปี` บนช่วง 60 วัน แล้วคำเตือนขึ้นจริง · ไม่ crash ไม่มี pageerror
> ⚠️ harness ใช้ `MemoryRouter` ⇒ **ตรวจ URL sync ในนั้นไม่ได้** (`window.location` ไม่ขยับ) —
> เส้นทางเดียวกับ `useTabParam` ที่ใช้งานจริงอยู่แล้วทั้งระบบ

## ⛔ ที่ **ไม่แปลง** — และทำไม (ตรวจของจริงแล้ว ไม่ใช่ตกหล่น)

การสำรวจรอบแรกใช้ grep จึงนับเกินมา 4 ไฟล์ · เปิดดูของจริงแล้วพบว่า**ไม่มีตัวกรองช่วงเวลา**:

| ไฟล์ | ของจริงคืออะไร |
|---|---|
| `/energy` | **ตัวเลือกเดือนเดียว** (`month_key`) ไม่ใช่ช่วง · ที่ grep เจอเป็นช่อง "มีผลตั้งแต่" ของค่า Emission Factor = **ฟิลด์ในฟอร์ม** · แปลงเป็น from–to = เปลี่ยนโครงคิวรีทั้งหน้า (ฟีเจอร์ใหม่ ไม่ใช่การจัดมาตรฐาน) |
| `/planner-sales` · `/production-plan` | "รายวัน/รายเดือน" ของมันคือ **แท็บ `useTabParam`** (ระยะเวลาที่วางแผนไปข้างหน้า) ไม่ใช่กรองข้อมูลย้อนหลัง |
| `/lpa` | วันที่ทั้งหมดเป็น **ช่องในฟอร์ม** (วันตรวจ · ช่วงเฝ้าระวัง Issue Date) ไม่ใช่ตัวกรองมุมมอง |

> **กฎ:** ตัวกรองที่หน้าไม่ได้ใช้จริง **ห้ามยัดแถบมาตรฐานลงไป** — ปุ่มตายแย่กว่าไม่มีปุ่ม
> (กติกาข้อ 3 ด้านบน) · ถ้าวันหลังอยากให้ `/energy` ดูข้ามเดือนได้ = งานฟีเจอร์ ต้องสั่งแยก

## ⚠️ หนี้ที่เจอระหว่างทาง ยังไม่แก้ — `getWorkDate` ถูกก๊อป **27 ไฟล์**

CLAUDE.md เขียนกฎ "ใช้ `getWorkDate()`" มาตั้งแต่ต้นโปรเจค แต่**ไม่เคยมี util กลางจริง** —
แต่ละหน้านิยามเองซ้ำ 27 ชุด (`getWorkDate` · `getWorkDateStr` · inline arrow)
⇒ แก้กฎทีต้องไล่แก้ 27 ที่ · สักวันต้องมีที่ตกหล่น

`src/utils/workDate.js` สร้างแล้วเป็น single source (**โค้ดใหม่ต้อง import จากที่นี่**)
แต่ **ยังไม่ได้ย้าย 27 จุดเดิม** — เป็นงานกวาดแยก ควรทำพร้อมเพิ่มด่าน `regressionGuards`
ห้ามนิยาม `getWorkDate` เองในไฟล์ (เพิ่มด่านก่อนกวาดไม่ได้ เพราะจะต้อง `allow` 27 ไฟล์ซึ่งผิดกติกาของด่าน)
