# 🏛️ OBEYA — ห้องบัญชาการโรงงาน (`/obeya`)

> หน้า **`src/components/ObeyaSqdcmBoard.jsx`** (แท็บ `?tab=sqdcm` ของ `/obeya` — ดูกล่องด้านล่าง) · KPI `src/utils/obeyaKpi.js` · migration `20260915_obeya_action_loop.sql` (Main — **apply แล้ว 2026-09-15**)
> ออกแบบ/สำรวจก่อนลงมือ: `docs/OBEYA-DESIGN.md` — **อ่านก่อนแก้หน้านี้เสมอ** (มีเหตุผลว่าทำไมหน้านี้ถึงไม่ใช่ dashboard ใบที่ 10)

---

> ## 🔀 2026-09-15 — `/obeya` เป็น **หน้าเดียว 2 แท็บ** (รวมงาน 2 session)
> สอง session ทำ `/obeya` คนละมุมพร้อมกันโดยไม่รู้ว่าอีกฝั่งทำอยู่ (`claude/obeya-kpi-dashboard-85xbgm`
> กับ `claude/oee-report-ppt-export-tjojy0`) — ทั้งคู่มาจากคำสั่ง user คนละครั้งและ**ไม่ทับกัน** จึงรวมเป็นแท็บ:
>
> | แท็บ | คือ | ไฟล์ | เอกสาร |
> |---|---|---|---|
> | `?tab=kpi` (default) | 📋 บอร์ด KPI ส่วนงาน — ราย**เดือน** × กลุ่มไลน์ × 8 หัวข้อ (กระดาษที่แปะผนัง) | `src/components/ObeyaKpiBoard.jsx` | `docs/modules/obeya-kpi-board.md` |
> | `?tab=sqdcm` | 🖥️ จอมอนิเตอร์ SQDCM — ราย**วัน/สัปดาห์/เดือน** × แกน SQDCM + ACTION BOARD | `src/components/ObeyaSqdcmBoard.jsx` | `docs/modules/obeya.md` |
>
> `src/pages/Obeya.jsx` เหลือเป็น**เปลือกสลับแท็บ** (`useTabParam`) เท่านั้น
>
> **🗺️ มีบอร์ดสถานะ SQDCM อีกใบบน `/factory-map` (แผงขวา โหมด 🏛️ OBEYA สด · 2026-09-22)** —
> ตั้งใจให้ **คนละหน้าที่** กับหน้านี้ ห้ามยุบรวม: ของที่นั่นคือ *"ตอนนี้เรื่องไหนมีปัญหา และไลน์ไหน"*
> (สด · ไม่มีเป้ารายเดือน · ไม่มีกราฟ · ไม่มี ACTION BOARD) เป็น**ทางเข้า**สู่ผัง ไม่ใช่ห้องประชุม
> · ตรรกะอยู่ `src/utils/obeyaLive.js` ซึ่ง **ยืม `statusOf`/`STATUS_COLOR`/`SAFETY_PROXY_NOTE`
> จาก `obeyaKpi.js`** และยืมเกณฑ์สีรายไลน์จาก `METRICS.<x>.cat` ของผังเอง — **ห้ามให้ไฟล์นั้น
> คิดเกณฑ์สีเอง** (จะกลายเป็นชุดที่ 3 ที่ตอบสถานะเดียวกันคนละสี) · รายละเอียด →
> `docs/modules/factory-master-map.md` §Side panel ขวา
> **🔴 ห้ามยุบ 2 แท็บเป็นบอร์ดเดียว** — คนละหน่วยเวลา (เดือน vs วัน) · คนละแกนตัด (กลุ่มไลน์ vs แกน SQDCM)
> · คนละเจ้าของตัวเลข (บัญชี/ลูกค้ากรอกมือ vs ระบบคำนวณสด) · ยุบรวม = ไม่ตรงกระดาษและไม่ตรงหน้างานพร้อมกันทั้งคู่

---

## 1. หน้านี้มีไว้ทำอะไร (และ "ไม่ใช่" อะไร)

ESM มีจอภาพรวมอยู่แล้ว 9 ใบ — Obeya จะคุ้มค่าก็ต่อเมื่อทำ **3 อย่างที่จอเดิมทำไม่ได้**:

| # | สิ่งที่จอเดิมทำไม่ได้ | หน้านี้ทำยังไง |
|---|---|---|
| 1 | จอเดิมตัดตาม**ส่วนงาน/ไลน์** (ใครรับผิดชอบ) | Obeya ตัดตาม **แกน S-Q-D-C-M** (ลูกค้า/ผู้บริหารมองอะไร) — แกนเดียวดึงข้ามส่วนงานมาชนกัน |
| 2 | จอเดิมตอบ "ตอนนี้เท่าไหร่" | Obeya ตอบ **"ห่างเป้าเท่าไหร่ · ดีขึ้นหรือแย่ลงจากงวดก่อน"** (ทุกแผ่นมีเป้า + Δ + เทียบ `prevRange`) |
| 3 | จอเดิม**ไม่มีที่บันทึกสิ่งที่ตกลงกันว่าจะแก้** | **ACTION BOARD** = ที่เดียวของหน้าที่เขียนข้อมูลได้ (ผูก `kpi_key` กับแกนที่จะไปแก้) |

**ไม่ใช่:** หน้าแก้ข้อมูลผลิต · ไม่ใช่หน้าวิเคราะห์ลึก (ลึกไปที่ `/oee-analytics`) · ไม่ใช่จอ Andon หน้าไลน์ (`/tv`)

---

## 2. 🖼️ ผัง "กระดาษ A4 ปูเต็มจอ" (คำสั่ง user 2026-09-15)

> **23/09:** ชิ้นส่วนของผังนี้ (`useSheetGrid` · `Sheet` · `StatusLamp` · `WarnNote` · `EmptyChart`) อยู่ที่ `src/components/ObeyaSheet.jsx`
> ใช้ร่วมกับแท็บ 📋 บอร์ด KPI ส่วนงาน (คำสั่ง user "2 แท็บควรรูปแบบเดียวกัน") — **แก้หน้าตาแผ่นที่ไฟล์นั้นที่เดียว**

> *"เปิดจอทีวี 70 นิ้ว ให้เหมือนมีกระดาษไซส A4 ติดเต็มหน้าจอ"* + *"หน้ารวมใน 1 หน้าที่มอนิเตอร์ทุกด้าน ลักษณะกราฟ"*

**คณิตที่ทำให้มันลงตัวพอดี:**
```
A4 แนวตั้ง 210×297  ⇒ กว้าง/สูง = 0.7071
จอ 16:9 (สูง = 1)   ⇒ แบ่ง 2 แถว ⇒ แผ่นสูง 0.5 ⇒ แผ่นกว้าง 0.354
                      1.778 ÷ 0.354 = 5.03  ⇒ **5 คอลัมน์ × 2 แถว = 10 แผ่น เต็มจอพอดี**
```
- แผ่นแนวตั้ง 1 ช่อง = S · Q · D · C · M (แถวบน)
- แผ่นแนวนอน (A4 landscape) = 2 ช่องติดกัน = **OEE** และ **ACTION BOARD** (แถวล่าง) · ตรงกลางแถวล่าง = Pareto "ทำไมหลุดเป้า"
- รวม **8 component / 10 ช่อง**

**วัดจริง (Playwright 2026-09-15 · โหมดจอ TV):**

| จอ | ขนาดแผ่น | อัตราส่วน | ตกขอบ? |
|---|---|---|---|
| 3840×2160 (4K TV) | 740×1046 | **0.707** | ไม่ |
| 1920×1080 (70" FHD) | 358×506 | **0.707** | ไม่ |
| 1366×768 (โน้ตบุ๊ก) | 247×350 | **0.707** | ไม่ |
| 1280×1024 (4:3) | 245×346 | **0.707** | ไม่ |
| 820×1180 / 390×844 (แนวตั้ง) | — | 0.707 | สลับเป็น **โหมดเลื่อน 1-2 คอลัมน์** อัตโนมัติ |

### ⚠️ กับดัก 2 ข้อที่เจอตอนทำ (ทั้งคู่ "พังเงียบ" — build/lint/เทสผ่านหมด)

1. **`clientHeight` รวม padding เสมอ** → เดิมวัดกล่องที่มี `padding: 8` แล้วเอาไปคิดความสูงแผ่น
   ⇒ แถวล่างสูงเกินกรอบ 16px แล้วถูก `overflow: clip` **ตัดหายเงียบ** (ปุ่ม "เจาะ OEE" หายทั้งปุ่ม)
   **กฎ: กล่องนอก = ใส่ padding · กล่องใน (ที่ถูก ResizeObserver วัด) = ห้ามมี padding**
2. **`height: 100vh` ไม่ได้แปลว่าเต็มจอ** — หน้านี้อยู่ใน `<main>` ที่มี sidebar + padding
   ⇒ 100vh ล้นออกนอกจอ · โหมดจอ TV ต้องใช้ **`position: fixed; inset: 0`** (+ `requestFullscreen()` best-effort)

### กฎของผังนี้ (ถ้าจะเอา pattern ไปใช้จอ TV ใบอื่น)
- วัดขนาดจาก **container จริงด้วย ResizeObserver** ห้าม hardcode / ห้ามใช้ media query
  (จอ TV แต่ละรุ่น devicePixelRatio + overscan ไม่เท่ากัน — hardcode แล้วมี scrollbar โผล่บางเครื่อง)
- **ห้ามใช้ `dvh/svh` (Cr 108) · `@container` (105) · `color-mix()` (111) · CSS nesting (112)** — เพดานจอ TV = **Chromium 94** (webOS 23)
- ฟอนต์ต่ำสุด 11px เสมอ (`fs()` ใน `Sheet`) — แผ่นเล็กจนฟอนต์จะต่ำกว่านั้น ให้**ลดจำนวนคอลัมน์** ไม่ใช่ลดฟอนต์

---

## 3. แกน SQDCM — มาจากไหน · เชื่อได้แค่ไหน

ทุกสูตรอยู่ใน `src/utils/obeyaKpi.js` (pure · มีเทส 16 เคส) — **ห้ามคำนวณซ้ำในหน้า**

| แกน | ตัวเลข | แหล่ง | ความน่าเชื่อ |
|---|---|---|---|
| **S** | ใส่ PPE ครบ % (นับเฉพาะคนที่ `is_present`) | `daily_production_logs` (Main) | 🔴 **`state: 'thin'` ถาวร** — ยังไม่มีทะเบียนอุบัติเหตุ/near-miss ⇒ ทำ KPI "วันปลอดอุบัติเหตุ" ไม่ได้ · เป็น leading indicator เท่านั้น |
| **Q** | %Q ถ่วงด้วยจำนวนผลิต (ค่าที่ stamp ตอนปิดกะ) | `production_sessions` + `defect_logs` (DR) | 🟡 `thin` เมื่อ `defect_logs` < **400 แถว**/ช่วง — ของจริงมี 179 แถวเทียบ downtime 8,627 = บันทึกไม่ครบ |
| **D** | ผลิตได้ตามแผน % จากใบงาน | `prod_orders` (DR) | 🟢 |
| **C** | บาทที่เสียไป = เครื่องหยุด**นอกแผน** × ค่าแรง/ชม. + ของเสีย × ต้นทุน/ชิ้น | `downtime_logs` + `cost_center_rates` + `parts_master` | 🟡 ขาดอัตรา/ต้นทุน = **คืน `null` + บอกว่าขาดอะไร ห้ามแสดง 0 บาท** |
| **M** | อัตรามาทำงาน % | `daily_production_logs` (Main) | 🟢 (⚠️ ไม่ใช่ "ขวัญกำลังใจ" ตามตำรา — ระบบยังไม่มีข้อมูลนั้น จอเขียนกำกับไว้) |
| **OEE** | ค่าที่ stamp ตอนปิดกะ ถ่วงตามตำรา (`wavg`/`wLoad`/`wRun`/`wProd`) | `production_sessions` | 🟢 เป้า = `avgOeeTarget()` ของกรุ๊ปที่อยู่ในขอบเขต |

> ### 🔴 กฎความซื่อสัตย์ของจอ (บังคับ — อย่าแก้ให้ "สวยขึ้น")
> แกนที่ข้อมูลไม่พอ **ต้องเขียนบนจอว่าไม่พอ** ห้ามโชว์ 0 ห้ามซ่อนแผง
> — *จอที่ยืนยันสิ่งที่ไม่จริง แย่กว่าจอที่ว่าง* · `axisXxx()` ทุกตัวคืน `{ state: 'ok'|'thin'|'none', note }`
> และเทสล็อกไว้แล้วว่า "ไม่มีข้อมูล = `value: null`" ไม่ใช่ 0 (`src/utils/__tests__/obeyaKpi.test.mjs`)

---

## 4. 🎯 ACTION BOARD — เหตุผลหลักที่หน้านี้มีอยู่

**ข้อค้นพบที่ชี้ขาดทั้งดีไซน์ (วัด 2026-09-15):** `meeting_action_items` มี **0 แถว**
ตั้งแต่สร้าง 13/07 ทั้งที่ downtime ถูกกรอก 8,627 แถว และใบ 4M 1,213 ใบในช่วงเดียวกัน
⇒ ระบบเก็บ "สิ่งที่เกิดขึ้น" ได้ดีมาก แต่ **"สิ่งที่ตัดสินใจจะทำ" ยังอยู่นอกระบบ** (ในหัวหัวหน้า / ไลน์แชท / กระดาษ)

หน้านี้จึงทำให้ลูปเดินได้:
```
ตัวเลขหลุดเป้า → [➕ ตั้ง Action] (ผูก kpi_key กับแกน) → ใครรับ · ภายในเมื่อไหร่
   → แจ้งเข้ากระดิ่ง (notifyEvent) → บอร์ดโชว์ "เกินกำหนด" ขึ้นก่อนเพื่อน → กดปิด → วัดผล
```
- **บอร์ดว่าง = ขึ้นคำเตือนสีแดง ไม่ใช่ "0 ใบ" เฉยๆ** — 0 ใบไม่ได้แปลว่าไม่มีปัญหา แปลว่าไม่มีใครบันทึก
- "เกินกำหนด" คำนวณจาก `due_date` เทียบวันนี้ **ไม่ใช่ `status` ดิบ** (`actionBuckets`)
- ใช้ตารางเดิมร่วมกับ `/morning-meeting` — **ห้ามสร้างตารางใหม่** · แยกที่มาด้วย `source = 'obeya' | 'morning'`

### คอลัมน์ที่เพิ่ม (migration `20260915_obeya_action_loop.sql`)
| คอลัมน์ | ใช้ทำอะไร |
|---|---|
| `source` | `'obeya'` / `'morning'` / null(=ของเดิม) — รู้ว่าใบนี้ตั้งจากที่ประชุมไหน |
| `kpi_key` | `'S'|'Q'|'D'|'C'|'M'|'OEE'` — ใบนี้จะไปแก้ตัวเลขตัวไหน (**ไม่มี check constraint ตั้งใจ** — เพิ่มแกนใหม่ไม่ควรต้องรอ migration · client คุมตัวเลือกผ่าน `OBEYA_AXES`) |
| `target_value` / `result_value` | เป้าที่ตกลงกัน / ผลจริงตอนปิด — มีทั้งคู่ถึงจะพิสูจน์ได้ว่า countermeasure ได้ผล |

### 🔴 RLS — เปลี่ยนจาก `using(true)` เป็น `has_perm()` (Phase 3 ที่ policy เดิมเขียนคอมเมนต์ค้างไว้เอง)
เดิม `for all to authenticated using(true) with check(true)` = ใครที่ login ก็เขียนได้ แม้ `/permissions` ปิดปุ่มแล้ว
ตอนนี้แยกครบ 4 cmd: `select` เปิดทุกคนที่ login (จอ TV/ผู้บริหารต้องอ่านได้) ·
`insert`/`update`/`delete` = `has_perm('morning_meeting:record') or has_perm('obeya:record')`
> ผลกระทบ = 0 (ตารางมี 0 แถว · ทางเขียนเดียวคือปุ่มใน `/morning-meeting` ที่ gate ด้วย `can('morning_meeting','record')` อยู่แล้ว)
> ⚠️ RLS ปฏิเสธ UPDATE = "สำเร็จ 0 แถว ไม่มี error" ⇒ ปุ่ม "ปิด" บนบอร์ด `.select('id')` แล้ว**นับแถว** ห้ามเชื่อ `!error`

---

## 5. สิทธิ์
| คีย์ | default | คุมอะไร |
|---|---|---|
| `page:/obeya` | **ทุก role** | เข้าหน้า (จอแขวนในห้อง Obeya ต้องเปิดได้ด้วยบัญชี display) |
| `obeya:record` | admin · manager · supervisor · leader | ปุ่ม ➕ ตั้ง Action / ปุ่มปิดใบ |

หน้าอ่าน `can('obeya','record') || can('morning_meeting','record')` — คนที่ตั้ง action ในประชุมเช้าได้อยู่แล้ว ไม่ต้องขอสิทธิ์ใหม่

---

## 6. ⚡ งบ egress (สำคัญ — อย่าเผลอทำให้แพงขึ้น)

จอนี้ = **จอห้องประชุม 1-2 จอ ไม่ใช่บอร์ดหน้าไลน์ 10 จอ** · โหมดเดือนโหลดประมาณ **400 KB/รอบ**

- `useLiveBoard` 2 ตัว (คนละ project): board → subscribe **`production_sessions` ตัวเดียว** (`tier: LIVE.BOARD` · `rate: RATE.ANALYTIC`)
  · actions → `meeting_action_items` (Main · `tier: LIVE.PAGE` — ตอนประชุมต้องเห็นใบใหม่เร็ว)
- ⚠️ **ห้ามเพิ่ม subscribe `prod_orders` / `downtime_logs`** — ทุกใบงานที่ปิดในโรงงานจะลากจอโหลดใหม่ทั้งก้อน
  (บทเรียน `docs/POLLING-AUDIT-2026-09-15.md`: prod_orders 6,876 req/วัน จาก realtime ล้วน)
- ตัวกรองส่วนงาน/ขอบเขต **กรองใน `useMemo` ไม่ใช่ในคิวรี** — สลับส่วนงานแล้วไม่ยิง DB ใหม่
- `useCallback` ของตัวโหลด **deps เป็น primitive ล้วน** (`from`/`to`/`prev.from`/`prev.to`) ตามกฎเหล็ก DB ข้อ 9

---

## 7. ทำไปแล้ว / ยังไม่ทำ

**ทำแล้ว (2026-09-15):** ผัง A4 + โหมดจอ TV · แกน S/Q/D/C/M + OEE + Pareto · ACTION BOARD (ตั้ง/ปิด/แจ้งเตือน) ·
สิทธิ์ + RLS ตามกฎ · ลิงก์กลับหน้าทำงานจริงทุกแผ่น · ช่วงเวลา สัปดาห์/เดือน + เทียบงวดก่อน
**ทำแล้ว (2026-09-22):** โหมด **ปี** 12 แท่ง + แท่งสรุป + YTD · drill-down แท่งเดือน → รายวัน · ตัด "วันนี้" ออก (ดู §9)

**ยังไม่ทำ (เรียงตามคุ้ม ÷ เสี่ยง):**
1. **ทะเบียนอุบัติเหตุ/near-miss** — ต้องมีก่อนแกน S ถึงจะเป็น KPI จริง (ตอนนี้เป็น PPE ซึ่งเป็นพฤติกรรม)
2. **วัดผล countermeasure อัตโนมัติ** — มี `target_value`/`result_value` ในตารางแล้ว แต่หน้ายังไม่เติม `result_value`
   ให้เอง (ควรดึงค่า KPI ของแกนนั้น ณ วันปิดใบมาเทียบ — ต่อกับ `improvements` ที่ทำ before/after อัตโนมัติอยู่แล้ว)
3. **โหมดประชุม** (เดินทีละแกน + จดมติ) — reuse pattern จาก `/morning-meeting`
4. ดัน `defect_logs` ให้ถูกบันทึกครบ — ตราบใดที่ยัง 179 แถว แกน Q ก็ยังขึ้นคำเตือน `thin` อยู่ดี

---

## 8. ประวัติ
- **2026-09-15** — สำรวจ + ออกแบบ (`docs/OBEYA-DESIGN.md`) แล้วลงมือทำ prototype ตามคำสั่ง user
  ("ทำมาก่อน จะให้ prototype ทดลองการใช้งานและสอนพนักงานได้") · ผัง A4 มาจากคำสั่งเดียวกัน 2 ข้อ
  ("หน้ารวมใน 1 หน้า ลักษณะกราฟ" + "จอทีวี 70 นิ้ว เหมือนมีกระดาษ A4 ติดเต็มหน้าจอ")

---

## 🚦 ไฟสถานะบนหัวแผ่น + ถอดแถบสีประจำแกน (2026-09-21 · คำขอ user)

user ส่งภาพจอ SQDCM มาวงไว้ว่า *"อยากได้สเตตัสสี แสดงสถานะ แล้วกดเข้าไปดู สำหรับบอร์ดมอนิเตอร์ทุกอย่าง
ส่วนสีสันของเส้นแต่ละ box ไม่เอา"* — กากบาททับแถบสีบนหัวแผ่นทุกใบ

**ทำ 2 อย่างพร้อมกัน (มันเป็นเรื่องเดียวกัน):**
1. **ถอด `borderTop: 4px solid <สีประจำแกน>` ออกทุกแผ่น** — บนบอร์ด สีต้องมีความหมายเดียวคือ *สถานะ*
   แถบสีประจำแกน (S แดง · Q ม่วง · D ฟ้า …) เป็นสี "ประจำตัว" ที่ไม่เปลี่ยนตามผล ⇒ แผ่นที่ปกติกับ
   แผ่นที่มีปัญหาเด่นเท่ากัน และแถบแดงของ S ยังอ่านผิดเป็น "มีปัญหา" ได้ทั้งที่ S เขียว
   · สีประจำแกนยังใช้อยู่ที่ **เส้น/แท่งในกราฟ** เหมือนเดิม (`axisSheet(k).color`) ไม่ได้ถอดทิ้ง
2. **เพิ่ม `<StatusLamp>` บนหัวแผ่นครบทั้ง 8 ใบ** — วงกลมสี + คำบอกสถานะ + `↗` กดเจาะเข้าหน้าจริง
   (ปลายทางเดียวกับปุ่มลิงก์ท้ายแผ่น) · tooltip บอกเหตุผลเต็มว่า "ทำไมเป็นสีนี้"

**ที่มาของสี — `statusWhy()` ใน `src/utils/obeyaKpi.js` เท่านั้น ห้ามหน้าคิดคำ/เกณฑ์เอง**

| แผ่น | ที่มาของไฟ |
|---|---|
| S · Q · D · M · OEE | `statusWhy(value, target, ทิศทาง, หน่วย)` ตรงๆ (ใช้ `statusOf` เดิม เกณฑ์ไม่เปลี่ยน) |
| C ต้นทุนที่เสียไป | เขียนเอง — **ยังไม่มีใครตั้งเพดานค่าความสูญเสีย** ⇒ แดงนี้แปลว่า *"มีเงินหายไป"* ไม่ใช่ *"เกินเป้า"* (เขียนไว้ใน tooltip ตรงๆ) |
| 🔎 ทำไมถึงหลุดเป้า | แผ่นนี้ตอบ *"ทำไม"* ไม่ใช่ KPI ของตัวเอง ⇒ มีเวลาหยุด = **เทา "ไม่มีเป้า"** · 0 นาที = เขียว |
| ACTION BOARD | ใบเกินกำหนด = แดง · ยังไม่มีใบเลย = เทา · ที่เหลือ = เขียว · **ไฟไม่กดได้** (รายละเอียดอยู่บนแผ่นแล้ว — ลิงก์ไป `/morning-meeting` ไม่ได้ ตารางเดียวกันแต่คนละ `source`) |

**กับดักที่เจอตอนทำ (วัดด้วย Playwright ทั้งคู่ — ตาเปล่าไม่เห็น):**
- วางไฟไว้ **แถวเดียวกับชื่อแผ่น** → ที่ 900px ชื่อ "S ความปลอดภัย" ถูก clip เหลือ 72px
  ⇒ ย้ายไปท้าย **แถวตัวเลขใหญ่** (แถวนั้นที่ว่างเยอะ + ไฟอยู่ติดเลขที่มันตัดสินพอดี)
- ย้ายแล้วไฟยังไปบีบ **ตัวเลขพาดหัว** (98.4% เหลือ 34px) ⇒ บล็อก `flexShrink: 0` ที่ตัวเลข
  \+ ย่อไฟเหลือดวงเปล่าเมื่อแผ่นแคบ
- 🔴 **ย่อไฟโดยดู `k` ไม่ได้** — `k = clamp(cw/330, 0.72, 2.4)` ชน floor ⇒ แผ่น 172px (จอ 900)
  กับ 217px (จอ 1280) ได้ `k` เท่ากันเป๊ะ แยกไม่ออก · ต้องส่ง **ความกว้างจริง** (`cw × span`) ลงไป
  แล้วย่อเมื่อ `< 200px` — ผลลัพธ์: 1920/1280 เห็นคำเต็ม · 900 เหลือดวงเปล่า (แตะอ่าน tooltip ได้)

**ผลวัดหลังแก้ (1920 โหมดจอ TV · 1280 · 900):** ไฟครบ 8/8 ทุกความกว้าง · ชื่อแผ่นไม่ถูก clip สักใบ ·
ไม่มีของล้นออกนอกแผ่น · `window.__crash` = false · ไม่มี pageerror

📄 กติกากลาง (ใช้กับบอร์ดมอนิเตอร์อื่นด้วย) → `docs/UI-CONVENTIONS.md` §2.1

---

## 9. 📅 โหมดปี + drill-down (2026-09-22 · คำสั่ง user)

> *"อยากให้มีรายปี กราฟโชว์แต่ละเดือน เดือนไหนแดง คลิก drill-down เข้าไปดูรายวันของเดือนนั้น · มีแค่สัปดาห์ เดือน ปี พอ
> เพราะรายวันข้อมูลมันไม่เป็นกราฟ"* → ตัด "วันนี้" ออก · *"ปีปฏิทิน 12 แท่ง + แท่งที่ 13 คือ averaged/total แล้วแต่ KPI นั้นๆ และมีตัวเลข YTD ด้วย"*

**ช่วงเวลาเหลือ 3 โหมด** `PERIODS` = สัปดาห์นี้ · เดือน · ปี (`obeyaKpi.js` — `periodRange('day')` ยังรองรับให้เทส/ผู้เรียกเก่า แต่ไม่มีปุ่ม)
· ปุ่ม ◀ ปี ▶ เลือกปีปฏิทินย้อนหลังได้ (ปีอนาคตกดไม่ได้) · **กดแท่งเดือนไหนบนโหมดปี = ทั้งจอสลับเป็นโหมดเดือนของเดือนนั้น**
(`monthSel`) เห็นกราฟรายวันครบทุกแผ่นพร้อมกัน + ปุ่ม "← ปี YYYY" กลับ · เทียบงวดก่อนของเดือนที่เจาะ = เดือนก่อนหน้า (`prevMonthRange`)

**🔴 โหมดปีห้ามโหลดแถวดิบ — อ่านผลรวมรายเดือนจาก RPC 2 ตัว (วัดจริง 22/09):**

| | โหมดเดือน (แถวดิบ) | โหมดปี (RPC) |
|---|---|---|
| ฝั่ง DR `obeya_year_rollup(from,to)` — กะ/downtime/ของเสีย/ใบงาน รวมต่อ (เดือน, ไลน์[, ชนิด/mat/สถานะ]) | ~400 KB/รอบ · ทั้งปีถ้าทำแบบเดียวกัน ≈ 5 MB | **~137 KB ทั้งปี 2026** |
| ฝั่ง Main `obeya_attendance_rollup(from,to)` — เช็คชื่อ/PPE รวมต่อ (เดือน, จุดงาน) | (แถวต่อคนต่อวัน ~100k แถว/ปี) | **~63 KB** |

- migration `20260922d_obeya_year_rollup_dr.sql` (DR · `security definer` + **grant anon** — client DR เป็น anon เสมอ) + `20260922d_obeya_attendance_rollup_main.sql` (Main · `security invoker` · authenticated) — **apply แล้วทั้งคู่ 22/09**
- **RPC คืน Σ อย่างเดียว ไม่หาร ไม่ตัดสินสี ไม่คำนวณ OEE** — การหาร/ถ่วง/สถานะอยู่ใน `src/utils/obeyaYear.js` (pure · เทส 8 เคส) ·
  น้ำหนักที่ SQL ส่งกลับ = ตัวเดียวกับที่โหมดเดือนใช้ (`wload=shift_min` · `wrun=wload×A/100` · `wprod=qty+ng`) ⇒ **ห้ามย้ายสูตรลง SQL** (จะกลายเป็น OEE ชุดที่ 2)
- `useLiveBoard` 2 ตัวสลับด้วย `enabled` (โหมดปี subscribe `production_sessions` ตัวเดียวเท่าเดิม) · เทียบ "ปีก่อน" **ยังไม่เปิดในโหมดปี** (ต้องยิง RPC เพิ่มอีกเท่าตัว — จอบอกไว้ที่ foot ของ OEE)

**กติกาแท่งที่ 13 + YTD (`obeyaYear.js`):**
- KPI อัตราส่วน (S · Q · D · M · OEE) → แท่งสรุป = **ค่าถ่วงน้ำหนักทั้งช่วง** ไม่ใช่ mean ของ 12 แท่ง (กฎเดิม mean-of-percentages) · เลขใหญ่บนแผ่น = **YTD** = ค่าเดียวกัน (ปีย้อนหลัง = ทั้งปี)
- KPI จำนวน (C บาท) → แท่งสรุป = **ผลรวม**
- **เดือนที่ไม่มีข้อมูล = ไม่มีแท่ง (`v: null`) ห้ามเป็นแท่ง 0/แดง** · แท่งสรุปจาง + ขอบประ ให้แยกจากเดือนจริงจากระยะไกล · สีแท่ง = `monthBarStatus` = เกณฑ์เดียวกับโหมดวัน
- ป้ายแกน X: แผ่นแคบ (<420px) ใช้เลขเดือน 1-12 (ชื่อย่อไทย 13 ตัวชนกันอ่านไม่ออก — วัดจริง) · แผ่นกว้าง (OEE 2 ช่อง) ใช้ชื่อย่อไทย · tooltip บอกชื่อเดือนเต็มเสมอ

**บั๊กเดิมที่เจอตอนทำ (แก้แล้วในคอมมิทเดียวกัน):**
1. **`downtime_logs` ไม่มีคอลัมน์ `reason`** (มี `description`) — จอโหมดเดือน select `reason` มาตลอด ⇒ คิวรี downtime ล้ม → แกน C + Pareto ว่าง + ขึ้น loadWarn ทุกครั้ง
2. **`daily_production_logs.assigned_line` = id จุดงาน (`workstations.id`) ไม่ใช่ชื่อไลน์** (มีเขียนไว้ใน `morning-meeting.md` แล้ว) — จอเอา id ไปเทียบชื่อไลน์ ⇒ ติดตัวกรองส่วนงานเมื่อไหร่ แกน S/M หายเกลี้ยงเงียบๆ · ตอนนี้โหลด `workstations(id, line_name)` แล้ว map ก่อนกรอง (แถวที่ไม่รู้จุดงาน = คงไว้ ไม่ทิ้ง)
3. harness: `audit/mockSupabase.js` มี `RPC_RESULT` ของ 2 RPC นี้แล้ว (ครอบเดือนว่าง · แถว null · ไลน์นอกทะเบียน · จุดงานไม่รู้จัก) — เพิ่ม RPC ใหม่ต้องเพิ่ม mock ไม่งั้นโหมดนั้นไม่เคยถูกเรนเดอร์ใน crashsweep

**ยังไม่ทำ:** เทียบปีก่อนในโหมดปี · ป้ายแกน Y ของทุกแผ่นถูกตัด (margin left −22 + width 34 — ของเดิมตั้งแต่ 15/09 ไม่ใช่ของโหมดปี)

---

## 🐛 คิวรี downtime ล้มเงียบ + เจาะแล้วทิ้งตัวกรอง (2026-09-22)

### 1. `downtime_logs.reason` — คอลัมน์ที่ไม่มีอยู่จริง
บอร์ด SQDCM `select('... reason ...')` แต่ตารางนี้ชื่อคอลัมน์ว่า **`description`**
(ทุก component อื่นในรีโปใช้ `description` หมด — `OeeInsightPanel` · `MachineReliability` · `SymptomSearch`)
⇒ คิวรีล้มทั้งก้อนตั้งแต่สร้างบอร์ด · แผง **"ทำไมถึงหลุดเป้า" กับครึ่งหนึ่งของ C** ว่างมาตลอด
(วัดจริง 21-22/09: มี downtime นอกแผน JIG 218 นาที · Conveyor 158 นาที ที่ไม่เคยขึ้นจอ)

> **🔴 ไฟสถานะทำให้อาการแย่ลง:** พอแผงว่าง `paretoStat` คืนเขียว *"ไม่มีเครื่องหยุด"* และ
> `costStat` คืนเขียว *"ไม่มีความสูญเสีย"* — **จอยืนยันสิ่งที่ไม่จริง** ทั้งที่แถบแดงบนหัวจอบอกว่า error
> ⇒ เพิ่ม `dtBad` (แยก error ของคิวรี downtime ออกจาก `loadWarn` รวม) · 2 แผงนี้ขึ้นเทา
> **"โหลดไม่ได้"** เมื่อคิวรีล้ม · **ว่างเพราะไม่มีเหตุการณ์ ≠ ว่างเพราะคิวรีพัง ห้ามหน้าตาเหมือนกัน**

### 2. ลิงก์เจาะ — โหลดเว็บใหม่ + ทิ้งตัวกรอง + ไปผิดแท็บ
user แจ้ง 22/09: *"กรอง PD3 ไว้ พอกดเข้าไปดูที่หลุดเป้า มันรีเฟรชเว็ปไปหน้าใหม่ ต้องกรองอีกรอบ"*

| ปัญหา | เดิม | แก้เป็น |
|---|---|---|
| **โหลดเว็บใหม่ทั้งก้อน** | `window.location.href = '...'` × 7 จุด | `navigate()` ของ react-router (SPA · วัดแล้ว 0 full reload) |
| **ทิ้งตัวกรอง** | ไม่ส่งอะไรไปเลย | ส่ง `?section=` + `?date=` ติดไปใน URL |
| **ไปผิดแท็บ** | `?tab=lean` × 3 จุด — **ไม่มีแท็บชื่อนี้** (`useTabParam` รับแค่ `today｜trend｜insight`) ⇒ ตกกลับแท็บแรกทุกครั้ง | `?tab=insight` (วิเคราะห์สาเหตุ = ที่อยู่จริงของพาเรโต/LOSS/ของเสีย) |

**กฎที่ตกผลึก:**
- **ส่ง param เฉพาะหน้าที่อ่านมันจริง** — ใส่ `?section=` บนหน้าที่ไม่ได้อ่าน = URL โกหก
  (ตอนนี้อ่านแล้วเฉพาะ `/oee-analytics` · `/daily-checker` `/production-plan` `/workforce-insight` ยังไม่ได้ทำ)
- **ส่วนงานที่เจาะมาอาจอยู่นอก scope ของคนที่กดเปิดลิงก์** ⇒ หน้าปลายทางต้องเช็คกับ `sectionOptions`
  แล้วเคลียร์ + ขึ้น toast บอก **ห้ามกรองด้วยค่าที่ไม่มีสิทธิ์แล้วโชว์ผลว่างเฉยๆ**

### 3. เครื่องมือที่เพิ่มมาเพราะเคสนี้
- **`scripts/audit-select-columns.mjs`** — เทียบทุก `.select()` ในรีโปกับ schema จริง
  **ด่านที่มีอยู่จับคลาสนี้ไม่ได้เลยสักด่าน** (build/lint = มันคือสตริง · เทส = ไม่ต่อฐาน ·
  crashsweep = mock คืนทุกคอลัมน์ · ตาคน = หน้าตาเหมือน "ยังไม่มีข้อมูล")
  · รันเต็ม 22/09: **DR 527 + MAIN 334 คอลัมน์ = ไม่มีที่ผิดแล้ว**
- **`audit/main.jsx` ส่ง query string จริงเข้า `MemoryRouter`** — เดิมเป็น router เปล่า
  ⇒ `useSearchParams()` ว่างเสมอ = **โค้ดสาย "รับค่าจาก URL" ไม่เคยถูกรันใน harness เลย**
