# Production Plan — วางแผนการผลิต (Active Planner, 2026-07-15)

> ย้ายมาจาก `CLAUDE.md` (2026-09-03 — แยกไฟล์เพื่อลด context) · โหลด**เฉพาะเมื่อแตะโมดูลนี้** · แก้ไฟล์นี้แทน CLAUDE.md เมื่อกฎของโมดูลเปลี่ยน


หน้า `/production-plan` (หมวด **ฝ่ายผลิต** + โผล่ซ้ำใน **Logistic-แผนงาน** ผ่าน `alsoIn` — 2026-09-17) — จากยอดลูกค้า (order รายวัน + forecast รายเดือน) เทียบ **"กำลังผลิตที่ทำได้จริง"** → บอกว่าต้องเปิดกี่กะ กี่วัน วันไหนเปิด OT/กะดึก/ทำวันหยุด วันไหนไม่ต้อง เพื่อทันดิว · **เฟส 1 อ่านอย่างเดียว ไม่เขียน DB**

- **กำลังผลิต = median(ยอดดีจริงต่อกะ) ใน 60 วันล่าสุด** ต่อ (ไลน์+พาร์ท) — util กลาง `src/utils/capacityModel.js` · median ตัดค่าโดด (วันเทพ/หายนะ) + บวก OEE/เบรค/NG ไว้ในตัว · พาร์ทที่มีประวัติ < 3 กะ fallback = (นาทีกะ×60÷CT)×OEE median ของไลน์ + ติดป้าย "ข้อมูลน้อย" · เลือกโหมดวางแผน **median (สมจริง)** หรือ **P25 (ปลอดภัยไว้ก่อน)**
  - **normalize กะที่แชร์ไลน์ (2026-07-21):** กะที่พาร์ทวิ่งไม่เต็มกะ (แชร์กับพาร์ทอื่น) ทำให้ยอด/กะต่ำ → median กำลังต่ำเกิน → OT/backlog เกินจริง · ProductionPlan โหลด `opened_at`/`confirmed_at` คิด run-min ต่อ (session,mat) แล้ว: กะวิ่ง **50–90%** ของกะ → คูณกลับเป็นเต็มกะ (scale ≤2×) **cap ด้วยกำลังทฤษฎี `shift×60÷CT` กัน over-scale** (overstate = วางแผนน้อยไป อันตราย) · กะวิ่ง **<50% = ตัดทิ้ง**จาก median (สัญญาณน้อยเกิน) · ไม่มี timestamp = ใช้ค่าดิบเดิม
> ### 📍 ทำไมอยู่ 2 หมวด — ห้ามย้ายไป Logistic (2026-09-17 · คำถาม user)
> หน้านี้มีเจ้าของจริง **2 ฝ่าย**: ฝ่ายผลิตคือคนตัดสินว่าจะเปิดกะ/OT/วันหยุด
> · planner คือคนเอายอดลูกค้า/forecast มาเทียบกำลังผลิต
> - **สิทธิ์ `page:/production-plan` เดิมให้ planner อยู่แล้ว** (`planner_store`, `sale`, `supervisor`, `leader`, `manager`, `dept_admin`, `admin`)
> ⇒ ปัญหาคือ "หาเมนูไม่เจอ" ไม่ใช่ "เข้าไม่ได้" — **การย้ายหมวดไม่ได้เพิ่มสิทธิ์ให้ใครเลย**
> - ย้ายไป Logistic ล้วนๆ = หัวหน้าไลน์/ผู้จัดการผลิตหาไม่เจอ — แก้ข้างหนึ่ง พังอีกข้างหนึ่ง
> - วิธีที่ใช้ = `alsoIn: LOGISTIC_GROUPS.control` ใน `NAV_ITEMS` (App.jsx) — กลไกเดียวกับ `/store-monitor`
>   · `inNavGroup()` ทำให้โผล่ sidebar/หน้า Home ทั้ง 2 หมวด · สิทธิ์/ค้นหา/ตัวนับ **นับครั้งเดียว**

- **หน่วยกลาง = shift-load** (qty ÷ กำลังต่อกะ) เพื่อรวมหลายพาร์ทบนไลน์เดียวถูกต้อง (ไลน์มี 1 กะ แต่หลาย product คนละ rate)
- **แท็บรายวัน:** order ค้างส่ง 21 วันข้างหน้า → เดินปฏิทินวันต่อวัน (greedy: กะเช้า → +กะดึก(ถ้าไลน์มี) → +OT 25% → วันหยุดทำเฉพาะเมื่อ backlog) · **ลำดับใช้วันหยุด (กฎ user 2026-07-21): วัน `shutdown75` (ม.75) = กำลังสำรองที่เรียกได้ด้วยค่าแรงปกติ ใช้เต็มกำลังเหมือนวันทำงาน (⚡ ยกเลิกหยุด75% สีม่วง) ก่อนถึง OT วันหยุด ot15/ot2 (⚠ แดง) เสมอ** — ทั้งแท็บรายวันและ verdict รายเดือน (tier ⚡ อยู่ก่อน 🚨 เกินกำลัง) · แถบปฏิทินระบายสี ☀/⏰/🌙/⚡/⚠ + สรุปต่อไลน์ · endBacklog > 0 = 🚨 เปิดเต็มที่ยังไม่ทัน
- **แท็บรายเดือน:** forecast 6 เดือนข้างหน้า → กะที่ต้องใช้ (shiftsNeeded) vs วันทำงานในเดือน (จาก company_calendar) → verdict: กะเช้าพอ / ต้อง OT N วัน / ต้องกะดึก / 🚨 เกินกำลังต้องเพิ่มไลน์-คน
- **แหล่งข้อมูล:** DR = customer_shipping_orders (order), customer_forecasts (forecast), production_sessions+prod_orders (กำลังจริง), dr_products (mat→line, CT) · Main = production_lines (std กะ), company_calendar (วันทำงาน) · map พาร์ท→ไลน์ผ่าน `dr_products.line_name` + normalize mat (ตัดขีด/ช่องว่าง)
- **⚠️ map เลขลูกค้า→SAP ต้องผ่าน `p_no` ด้วย (2026-07-21):** order/forecast มักอ้าง**เลขลูกค้า** (เช่น `RB3B 8B225 AA`) ไม่ใช่ mat_no SAP — `resolveMat()` ลอง ตรง → normalize mat → `pnoToMat[normalize(p_no)]` · **map ไม่เจอ = ขึ้น banner ⚠️ (N ออเดอร์/พาร์ท ยังไม่ตั้ง SAP)** ห้ามทิ้งเงียบ (เดิม map ผ่าน mat_no อย่างเดียว ทิ้ง ~38% order/85% forecast เงียบ) · ต้นเหตุหลัก = master data `dr_products.p_no` ยังไม่กรอก → ไปตั้งที่ Product Master / ปุ่ม 🔗 จับคู่ SAP ในหน้า Planner&Sales
- **⚠️ กับดัก `prod_orders` ไม่มีคอลัมน์ `line_name`/`work_date`** — 2 ค่านี้อยู่บน `production_sessions` (join ผ่าน `session_id`) · select ตรงจาก prod_orders = PostgREST error 42703 (ถ้าดึงแค่ `data` จะถูกกลืนเงียบ prodArr ว่าง) — เคยพังที่ PmForecast (shot สะสม = 0) · หน้าใหม่ที่ query prod_orders ตามไลน์/วัน ให้ embed `production_sessions!inner(line_name, work_date)` + เช็ค `error` เสมอ (2026-07-21)
- **Scope:** leader = family ไลน์ตัวเอง (branch มาก่อน) · role อื่นตาม `sections` · migration สิทธิ์: `20260715_production_plan_page_permission.sql`
> ### 🔴🔴 2 บั๊กตัวเลขที่ audit 2026-09-22 จับได้ (แก้แล้ว — ห้ามถอยกลับ)
>
> **① งานคู่ RH/LH ทำให้ "ภาระกะ" ถูกนับ 2 เท่า**
> เดิมบวก `qty ÷ กำลังต่อกะ` ของทุกพาร์ทรวมกันทันที (`loadByDate[date] += …`) ⇒ คู่ที่ปั๊มทีเดียว
> ได้ทั้ง RH+LH ถูกคิดเวลาสองรอบ · ของจริงในฐาน DR วันนั้น — คู่ที่มี demand ครบทั้ง 2 ข้าง:
> `20065635↔20065715` (LASER-789) · `20059957↔20059959` และ `20059966↔20059967` (LINE D 110&300T)
> ⇒ **LINE D ซึ่ง `std_night_shift = 0` (ไม่มีกะดึกให้เปิด) ถูกดันไป tier 🚨 เกินกำลัง** ทั้งที่โหลดจริงน้อยกว่านั้น
> · **แก้:** สะสมโหลด**แยกราย mat** (คีย์ = เลข SAP จาก `resolveMat`) แล้วรวมด้วย
>   **`pairLoadTotal(loadByMat, pairOf)`** (`src/utils/pairTotals.js`) ทั้ง 3 จุด: order รายวัน ·
>   ยอดค้างส่ง (carry) · forecast รายเดือน · ต้อง `select('pair_mat_no')` ด้วย (ลืม = pairOf ว่าง = บั๊กเดิมกลับมาเงียบๆ)
> · **ยอดชิ้น (`duePcs`) ยังบวกทั้งคู่ตามปกติ** — RH/LH ส่งลูกค้าแยกใบ เป็นชิ้นจริงทั้งคู่ (กฎ "ชิ้น ≠ shot")
> · วัดผลจริงหลังแก้ (window เดือนนี้): LASER-789 โหลด **−48.2%** · LINE D **−18.1%** · อีก 9 ไลน์ **0.0%** (ไม่มีคู่)
> · ด่านกันถอยกลับ: `plan-load-needs-pair-collapse` ใน `regressionGuards.test.mjs`
>   (ไฟล์ไหนมี `/ perShift` ต้องมี `pairLoadTotal`)
>
> **② กำลังผลิตทาง fallback เฟ้อ 16.3% (ทิศทางอันตราย = วางแผนน้อยไป)**
> `estimateCapacity` คิด `(shiftMin×60 ÷ CT) × lineOee` แต่ `lineOee` วัดบน
> `netAvail = elapsed − plannedDT − breakMin` = **หักพักไปแล้ว** ⇒ เดิมส่ง `DEFAULT_SHIFT_MIN = 570`
> (เวลาดิบ 08:00–17:30) เข้าไปคูณ = เอา OEE ที่หักพักแล้วไปคูณฐานที่ยังไม่หักพัก
> · พักกะเช้าจาก `break_policies` (ot_scope=always): ประชุมแถว 10 + เที่ยง 50 + เบรค 10 + 10 = **80 นาที**
>   ⇒ ฐานที่ถูก **490 นาที** · เฟ้อ 570/490 = **+16.3%** · กระทบ **19 จาก 36 พาร์ทที่มี demand (53%)**
>   ที่ประวัติ < `MIN_SESSIONS` จึงตกมาทางสูตรนี้
> · **แก้:** หน้าโหลด `break_policies` แล้วคิด
>   `netShiftMin(DEFAULT_SHIFT_MIN, policyBreakForShift({...}))` ส่งเข้า `estimateCapacity`
>   · เวลาพักมีเจ้าของที่ `src/utils/oee.js` ที่เดียว **ห้ามตั้งรายการพักเองในหน้า**
> · ⚠️ **`SHIFT_MIN` (ดิบ) กับ `netMin` (สุทธิ) ในบล็อก normalize กะแชร์ไลน์ ต่างกันโดยเจตนา ห้ามยุบ:**
>   `rm` มาจาก `opened_at→confirmed_at` = **นาฬิกาแขวน** (กินเวลาพักด้วย) ⇒ อัตราขยายกลับเป็นเต็มกะ
>   เทียบเวลาดิบ · แต่**เพดานกำลังทางทฤษฎี** ต้องเป็นเวลาสุทธิ
> · อ่านตาราง `break_policies` ไม่ได้ = ใช้ `FALLBACK_SHIFT_BREAK_MIN` (80) **แล้วขึ้นจอบอกว่าใช้ค่าสำรอง**
> · ด่านกันถอยกลับ: `capacity-shiftmin-must-be-net` (ห้ามส่ง `shiftMin: DEFAULT_SHIFT_MIN`)
> · เทส: `src/utils/__tests__/planCapacity.test.mjs` (10 เคส)
>
> **จอต้องบอกที่มาเสมอ:** ใต้หัวเพจแสดง "เวลาทำงานสุทธิ N นาที/กะ (เวลากะ 570 − พัก 80)" +
> "🔗 งานคู่ N คู่ — ยอดชิ้นบวกทั้งคู่ แต่ภาระเวลานับครั้งเดียว" ⇒ คนที่บวกเลขมือเองจะไม่เข้าใจผิดว่าระบบตกหล่น

> ### 🌑 "ไลน์ที่ไม่มีความต้องการ" ห้ามหายจากจอ (2026-09-22 · audit)
>
> เดิมแท็บรายวันกรอง `orderCount > 0 || unknownCap > 0 || carryPcs > 0` ⇒ **ไลน์ที่ไม่มี order/forecast
> หายทั้งไลน์** · วัดจริง 22/09: **10 จาก 22 ไลน์** ที่มีพาร์ทลงทะเบียนแต่ไม่มีความต้องการในระบบเลย
> (LINE A · LINE B ที่มี FG 16 ตัว · LINE SUB-STATIONARY · MAIN TSRA-1/2 · BENDING · Laser GOR …)
> เพราะลูกค้าเจ้าเหล่านั้น (TSRA 28 พาร์ท · TSPK 15 พาร์ทซึ่งเป็น FG ทั้งหมด · ISUZU RT50 · GWM · TSESA …)
> **ยังไม่มีทางนำ order เข้าระบบ** (ไม่ได้ส่ง EDI 830/862 และไม่มีใน e-SMART)
> ⇒ จอว่างเปล่าอ่านได้ว่า "ไลน์ว่าง" ซึ่งตรงข้ามกับความจริง
> · **แก้:** เพิ่มแถบเทา `silentLines` — ชื่อไลน์ + จำนวนพาร์ท + ลูกค้าของพาร์ทนั้น
>   พร้อมข้อความว่า "ไม่ใช่ไลน์ว่าง แต่ยังไม่มีข้อมูลความต้องการ"
>
> **✅ คลี่คลายแล้ว 23/09 — แพลนนิ่งส่งไฟล์ที่ใช้คุมลูกค้าพวกนี้มาแล้ว** (`1.Monitoring-<เดือน>.xlsx`)
> นำเข้ารอบแรกเรียบร้อย: FC 92 แถว 694,940 ชิ้น · ออเดอร์ล่วงหน้า 232 แถว 414,178 ชิ้น (ถึง มี.ค. 2570) ·
> MIN/MAX 60 พาร์ท · ยอดคงเหลือ 88 รายการ ⇒ **LINE A/B/C/D · LASER E50 · MAIN TSRA มีงานในแผนแล้ว**
> · แท็บ `📗 Monitoring (Planning)` ใน `/planner-sales` ให้แพลนนิ่งอัพเองทุกเดือน
> · **แถบ `silentLines` ห้ามถอด** — ยังมีไลน์ที่ไม่ได้อยู่ในไฟล์นี้ และเดือนไหนไม่ได้อัพก็กลับมาเงียบได้
> 📄 กติกาการแกะไฟล์ + กับดัก 13 ข้อ → `docs/modules/logistic-planner-sales.md` §📗 ไฟล์ Monitoring
>
> ### 🔍 ความต้องการที่ map SAP ได้แล้ว แต่ยังไม่ถูกนับในแผน (`demandGaps`)
>
> `lineOfMat()` คืน `null` แล้วแถวถูกข้ามเงียบๆ ทั้ง 2 กรณีซึ่งคนละเรื่องกัน · ตัวนับ `unmapped` เดิม
> ดูแค่ `resolveMat` จึงมองไม่เห็นทั้งคู่:
> | กรณี | ความหมาย | จอแสดง |
> |---|---|---|
> | ไลน์อยู่นอกตัวกรองส่วนงาน | ปกติ | บรรทัดเทา "ซ่อนไว้ตามตัวกรอง N รายการ" |
> | พาร์ทไม่มี `line_name` / ชื่อไลน์ไม่มีในทะเบียน | **ข้อมูลขาด** | แถบส้ม + รายชื่อพาร์ท + ทางแก้ (ตั้งไลน์ที่ Product Master) |
>
> ### 🔁 `buildDayPlan()` = ตัวเดียวของระบบ (ยุบโค้ดตาย 2026-09-22)
> เดิมตัวกลางใน `capacityModel.js` **ไม่มีใครเรียกเลย** ส่วนของจริงเขียนซ้ำอยู่ในหน้า แล้วเดินหน้าไปไกลกว่า
> ⇒ ตัวกลางล้าสมัย 2 เรื่อง (ไม่รู้จัก `shutdown75` · คิดเป็น "ชิ้น" ไม่ใช่ภาระกะ) — ใครหยิบไปใช้จะได้
> ลำดับวันหยุดผิดทันที · ตอนนี้หน้าเรียก `buildDayPlan({ dates, loadByDate, carryLoad, hasNight, otFactor, dayTypeOf })`
> **ห้ามเขียน walk ซ้ำในหน้าอีก** · เทสตรึงลำดับไว้ที่ `planCapacity.test.mjs` §③ (7 เคส)

> ### 🌳 แผนรวม "งานที่ระเบิดจาก BOM" แล้ว — ไลน์ที่ทำพาร์ทลูกเห็นงานของตัวเอง (2026-09-22 · คำสั่ง user)
>
> **ปัญหาเดิม:** แผนจับพาร์ท→ไลน์ตรงๆ จาก `dr_products.line_name` ⇒ ไลน์ปั๊ม/เลเซอร์เห็นเฉพาะงานที่
> **ลูกค้าสั่งพาร์ทลูกโดยตรง** · ส่วนที่ต้องทำเพื่อป้อนไลน์ประกอบไม่เคยปรากฏในแผนเลย
> (`fn_explode_child_demand` เป็น trigger บน `prod_orders` = ระเบิด**หลังปิดใบ** เพื่อออกใบเบิก/ตัดสต็อก
> — คนละเรื่องกับการวางแผนล่วงหน้า)
>
> **กลไก:** `src/utils/demandExplode.js` (pure + เทส 11 เคส) — `explodeDemand(demand, ix, explodeBom)`
> · ต้นไม้ผ่าน `buildBomIndex` + `sheetFor` เสมอ (กฎเหล็ก BOM) · หน้าเรียกผ่าน memo `demandPcs`
> ซึ่งคืน `byDate` / `byMonth` / `carry` เป็น **ชิ้นต่อ mat** แล้วแต่ละไลน์ค่อยกรองเฉพาะ mat ของตัวเอง
>
> **🔴 กติกาที่ห้ามพลาด**
> 1. **ลูกค้าสั่งตรง + ระเบิดจาก BOM = บวกกัน ห้ามแทนกัน** — ของจริง 22/09 ลูกค้าสั่งพาร์ท `2xxxxxxx`
>    ตรงๆ อยู่แล้ว **687 แถว / 18 mat** (LINE C 8 · LINE D 6 · LASER-789/E50/EXPORT) แทนกัน = ยอดหายครึ่ง
> 2. **ต่อโซ่หลายชั้นแบบ SAP** (คำตอบ user 22/09) ⇒ ข้ามแถวที่ `explodeBom` ตีว่า `isDupeRow`
>    (หลานที่ถูกใส่ซ้ำไว้ชั้น 1 ของใบเดียวกัน) · **ระบบไม่ลบข้อมูลให้** — ขึ้นเป็น worklist บนจอ
>    ให้ PE/Planning ไปเก็บที่ `/products` แท็บ BOM (กฎเดิมของ `bomTree.js`: ระบบเสนอ คนตัดสิน)
> 3. **หน่วยต้องติดมาด้วย** — BOM มีทั้ง PC และ KG (coil) · แผนกรองเฉพาะ mat ที่มีไลน์ผลิตจริง
>    ⇒ ของซื้อ/วัตถุดิบ (ไม่มีไลน์) ไม่เข้าแผนกำลังผลิตโดยอัตโนมัติ
> 4. **วนลูปต้องไม่ทำให้จอค้าง** — `explodeBom` ตัดที่ `maxDepth` แล้วคืน `cycles` · จอขึ้นแถบแดงบอก
>    ว่าความต้องการของกิ่งนั้นอาจขาด
> 5. ⏱️ **แท็บรายวันไล่ย้อนวันให้แล้ว** (2026-09-22 — ดูหัวข้อ *Backward Scheduling* ด้านล่าง)
>    เดิมงานของลูกถูกวางไว้ **วันเดียวกับดิวของแม่** ซึ่งมองโลกในแง่ดีเกินจริง
>    · **แท็บรายเดือน**ยังรวมเป็นก้อนเดือนเดียวกับดิวแม่ (พอสำหรับหน่วย "เดือน" เพราะ lead time
>      ส่วนใหญ่สั้นกว่าหนึ่งเดือน) — จะทำให้ข้ามเดือนต้องไล่ย้อนรายวันก่อนแล้วค่อยยุบเป็นเดือน
>
> **ผลจริงที่วัดได้ (ระเบิดชั้นเดียว · order 21 วัน):** LINE D **+30,100 ชิ้น** (6 พาร์ทลูก) ·
> LINE C **+14,756** (4) · Laser LWR +7,672 · Assy GOR / Laser GOR +270
> · อีก 41 พาร์ท (570,933 ชิ้น) เป็นของซื้อ/วัตถุดิบที่ไม่มีไลน์ผลิต ⇒ ไม่เข้าแผนกำลัง (ถูกแล้ว — เป็นงานจัดซื้อ)
>
> · ปุ่ม **🌳 รวมงานจาก BOM** บนหัวเพจ (default เปิด) ปิดได้เพื่อเทียบกับของเดิม
> · อ่าน BOM ไม่ได้ = แผนถอยไปนับเฉพาะที่ลูกค้าสั่งตรง **แต่ขึ้นแถบเตือนบนจอ ห้ามเงียบ**
> · ด่านกันถอยกลับ: `plan-demand-via-explodeDemand` (หน้าห้ามเรียก `explodeBom().rows` เองเพื่อรวมความต้องการ)

> ### 📦 หัก buffer stock ที่ STORE ก่อนสั่งผลิตซ้ำ (2026-09-22 · คำถาม user *"ต้องคิดกับ buffer stock ใน store ป่ะ"*)
>
> ใช่ — ของที่มีอยู่แล้วไม่ต้องผลิตซ้ำ · `netOffBuffer()` (`src/utils/demandExplode.js`) หักยอดคงเหลือ
> ออกจากความต้องการ **เรียงตามเวลา** (ของค้างส่งก่อน → วันถัดไปเรื่อยๆ)
>
> **🔴 หักได้เฉพาะยอดที่ `line_stock_summary` แถว `STORE` เท่านั้น — ห้ามหักยอด "ที่ไลน์" (mini-store)**
> กฎเหล็กจาก `demand-flow-tower.md`: backflush ยังไม่ทำงานครบ (`issue` 5,908 : `consume` 40)
> ⇒ ยอดคงเหลือที่ไลน์ **สูงกว่าความจริงเสมอ** · เอามาหัก = สั่งผลิตน้อยกว่าที่ต้องใช้ = **ของขาด**
> (ทิศเดียวกับบั๊ก "กำลังผลิตเฟ้อ" — ผิดทางที่อันตราย)
>
> **🔴 ของกองเดียวใช้ได้ครั้งเดียว** — หักทุก bucket ด้วยยอดเดิม = ความต้องการหายเกือบหมด
> แล้วแผนบอก "ไม่ต้องเปิดกะ" ทั้งที่ของไม่พอ (มีเทสตรึงไว้)
> · แท็บรายวันกับรายเดือนหักจาก buffer **ชุดของตัวเอง** (คนละมุมมองของช่วงเวลาเดียวกัน
>   ไม่ใช่หักต่อกัน ไม่งั้นแท็บที่คำนวณทีหลังจะเหลือ 0 เสมอ)
>
> **วัดจริง (order 21 วัน · ระเบิดชั้นเดียว):** LINE C gross 14,216 → **net 11,216** (buffer ซับ 3,000 = 21%) ·
> Assy GOR 270 → **0** (ของพอทั้งหมด) · LINE D ไม่มีของที่ STORE ⇒ ยังเต็ม 29,560
> · ปุ่ม **📦 หักสต็อก STORE** บนหัวเพจ (default เปิด) · จอบอกเสมอว่าหักไปกี่ชิ้น + ไม่หักยอดที่ไลน์เพราะอะไร

> ### ⏮️ Backward Scheduling — ไล่ย้อนจากวันส่งลูกค้าว่า "พาร์ทลูกต้องมีของก่อนวันไหน" (2026-09-22 · คำสั่ง user)
>
> คำถาม user: *"เหลือลีดไทม์พาร์ทลูกใช่มั้ย ต้องเสร็จก่อน แบบ reverse calculate ปะ — สมมติต้องการงานขายวันนี้
> แตก BOM รู้ว่าต้องผลิตพาร์ทแม่วันนี้ คำนวณสต็อกที่มีในสโตร์ หักลบเสร็จ ย้อนกลับ capacity ไลน์ปั๊ม
> เช็คงานลูกต้องมีของก่อนวันไหน"* ⇒ **ปิดข้อ ⏱️5 ของหัวข้อ BOM ข้างบน** (เดิมงานลูกถูกวางวันเดียวกับดิวแม่)
>
> **กลไก:** `src/utils/backwardPlan.js` — `scheduleBackward({ demandRows, ix, capPerDayOf, stock,
> prevWorkDay, today, explode, transferDays })` (pure · เทส `__tests__/backwardPlan.test.mjs` 18 เคส)
>
> ```
> ดิวลูกค้า ──หัก buffer STORE──▶ ยอดที่ต้องผลิตจริง
>    └─ lead time = ceil(ยอด ÷ กำลังต่อวันของไลน์นั้น)     ← มาจาก capacity จริง ไม่ใช่ค่าคงที่
>         └─ วันเริ่มผลิต = ถอยจากดิว (days−1) **วันทำงาน**
>              └─ ลูกต้องมีของ = ถอยอีก transferDays (default 1)
>                   └─ ทำซ้ำทุกชั้นของ BOM
> ```
>
> **🔴 กติกาที่ห้ามพลาด**
> 1. **lead time คิดจากกำลังผลิตจริง** — `capPerDayOf(mat)` = กำลังต่อกะ × (ไลน์มีกะดึก ? 2 : 1)
>    · ใช้ `allLines` **ไม่ใช่ `viewLines`** — กำลังของไลน์ไม่เปลี่ยนตามตัวกรองส่วนงานบนจอ
>    · ไลน์ที่ยังไม่รู้กำลัง = คิด 1 วันไว้ก่อน **แล้วขึ้นจอบอกจำนวนพาร์ท** ห้ามเงียบ (อาจสั้นกว่าจริง)
> 2. **ถอยวันต้องข้ามวันหยุด** — `makePrevWorkDay(calOf, addDays)` · วันหยุด**ทุกชนิดรวม `shutdown75`**
>    = ไม่ใช่วันทำงาน (กฎเดียวกับ `countWorkingDaysInMonth`) · วันที่ไม่มีแถวในปฏิทิน: จ-ศ = ทำงาน
>    · มี `guard = 400` กันวนไม่รู้จบเมื่อปฏิทินมาร์คหยุดยาวผิดปกติ
> 3. **มีของที่ STORE พอ = ตัดทั้งกิ่ง** — ไม่ต้องผลิตตัวแม่ ⇒ ไม่ต้องเบิกลูกของมันด้วย
>    · เดิน `demandRows` **เรียงตามดิวจากเร็วไปช้าเสมอ** (ของกองเดียวใช้ได้ครั้งเดียว — ใบที่ถึงกำหนดก่อนจองก่อน)
> 4. **ย้อนแล้วได้วันก่อนวันนี้ = 🚨 สายแล้ว ห้ามปัดเข้าวันนี้เงียบๆ** — เข้า `sched.late`
>    แล้วขึ้นแผงแดงบอก *"ต้องเริ่ม `needBy` เพื่อให้ทันดิว `dueOfParent` · ใช้เวลาผลิต ~N วัน"*
>    · ยอดของวันก่อนวันนี้ถูกยกมาเป็น **backlog ตั้งต้น (carry) ของวันแรก** ⇒ ปฏิทินยังเดินต่อได้
>    · ของค้างส่ง (เลยดิวแล้ว) ถือว่าต้องส่ง **วันนี้** แล้วไล่ย้อนจากวันนี้
> 5. **เพดาน `maxLeadDays = 60` + ธง `capped`** — พาร์ทที่นานๆ ผลิตทีมี median ต่ำมาก
>    (วัดจริง 22/09: มี 1 ใบคิดออกมาได้ **285 กะ**) ปล่อยไป = วันย้อนหลุดไปเป็นปี แล้วงานทั้งก้อน
>    ไปกองเป็น backlog วันแรกจนอ่านแผนไม่ออก ⇒ ตัดที่เพดาน **แต่จอต้องเขียนว่าตัด** (`ตัดที่เพดาน 60 วัน`)
> 6. **ไม่มี BOM / ปิดสวิตช์ 🌳 = ต้องยังจัดตารางของตัวแม่ได้** (`ix = null` ⇒ `hasBom = false`)
>    — คืนค่าว่างทั้งก้อน = แท็บรายวัน**ว่างทั้งหน้า** ซึ่งแย่กว่าการไม่มีงานลูก (มีเทสตรึงไว้)
> 7. **ต่อโซ่แบบ SAP ใช้ตัวตรวจเดิม** — ส่ง `explode: explodeBom` เข้าไปเพื่อให้ `flatDupes`
>    มาจากตัวเดียวกับระบบ **ห้ามเขียนตัวตรวจแบนซ้ำใหม่ในไฟล์นี้**
>
> **จอบอกที่มาเสมอ:** ใต้หัวเพจ "⏮️ ไล่ย้อนวันจากดิวลูกค้าแล้ว — lead time ของแต่ละชั้นคิดจากกำลังผลิตจริงของไลน์นั้น"
> + บรรทัด ⏱️ จำนวนพาร์ทที่ยังไม่รู้กำลัง + แผง 🚨 รายการที่สายแล้ว

- **แผนต่อไป (ยังไม่ทำ):** เฟส 2 what-if (เพิ่มคน/ลด NG ทันมั้ย) · เฟส 3 ผูก `ot_night_bookings` — กดจากแผนแล้วจองรถ OT/เปิดกะอัตโนมัติ + Telegram

---


---

## 📊 แท็บ Capacity — ภาษาเดียวกับสไลด์ของโรงงาน (`?tab=capacity` · 2026-09-24)

**ที่มา:** user ส่ง `Capacity_TSATP.4_update_June_26.pptx` มาพร้อมภาพหน้านี้ แล้วบอกว่า
*"capacity โรงงานเราดูกันแบบนี้ แต่ในโปรแกรมเราเป็นแบบนี้ **คนจะดูไม่เข้าใจ**"*

⇒ ปัญหาไม่ใช่ตัวเลขผิด แต่เป็น**คำศัพท์คนละชุด** — แท็บ 📆 รายเดือน พูดว่า "ต้องใช้กี่กะ / verdict"
ส่วนฝ่ายวางแผนพูดว่า "2 shift / ActWorkload / RworkOEE / Cap OEE Act / Diff OT OEE"
**🔴 ตั้งใจให้ 2 แท็บอยู่คู่กัน ห้ามยุบ** — คนละคนอ่าน คนละคำถาม

### คำศัพท์ (ถอดจากสไลด์ — ห้ามเปลี่ยนชื่อโดยไม่ถาม user)

| สไลด์ | ความหมาย | สูตร |
|---|---|---|
| `2 shift` | เพดานชั่วโมงของเดือนนั้นที่รูปแบบ 2 กะ | วันทำงาน × 15.5 ชม. |
| `ActWorkload` | ภาระงานจริง (ยังไม่คิด OEE) | Σ (ชิ้น × CT) ÷ 3600 |
| `RworkOEE` | ชั่วโมงที่ต้องใช้จริงเมื่อคิด OEE | `ActWorkload ÷ OEE` |
| `Cap OEE Act` | เพดานหลังคิด OEE | `เพดาน × OEE` |
| `Diff OT OEE` | เหลือ/ขาด | `Cap OEE Act − ActWorkload` |

### 🔴 กฎเหล็ก 1 — **1 กะของสไลด์ = 7.75 ชม. (465 นาที) ไม่ใช่ 490 นาทีของแท็บรายวัน**

ทุกไลน์ในสไลด์ใช้ `2 shift = วันทำงาน × 15.5 ชม.` ⇒ 1 กะ = 7.75 ชม.
แต่ `capacityModel.js` ใช้ `netShiftMin(570, 80) = 490` (08:00–17:30 หักพัก 80 นาที) — **ต่างกัน 25 นาที/กะ ≈ 5%**

**ตั้งใจให้ต่าง เพราะคนละนิยาม:**
- **490** = เวลาที่เดินเครื่องได้ → ใช้ตอบ "กะนี้ทำได้กี่ชิ้น" (แผนรายวัน)
- **465** = เวลาที่ฝ่ายวางแผนใช้คิดเพดานรายเดือน (ตัดเวลาเตรียม/ส่งงานออกอีกชั้น)

⇒ **ห้ามไปแก้ `DEFAULT_SHIFT_MIN` ให้เท่ากัน** จะทำให้แผนรายวันเพี้ยนทั้งระบบ
⇒ ตัวเลขนี้อยู่ในทะเบียน `capacity_shift_patterns` (DR) ฝ่ายวางแผน**แก้เองได้จากปุ่ม ⚙️ ในแท็บ ไม่ต้องแก้โค้ด**

### 🔴 กฎเหล็ก 2 — เพดานต่างกันที่ **"จำนวนวัน"** ด้วย ไม่ใช่แค่ชั่วโมง/วัน

`+ ทำเสาร์` = เอาเสาร์มานับเป็นวันทำงานเพิ่ม · `Max` = ทุกวันในเดือน
⇒ ทะเบียนจึงมี `day_source` (`working` / `working_sat` / `all`) คู่กับ `hours_per_day`
เขียนเป็น `ชั่วโมง × วันทำงาน` เฉยๆ = **เพดาน +Sat/Max ผิดทุกเดือน** (มีเทสจับ)

### 🔴 กฎเหล็ก 3 — ActWorkload ต้องมาจาก **เวลามาตรฐาน (qty × CT)** ห้ามใช้กำลังผลิตจริง

median ของจริงรวมความสูญเสียไปแล้ว ⇒ เอามาหารด้วย OEE อีก = **คิด OEE ซ้ำสองรอบ**
(ต่างจากแท็บรายวัน/รายเดือนที่ใช้ `capOfMat` median โดยเจตนา — คนละคำถาม)
· พาร์ทที่ยังไม่มี CT คิดภาระไม่ได้ → **จอต้องเขียนว่ากี่ชิ้นที่ไม่มี CT ห้ามเงียบ**

### OEE 2 เส้น (คำสั่ง user: *"โชว์ทั้งสองเส้นให้เทียบ"*)

- **จริง** = median OEE 60 วันของไลน์ (จาก `production_sessions.oee`) — ไม่มีประวัติ = เขียนบนจอว่า "ยังไม่มีประวัติ" ห้ามแอบใช้ 1.0
- **เป้า** = `A×P×Q` จาก `oee_targets` ของกรุ๊ป (ไลน์เอง → ไลน์แม่ → มาตรฐาน 90/90/99)
  🔴 **ห้ามอ่าน `target_oee` ตรงๆ** (คอลัมน์ vestigial · กฎเหล็ก CLAUDE.md)
- สลับได้ว่าตาราง/กราฟยึดเส้นไหน

### ไฟล์ / ตาราง

| | |
|---|---|
| `src/utils/capacityPatterns.js` | สูตรล้วน — `SEED_PATTERNS` · `monthDayCounts` · `patternHours` · `capacityRow` · `oeePair` (**เทส 22 เคส** ในด่าน build) |
| `src/components/CapacityBoard.jsx` | แท็บ (แถบกรอง + OEE 2 เส้น + ทะเบียน ⚙️ + กราฟ + ตาราง 12 เดือน) |
| `capacity_shift_patterns` (DR) | ทะเบียนรูปแบบกะ · migration `20260924_capacity_shift_patterns_dr.sql` (**apply แล้ว 24/09**) |

โหลดทะเบียนไม่ได้ → ถอยไปใช้ `SEED_PATTERNS` ในโค้ด (จอไม่พัง) **แต่ขึ้นแถบเตือนบนจอเสมอ** และปิดการแก้ไข

### ยังไม่ได้ทำ (รู้ตัว)

- สไลด์มีกราฟแท่งซ้อนแยกตาม**ลูกค้า/รุ่น** — ยังไม่ทำ (แท็บนี้รวมทั้งไลน์)
- สไลด์มี `Rolling vs Act (pcs)` + `Volume per Day` — ยังไม่ทำ
- เส้นเพดานวาดจากเดือนแรก (เหมือนสไลด์) — เดือนที่วันทำงานต่างกันมากจะเห็นเส้นไม่ตรงเป๊ะกับเดือนนั้น
