# 💬 กล่องรับ Feedback จากหน้างาน (2026-08-14 · คำขอ user)

> ย้ายมาจาก `CLAUDE.md` (2026-09-03 — แยกไฟล์เพื่อลด context) · โหลด**เฉพาะเมื่อแตะโมดูลนี้** · แก้ไฟล์นี้แทน CLAUDE.md เมื่อกฎของโมดูลเปลี่ยน


ปุ่ม **"💬 แจ้งปัญหา / ข้อเสนอแนะ"** ท้าย sidebar (ใต้เปลี่ยนรหัสผ่าน) → `src/components/FeedbackModal.jsx` (lazy chunk)
เดิม feedback วิ่งผ่าน LINE → **ตกหล่น · ค้นย้อนไม่ได้ · ไม่รู้ว่าเรื่องไหนแก้แล้ว**

- **ตาราง `user_feedback` (Main)** — migration `20260814_user_feedback.sql` · kind (bug/idea/question) · status (new/seen/doing/done/wontfix) · เก็บ **snapshot ชื่อ+role** ณ วันที่แจ้ง (โปรไฟล์เปลี่ยนทีหลังได้ — pattern เดียวกับ `lpa_audit_answers.question_text`)
- **RLS:** ทุกคนที่ login ส่งได้ (ผูก `auth.uid()`) · เห็นของตัวเองเสมอ · **admin/manager เห็น+จัดการทั้งหมด**
- **ไม่ทำเป็นหน้าแยก และไม่เพิ่ม permission key ใหม่โดยตั้งใจ** — เลี่ยงกับดัก seed `enum_range` ที่ทำให้ role ใหม่เข้าไม่ได้แบบ fail-closed · กล่องขาเข้าเป็น**แท็บในโมดัลเดียวกัน** เห็นเฉพาะ admin/manager ซึ่งตรงกับ RLS อยู่แล้ว
- **ออกแบบให้ "ส่งง่ายที่สุด"** (คนหน้างานใส่ถุงมือ/รีบ): พิมพ์ข้อความอย่างเดียวก็ส่งได้ · **`page_path` เก็บให้อัตโนมัติ** ไม่ต้องอธิบายว่าอยู่หน้าไหน
- **⚠️ ส่งไม่เข้าต้องบอกให้ชัด ห้ามขึ้น toast เขียว** — ยังไม่ apply migration (42P01) จะขึ้นข้อความบอกตรงๆ ว่าต้องแจ้ง admin (คนหน้างานจะได้ไม่นั่งรอคำตอบที่ไม่มีวันมา)
- **📋 ปุ่ม "คัดลอกให้ Claude"** ในกล่องขาเข้า — คัดลอกเรื่องที่แสดงอยู่เป็นข้อความ (สรุปรวมตามหน้า + รายเรื่อง: ประเภท/สถานะ/วันที่/ผู้แจ้ง/หน้า/ข้อความ) เอาไปวางในแชทได้เลย
  - **⚠️ จำเป็นเพราะ Claude Code เซสชันบนเว็บ "อ่านฐานข้อมูลตรงไม่ได้"** — Supabase MCP ติด approval ที่กดไม่ได้ในเซสชันแบบนั้น + network policy บล็อก `supabase.co` (ตรวจแล้ว 2026-08-14: `execute_sql` คืน `requires approval` · curl ได้ 403 ที่ proxy) → ถ้าไม่มีปุ่มนี้ ลูป "user แจ้ง → สั่ง Claude ไปเช็ค" จะสะดุดตรงขั้นสุดท้าย
  - รัน Claude Code แบบ interactive แล้วกดอนุมัติ MCP ได้ = อ่านตรงจากตารางได้เลย ไม่ต้องใช้ปุ่มนี้
  - clipboard API ถูกบล็อกในบางบริบท → มี fallback `execCommand('copy')` และถ้ายังไม่ได้ก็ **ขึ้น error ห้ามเงียบ**
- **⚠️ กฎ: แก้ feedback เรื่องไหนแล้ว ต้องอัพเดทแถวใน `user_feedback` ใน session เดียวกัน** (status + `admin_note` + `handled_by`/`handled_at`) — เคยเกิดจริง 2026-09-08: แก้ไปแล้ว 2 เรื่อง (Dashboard ตารางกะหมด 28/08 · ข้าม QA 04/09) แต่แถวยังค้าง `new` จน user ถามว่า "อันไหนแก้แล้ว" · **status ต้องตรงความจริง**: `done` = deploy แล้ว · `seen` = หาต้นเหตุเจอ/รับทราบแต่ยังไม่แก้ (เขียนต้นเหตุ + วิธีแก้ชั่วคราวลง note) · `doing` = กำลังทำ · ห้ามปล่อยแถวเป็น `new` ทั้งที่มีคนไล่ไปแล้ว · note เขียนให้คนหน้างานอ่านรู้เรื่อง (ทำอะไรไปแล้ว · ต้องทำอะไรต่อ · ถ้ายังติดให้แจ้งอะไร) ไม่ใช่ภาษาโค้ด
  - session ที่อ่านฐานตรงได้ (Supabase MCP ผ่าน — เช่น 2026-09-08 อ่าน/อัพเดท `user_feedback` ตรงได้ทั้ง Main และ DR) ให้ `update user_feedback set ...` เองเลย · session ที่อ่านไม่ได้ ให้วาง SQL update เต็มๆ ให้ user รันใน SQL Editor (Main `ewhdfqwfwofivojtsizn`)
### 🔁 งานตรวจอัตโนมัติรายวัน — Routine "ESM — ตรวจ feedback หน้างานรายวัน (08:30)" (2026-09-08 · คำขอ user)

user สั่ง "เข้าไปเช็คทุกวันแล้วมารีพอร์ทว่ามี feedback อะไรมาบ้าง + เสนอการแก้ไข" → ตั้งเป็น **Routine ของ Claude Code (scheduled trigger)** ไม่ใช่ cron ในโค้ด/edge function

- **trigger id `trig_01EtLWezxV4ZCPtn6qe1X6bs`** · cron `30 1 * * *` UTC = **08:30 น. ไทย ทุกวัน**
- **⚠️ ผูกกับ "เซสชันเดิม" (self-bind) ไม่ใช่เปิดเซสชันใหม่ — และนี่คือข้อจำกัดที่ต้องรู้ก่อนแก้:**
  - เวอร์ชันแรกตั้งเป็น `create_new_session_on_fire` (เปิดเซสชันใหม่ทุกวัน + แจ้งเตือนเข้ามือถืออัตโนมัติ) ซึ่ง**สวยกว่า** แต่ **เซสชันที่ถูกปลุกไม่มีเครื่องมือ Supabase MCP ติดไปด้วย** → อ่าน `user_feedback` ไม่ได้เลย = รอบตรวจไร้ความหมาย
  - พิสูจน์แล้วด้วยการวางแถวทดสอบใน `user_feedback` แล้วสั่งให้รอบตรวจมาอัพเดท → **แถวไม่ถูกแตะเลย** (ไม่ใช่การเดา)
  - พารามิเตอร์ `connectors` ของ `create_trigger` **ใช้กับ org นี้ไม่ได้** (`connectors parameter is not available for this organization`) จึงแนบ Supabase connector ไปกับ Routine ไม่ได้
  - ⇒ ทางที่ใช้ได้จริงคือ **fire เข้าเซสชันที่ถือ connector อยู่แล้ว** (เซสชันที่ตั้ง Routine) · แลกมาด้วย: ไม่มี push อัตโนมัติจากระบบ Routine → ตัวรอบตรวจจึงต้อง **เรียก `PushNotification` เองเมื่อมีเรื่องต้องอ่าน** (ไม่มีอะไรใหม่ = ห้ามส่ง กันเสียงรบกวน)
  - ถ้าวันหนึ่งอยากได้แบบเซสชันใหม่รายวัน ให้ user **สร้าง Routine จากหน้า Routines บน claude.ai แล้วแนบ connector Supabase เอง** แล้วค่อยลบตัวนี้
- **สิ่งที่รอบตรวจทำ:** อ่าน `user_feedback` ที่ยังไม่ปิด → ไล่ต้นเหตุจริงในโค้ด+ฐานข้อมูล → **อัพเดทแถวเป็น `seen` + `admin_note`** (ตามกฎด้านบน · เขียนแล้ว select นับแถวยืนยัน) → รายงาน + เสนอลำดับการแก้ · บวก **health check 3 จุดที่เคยทำหน้างานเดือดร้อนซ้ำ**: ตารางกะใกล้หมด (Dashboard กลายเป็น 0 เงียบๆ) · ใบซ่อม MO ค้างขั้นเดิมเกิน 7 วัน · feedback ค้าง `seen`/`doing` เกิน 7 วัน
- **ขอบเขตที่ตั้งใจจำกัด:** รอบตรวจ **ห้ามแก้โค้ด/merge/deploy เอง** — รายงานแล้วรอคำสั่ง (การแก้จริงยังผ่านกติกา build+crashsweep+rollback ตาม CLAUDE.md เหมือนเดิม) · ไม่มีอะไรใหม่ = ตอบบรรทัดเดียว
- **แก้/ปิด Routine:** สั่งในแชทได้เลย (update/delete trigger ด้วย id ข้างบน) หรือหน้า Routines ใน claude.ai — **แก้ prompt/เวลาใช้ update ห้ามลบแล้วสร้างใหม่** (เสียประวัติการรัน)
- **📌 กฎที่ตกผลึก: Routine ที่ต้องอ่าน/เขียนฐานข้อมูล ต้องยิงทดสอบ 1 รอบเสมอหลังสร้างหรือย้าย environment** แล้ววัดผลจาก**ของจริงในฐาน** (เช่น แถวทดสอบเปลี่ยนสถานะไหม) ห้ามเชื่อว่า "ตั้งแล้วใช้ได้" — งานอัตโนมัติที่พังเงียบแย่กว่าไม่มีงานอัตโนมัติ

- **ยังไม่ทำ:** แนบรูปหน้าจอ (โมดัลบอกให้ส่งใน LINE ตามหลังไปก่อน) · แจ้งเตือน admin ตอนมีเรื่องใหม่ (ยังต้องเปิดกล่องดูเอง)

---

## 📎 แนบรูปหน้าจอ (2026-09-22 · คำขอจากหน้างาน)

**ที่มา:** คุณสุรเสนแจ้งทาง LINE 22/09 *"ตอนผมรายงานบัคไป เพิ่มให้มีแนบรูป"* แล้วส่งสกรีนช็อต
ที่เขาวงกรอบสีไว้ 3 จุดมาให้ดู (แดง = แก้ไข · เหลือง = เปลี่ยนชื่อ · ฟ้า = จัดคอลัมน์)

> 🔴 **ตัวฟอร์มเองเป็นคนบอกให้เขาไปใช้ LINE** — ข้อความเดิมใต้ช่องพิมพ์เขียนว่า
> *"ถ้ามีรูปหน้าจอ ส่งใน LINE ตามหลังได้ (ระบบยังไม่รับไฟล์แนบ)"*
> ซึ่งย้อนแย้งกับเหตุผลที่สร้างกล่องนี้ขึ้นมาแต่แรก (*"เดิม feedback วิ่งผ่าน LINE → ตกหล่น ค้นย้อนไม่ได้"*)
> **บทเรียน: ข้อความช่วยเหลือที่บอกทางออกนอกระบบ = ฟีเจอร์ที่ขาด ไม่ใช่คำอธิบาย** — เจอเมื่อไหร่ให้ถือเป็นงานค้าง

### ทำอะไรไป
- `user_feedback.images text[]` (Main · migration `20260922_feedback_images_main.sql` — **apply แล้ว 22/09**)
  · แถวเดิม 15 แถวได้ `{}` ครบ ไม่มีจอไหนเปลี่ยนพฤติกรรม
- bucket **`feedback-images`** (public read · 5 MB · jpeg/png/webp) — **ไม่รับ GIF** (บีบไม่ได้ เฉลี่ย 4.3 MB/ไฟล์)
- policy เขียน/ลบ **แคบกว่า bucket อื่นในระบบโดยตั้งใจ**: ลงได้เฉพาะโฟลเดอร์ `<auth.uid()>/` ของตัวเอง
  · ลบได้ = เจ้าของ หรือ admin/manager (รูปพวกนี้มาจาก "ใครก็ได้ที่ login" ไม่ได้ผ่านหน้าที่มีสิทธิ์คุม)
- บีบด้วย `compressScreenshotImage()` (`utils/layoutImage.js`) = **WebP 1920px q0.85 ≤ 0.5 MB**
  · **ห้ามย่อความละเอียดลงกว่านี้** — สกรีนช็อตคือตัวหนังสือ ย่อแล้วไม่รู้ว่ากรอบสีชี้ปุ่มไหน = รูปเสียเปล่า

### กติกาที่ตั้งใจเลือก (อย่าเปลี่ยนโดยไม่อ่านเหตุผล)
- **ไม่ทำเครื่องมือวาดกรอบในแอป** — เขาวงเองในมือถือได้อยู่แล้วและเร็วกว่า สิ่งที่ขาดคือ "ช่องแนบ" ไม่ใช่ "โปรแกรมวาด"
- **ช่องแนบอยู่ติดใต้ช่องพิมพ์ ไม่ใช่ท้ายฟอร์ม** — บทเรียนจาก "รูปก่อนซ่อม" ใน `/mtn-repair` ที่อยู่ล่างสุด
  แล้ว**คนแจ้งใส่แค่ 25% (116/473 ใบ) ขณะที่ช่างใส่รูปหลังซ่อม 69%** ⇒ ช่องที่มองไม่เห็น = ช่องที่ไม่มี
- **อัปรูปตอน "กดส่ง" ไม่ใช่ตอนเลือก** — เลือกแล้วเปลี่ยนใจปิดหน้าต่าง จะได้ไม่มีไฟล์กำพร้าค้าง storage
  · insert ล้ม = ลบรูปที่อัปไปแล้วทิ้งทันที
- **คอลัมน์ `images` หายไป (ยังไม่ apply migration) ต้องส่งข้อความได้อยู่** — ตัด `images` แล้ว insert ใหม่
  (กฎเดิม: การแจ้งเรื่องต้องไม่พังเพราะฟีเจอร์เสริม)
- ลิงก์รูปถูกส่งเข้า Telegram ด้วย — คนรับกดดูได้เลยโดยไม่ต้องเปิดแอปก่อนถึงจะรู้ว่าเรื่องอะไร
- ปุ่ม 📋 คัดลอกให้ Claude เตือนไว้ว่า **Claude เปิด URL รูปเองไม่ได้** ⇒ คนที่เอาไปวางต้องลากรูปเข้าแชทด้วย

### 🐛 กับดักที่เจอตอนทำ (build/lint/เทสผ่านหมด เห็นตอนกดจริงเท่านั้น)
1. **`const fl = e.target.files; e.target.value = '';`** — `files` เป็น **live FileList ผูกกับ input**
   เคลียร์ `value` ปุ๊บลิสต์ว่างทันที ⇒ **แนบไม่ติดสักใบแบบเงียบ** · ต้อง `[...e.target.files]` ก่อนเสมอ
   → เพิ่มด่าน `filelist-copy-before-reset` ใน `regressionGuards` แล้ว (พิสูจน์แล้วว่าจับได้จริง)
2. **`useEffect(() => () => revoke(shots), [shots])`** — cleanup วิ่งทุกครั้งที่ลิสต์เปลี่ยน
   แล้วไป revoke blob URL ของรูปที่**ยังโชว์อยู่บนจอ** (แนบใบที่ 2 แล้วใบที่ 1 กลายเป็นรูปเสีย)
   · ต้องเก็บ `ref` แล้ว revoke ตอน unmount อย่างเดียว
3. **harness ไม่เคย mount `<ToastContainer/>`** ⇒ กฎ *"ปฏิเสธไฟล์ต้องขึ้น toast ห้ามเงียบ"* **ตรวจอัตโนมัติไม่ได้มาตลอด**
   (เทสรายงานว่าไม่มีข้อความเตือน ทั้งที่โค้ดเรียก `toast.error` ถูกแล้ว) — เพิ่มใน `audit/main.jsx` แล้ว

### harness
`?p=__feedback` ใน `audit/` — โมดัลนี้ถูก render จาก App shell ไม่ใช่จากหน้าไหน
⇒ **`crashsweep` ที่ไล่เปิดทีละ "หน้า" มองไม่เห็นเลย** ต้องมี harness ของตัวเอง

**ผลวัดจริง (1500px + 390px):** แนบ 2 → เพิ่มเป็น 3 → ชนเพดาน 4 → ถอดเหลือ 3 ·
`naturalWidth` ของทุก preview ยังเป็น 120 ตลอด (blob ไม่ถูก revoke ผิดจังหวะ) ·
GIF/PDF ถูกปฏิเสธพร้อมเหตุผล+ทางแก้ · ไม่มีของล้นโมดัล · `__crash` = null · pageerror = 0

---

## 💬 ตอบกลับคนแจ้งได้แล้ว (2026-09-22 · คำสั่ง user "ตอบคำถาม user ที่แจ้งเข้ามา")

**บั๊กที่เจอตอนจะตอบ:** คอลัมน์ `admin_note` มีมาตั้งแต่แรกและเขียนลง DB ได้ แต่
**`FeedbackModal.jsx` ไม่เคย render มันเลยสักที่** (grep ทั้งไฟล์ = 0 จุด)
⇒ คนหน้างานส่งเรื่องเข้ามา แล้ว**ไม่มีวันเห็นคำตอบ** เห็นแค่ป้ายสถานะเปลี่ยนสี
— ซึ่งเป็นเหตุผลที่คนเลิกแจ้ง (กล่องนี้ทั้งกล่องมีคนแจ้งแค่หลักหน่วยต่อเดือน)

**ที่ทำ:**
- การ์ดแต่ละเรื่องโชว์บล็อก **"💬 คำตอบจากทีมงาน"** (เส้นซ้ายสี accent) เมื่อ `admin_note` ไม่ว่าง
  — เห็นทั้งฝั่ง admin และ**ฝั่งคนแจ้ง** (แท็บ 📄 เรื่องที่ฉันส่ง · RLS ให้เห็นของตัวเองอยู่แล้ว)
- admin มีช่องพิมพ์ตอบใต้การ์ด + ปุ่ม 📨 ส่งคำตอบให้ผู้แจ้ง → เขียน `admin_note` +
  `handled_by`/`handled_at` + ดัน `new` → `seen` ให้เอง
- **ยิงแจ้งเตือนเข้ากระดิ่ง 🔔 ของคนแจ้ง** (insert `notifications` ตรง · pattern เดียวกับ mention
  ใน `EventComments`) — ไม่งั้นเขาไม่รู้ว่ามีคำตอบ ไม่มีใครเปิดกล่อง feedback เองทุกวัน
- ⚠️ RLS ปฏิเสธ UPDATE = **สำเร็จ 0 แถว ไม่มี error** ⇒ `.select('id')` แล้วนับแถว
  ก่อนขึ้น toast เขียว (กฎเหล็กเขียน DB ข้อ 2)

---

## เปิดกล่องนี้จากหน้าอื่น พร้อมข้อความตั้งต้น (2026-09-22)

`src/utils/feedbackPrefill.js` — `openFeedback('ข้อความตั้งต้น…')` ยิง event `esm:open-feedback`
→ `Sidebar` (ที่ mount กล่องนี้อยู่) เปิดโมดัล → `FeedbackModal` อ่าน prefill ตอน mount ครั้งเดียวแล้วล้าง

- ใช้ครั้งแรกโดยหน้า `/schema` ปุ่ม **🐛 แจ้งบัคเรื่องตารางนี้** (แนบชื่อตาราง/ฐานข้อมูล/PK/FK/หน้าที่ใช้ไปให้เลย)
- **หน้าอื่นที่อยากมีปุ่มแบบนี้ให้เรียกตัวนี้ ห้ามทำกล่องรับ feedback ใบที่ 2** — ที่รับเรื่องมีที่เดียวคือ
  `user_feedback` ผ่านโมดัลนี้ (ไม่งั้นกล่องขาเข้าแตกเป็นหลายที่ แล้วเรื่องตกหล่นเหมือนตอนใช้ LINE)
- ไม่เพิ่ม permission key ใหม่ (กล่องเดิมไม่มี key อยู่แล้ว)

---

## 📮 รอบตอบกลับจริงรอบแรก (2026-09-22 · คำสั่ง user *"ตอบคำถาม user ที่แจ้งเข้ามา ภาษาที่เค้าจะเข้าใจ"*)

ตอบครบ 4 เรื่องที่ค้าง เขียนลง `admin_note` + ยิงกระดิ่ง 🔔 ให้คนแจ้งทุกคน (4 แถวใน `notifications`)

| เรื่อง | ผู้แจ้ง | คำตอบ |
|---|---|---|
| รูป MO 16:9 | สุรเสน | **ทำเสร็จแล้ว** — แต่บอกตรงๆ ว่าทำ *ต่างจากที่ขอ*: ครอบกลางภาพให้อัตโนมัติ แทนการปฏิเสธรูป (user ตัดสิน "3.ก") + เตือนให้ถ่ายโดยวางของสำคัญไว้กลางภาพ |
| รื้อฟอร์มแจ้งซ่อม | สุรเสน | **ไม่แก้ — ถามกลับ 4 ข้อ** (user: *"เราไม่เข้าใจความต้องการ user แก้แล้วกระทบรึป่าว"*) ระบุ blast radius ของแต่ละข้อให้เขาเห็น โดยเฉพาะ **"แผนก" ที่ยังไม่มีชั้นนี้ในระบบ** และ lookup ลูกค้า/โมเดล/Cost center ที่ยังไม่รู้ว่าต่อท่อจากทะเบียนไหน |
| ไลน์ Rework PD1 | ณัฐวุฒิ | **ไลน์มีอยู่แล้ว** — สาเหตุที่ดูเหมือนหายคือยังไม่มีสินค้าผูกสักตัว (0 รายการ) บอกวิธีเพิ่มเอง + ถามกลับว่างาน Rework เป็นลักษณะไหนเพื่อตั้ง `line_type` (ตอนนี้ null) |
| เมนูย่อย + ไฮไลต์เมนู | สุรเสน | **ยืนยันว่าเป็นจริง ยังไม่แก้** — บอกเหตุผลตรงๆ ว่าแถบเมนูเป็นของกลาง 70+ หน้า + ให้ทางลัดใช้ระหว่างรอ (หัวเพจบอกชื่อแท็บ · บุ๊กมาร์กได้เพราะแท็บอยู่ใน URL) |

**กติกาการเขียนคำตอบที่ตกผลึก (ใช้กับรอบถัดไป):**
- **เขียนถึงคนแจ้ง ไม่ใช่เขียนถึง dev** — ห้ามใช้ชื่อไฟล์/ชื่อคอลัมน์/ศัพท์ระบบในคำตอบ (บันทึกเชิงเทคนิคไว้ในไฟล์โมดูล)
- **ทำต่างจากที่ขอ = ต้องบอกว่าต่างตรงไหนและทำไม** ห้ามตอบแค่ "ทำแล้ว" (เขาจะไปเทสต์ตามที่ขอแล้วคิดว่าพัง)
- **ไม่แก้ = ต้องบอกเหตุผลจริง + ให้ทางลัดใช้ระหว่างรอ** "อยู่ในคิว" เฉยๆ = คำตอบที่ทำให้คนเลิกแจ้ง
- **ไม่เข้าใจโจทย์ = ถามกลับพร้อมบอกผลกระทบของแต่ละทางเลือก** ไม่ใช่เดาแล้วทำ (งานรื้อฟอร์มที่เดาผิด = คนทั้งโรงงานใช้ไม่ได้)
