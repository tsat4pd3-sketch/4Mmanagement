# 🔍 วิเคราะห์ปัญหา (ซ่อมบำรุง) — QC 7 Tools · `/mtn-analysis` (2026-09-22)

> **ที่มา (คำสั่ง user):** *"ตอนนี้ หมวด mtn ยังไม่มี พวก dashboard ปัญหา เครื่องจักร/แม่พิมพ์/jig fixture เลย
> สรุปปัญหา ระบบวิเคราะห์ qc7tools ยังไม่เห็น"*

หน้า `src/pages/MtnAnalysis.jsx` (หมวด **การตรวจสอบและซ่อมบำรุง**) — **อ่านอย่างเดียว**
ไม่มีปุ่มเขียนข้อมูลเลย ⇒ ไม่มี `resource:action` ใหม่ มีแค่ `page:/mtn-analysis`

---

## โครงแท็บ — 2 ชั้น คนละ query param

| ชั้น | param | ค่า |
|---|---|---|
| แท็บหลัก | `?tab=` | `kpi` (📊 KPI ช่าง · default) · `qc7` (🧪 QC 7 Tools) |
| แท็บชนิดอุปกรณ์ (เฉพาะใน `qc7`) | **`?asset=`** | `machine` (default) · `die` · `jig` · `other` |

> **⚠️ แท็บซ้อนแท็บห้ามใช้ `tab` ซ้ำกัน** (UI-CONVENTIONS §6.8) — `useTabParam` อ่าน/เขียน query param ตัวเดียว
> ใช้ซ้ำ = กดแท็บลูกแล้วแท็บแม่เด้งตาม (หรือเด้งกลับ default เงียบๆ)

### 📊 แท็บ `kpi` — ย้ายมาจาก `/mtn-repair?tab=kpi` (2026-09-22 · คำสั่ง user *"ฟังก์ชันของช่าง เหมือนไปกระจายอยู่หลายหน้า"*)

`src/components/MtnKpiPanel.jsx` — MTTA/MTTR/MDT · ความพึงพอใจ 5 ด้าน · ความน่าเชื่อถือรายเครื่อง (MTTR/MTBF จาก downtime จริง)
· **กฎเหล็กเดิมของ panel นี้ยังอยู่ครบ ดู `docs/modules/mtn-work-order.md` §แท็บ KPI ช่าง** (โดยเฉพาะ
  "MTTR 2 ตัวจากคนละฐาน ต้องติดป้ายที่มาเสมอ" — การ์ดบน = จากใบ MO ที่ปิดแล้ว · ตารางล่าง = จาก downtime ของเครื่อง)

> **🔴 พาเรโตอยู่ที่แท็บ `qc7` ที่เดียว — ถอดออกจาก KPI panel แล้ว ห้ามใส่กลับ**
> เดิมมีพาเรโตลักษณะปัญหาทั้งใน `/mtn-repair?tab=kpi` และในหน้านี้ = **จอเดียวกันตอบคนละเลข**
> (คนละตัวกรอง คนละฐานเวลา) ซึ่งเป็นบทเรียนเดิมของโมดูล MTN อยู่แล้ว ⇒ เหลือปุ่มลิงก์ข้ามแท็บแทน

`/mtn-repair?tab=kpi` และ `?tab=equip` redirect มาที่ `?tab=kpi` (bookmark เก่าไม่ตาย)

---

## ทำไมเป็นหน้าใหม่ ไม่ใช่แท็บที่ 6 ของ `/mtn-repair`

| เหตุผล | รายละเอียด |
|---|---|
| คนละงาน | `/mtn-repair` = หน้า**ทำงาน** (เปิดใบ · เดินขั้น · คลังอะไหล่) · หน้านี้ = หน้า**วิเคราะห์** |
| ขนาด bundle | chunk `MtnRepair` = **347 KB** อยู่แล้ว (หลังย้าย KPI/อะไหล่ออก เหลือ **207 KB**) — ยัดกราฟเข้าไปอีก = คนที่แค่จะเปิดใบต้องโหลดตาม (chunk ใหม่ = 44 KB) |
| คำพูด user | พูดถึง "**หมวด** mtn" (เมนู) ไม่ใช่ "หน้าแจ้งซ่อม" ⇒ ต้องมีรายการเมนูของตัวเอง |

---

## 📊 พาเรโต — เปลี่ยนเป็นแท่งตั้งมาตรฐานสากล (2026-09-22)

user เทียบพาเรโตของเรากับใบมาตรฐาน (Excel · ASQ · QI Macros) แล้วสรุปว่า *"ยังเทียบกันไม่ติดเลย …
แนวนอนไม่เวิค เป็นแนวตั้งและเอียง text เอา"* ⇒ เปลี่ยนตัววาดที่ `ParetoAbcChart` จุดเดียว
**กราฟที่ได้ผลพร้อมกัน 5 จุด:** หน้านี้ · `/oee-analytics` ×2 · `/dept-dashboard` ×2

- ของเดิม = **แท่งนอน ความหนาไม่เท่ากันตามกลุ่ม ABC** (ออกแบบไว้เพื่อแก้ปัญหาชื่อยาวเขียนทับกันบนแกน)
- ของใหม่ = `<ParetoChart>` แท่งตั้งตามตำรา · แก้ปัญหาชื่อยาวด้วย **ป้ายเอียง -45°/-90°** แทนการพลิกกราฟ
- กติกาเต็ม + กับดักที่เจอตอนทำ → `docs/UI-CONVENTIONS.md` §6.9 · สูตร `utils/pareto.js` · เทส `paretoGeometry.test.mjs`
- **🔴 ห้ามกลับไปวาดแท่งนอน** — มีด่าน `regressionGuards` กฎ `pareto-via-ParetoChart`

---

## 🔴 แกนชนิดสินทรัพย์ — **ห้ามแยกด้วย `mtn_orders.mtn_dept`**

วัดจริง 22/09/2026 (473 ใบ):

| `mtn_dept` | จำนวนใบ |
|---|---|
| `production` (ช่างประจำไลน์) | **422 (89%)** |
| `jig_maintenance` | 28 |
| `maintenance` | 19 |
| `die_maintenance` | 3 |

ทีม `production` ซ่อม **ทั้งเครื่อง/จิ๊ก/แม่พิมพ์ปนกัน** ⇒ ถ้าแยกแท็บด้วยทีม
แท็บ "แม่พิมพ์" จะเหลือ 3 ใบ และ "JIG" เหลือ 28 ใบ **ทั้งที่มีงานจริงมากกว่านั้นมาก**

**ตัวแยกที่ถูก = `machines.equipment_kind`** (ทะเบียนเครื่องจักร · die 266 · machine 249 · jig 162 · facility 38)
ผ่าน `assetClassOf(row, kindByMachineNo)` ใน `src/utils/qc7.js`

```
① เลขเครื่องในใบตรงกับทะเบียน → ใช้ equipment_kind (known: true)
② ไม่ตรง → เดาจาก item_type / ชื่อเครื่อง        (known: false — จอต้องบอกว่าเดา)
③ เดาไม่ออก → 'other' = แท็บ "ยังแยกชนิดไม่ได้"  **ต้องมีตะกร้ารับเสมอ ห้ามให้ใบหายจากจอ**
```

> บทเรียนเดียวกับ `line_type` ที่ว่างแล้วต้องไปอยู่ท้ายลิสต์ ไม่ใช่หายไปจาก dropdown
> · KPI บนหัวจอบอกเสมอว่า "แยกชนิดจากทะเบียนได้กี่ใบ / เดากี่ใบ"

**แท็บ:** 🏭 เครื่องจักร (รวม facility) · 🔨 แม่พิมพ์ (DIE) · 📐 JIG/Fixture · ❓ ยังแยกชนิดไม่ได้
— **แท็บที่ไม่มีข้อมูลยังต้องอยู่** กดเข้าไปแล้วเห็นว่า "ช่วงนี้ไม่มีงานกลุ่มนี้" ไม่ใช่หายไปเฉยๆ

---

## เครื่องมือ QC 7 อย่าง — สูตรอยู่ `src/utils/qc7.js` (pure + มีเทส 26 เคส)

| # | เครื่องมือ | ฟังก์ชัน | ตอบคำถามอะไร |
|---|---|---|---|
| ① | ใบตรวจสอบ (Check sheet) | `checkSheet()` | อุปกรณ์ไหน × ปัญหาแบบไหน ซ้ำบ่อยสุด (ตารางนับไขว้) |
| ② | พาเรโต | **`ParetoAbcChart` เดิม** (`utils/pareto.js`) | ปัญหาไหนกิน 80% แรก |
| ③ | ผังก้างปลา | `fishbone()` | สาเหตุกองอยู่แกนไหน (4M/6M) |
| ④ | ฮิสโตแกรม | `histogram()` | เวลาซ่อม/เวลาหยุดกระจายยังไง มีหางยาวแค่ไหน |
| ⑤ | ผังกระจาย | `scatter()` + `pearson()` | ซ่อมบ่อย ↔ เสียเวลามาก ไปด้วยกันไหม |
| ⑥ | กราฟควบคุม | `controlChartXmR()` | สัปดาห์นี้ผิดปกติ หรือเป็นความผันแปรปกติ |
| ⑦ | กราฟ/แบ่งชั้น | `runChart()` · `stratify()` | แนวโน้มดีขึ้นไหม · ใครเป็นตัวหลัก |

**ตัววาดอยู่ `src/components/Qc7Charts.jsx`** (วาดอย่างเดียว ห้ามคิดเลขเอง —
บทเรียนเดียวกับ `ParetoAbcChart` ที่สูตรเคยฝังใน `.jsx` แล้วไม่เคยถูกเทสเลย)
⇒ **โมดูลอื่น (QA/ผลิต/PE) เอา component ชุดนี้ไปใช้ต่อได้เลย ห้ามเขียนกราฟพวกนี้ใหม่**

---

## 🔴 กฎที่ต้องรักษา (กฎความซื่อสัตย์ของจอ)

- **ข้อมูลไม่พอ = คืน `{ ok:false, reason }` แล้ววาดกล่อง `<NotEnough>` พร้อมเหตุผล**
  ห้ามคืน 0 · ห้ามวาดกราฟเปล่า · ห้ามซ่อนแผง (0 = "วัดแล้วได้ศูนย์" คนละเรื่องกับ "ยังวัดไม่ได้")
- **KPI ที่ยังวัดไม่ได้ขึ้น `—` + "ยังไม่มีข้อมูลพอ"** ไม่ใช่ 0 ที่อ่านแล้วนึกว่าดีเยี่ยม
- **ค่าที่ไม่ถูกนับต้องบอกจำนวน** — "⚠️ 12 รายการยังไม่มีเวลา — ไม่ถูกนับ" (ไม่ตีเป็น 0 นาที)
- **ตัดหางต้องบอกว่าซ่อนไปเท่าไหร่** (`hiddenRows`/`hiddenCols`/`hidden`) — ตัดเงียบ = ตารางโกหกยอดรวม
- **`LCL` ของกราฟควบคุมที่คำนวณได้ติดลบ ต้อง clamp เป็น 0 + เขียนบนจอว่า clamp**
  (ค่า "นาที"/"จำนวนครั้ง" ติดลบไม่ได้ — ไม่บอก = คนอ่านว่า "ยังลดได้อีก −30 นาที")
- **ค่าสุดโต่งของฮิสโตแกรมไปอยู่ "ถังล้น" สีม่วงที่มองเห็น ห้ามกลบรวมถังปกติ ห้ามทิ้ง**
  (เคสจริง: ใบเดียวรอ supplier 313 ชม. ท่ามกลางใบปกติ 10-20 นาที)
- **`r` ของผังกระจายไม่ใช่เหตุผล** — จอเขียนกำกับเสมอว่า "ไปด้วยกัน ≠ ทำให้เกิด"
- **ช่วงเวลาที่ไม่มีเหตุการณ์ = 0 จริง ต้องอยู่บนกราฟ** ⇒ ผู้เรียกต้องส่ง `keys` (ทุกสัปดาห์ในช่วง)
  ให้ `runChart`/`controlChart` ไม่งั้นสัปดาห์ที่เงียบหายไป แล้วแนวโน้มเพี้ยน

---

## ⚠️ ก้างปลาส่วนใหญ่ "เดา" มา — ต้องบอกบนจอ

วัดจริง 22/09: `mtn_orders.cause_category` มีค่าแค่ **5 จาก 473 ใบ (1%)**
⇒ ผังก้างปลาที่ปั้นจากช่องนั้นอย่างเดียวจะ**ว่างเปล่าทั้งผัง** = จอโกหกว่า "ไม่มีปัญหา"

ลำดับตัดสินใน `boneOf(category, text)`:
1. **ค่าที่คนเลือกไว้จริงชนะเสมอ** (`man` / `method` / `part_life`→material)
2. ไม่มี (หรือเลือก `other` ซึ่งยังไม่บอกว่าอะไร) → เดาจากคำใน `root_cause`/`cause_other`/อาการ/หมายเหตุ
3. เดาไม่ออก → แกน `unknown` **ที่ต้องโชว์บนผัง ห้ามซ่อน**

จอแสดงแถบเหลือง "⚠️ X% ของรายการ ระบบเดาแกนให้จากข้อความ" + ชิป "เดา" รายบรรทัด
และชี้ทางแก้: *ให้ช่างเลือก "สาเหตุเกิดจาก" ตอนบันทึกขั้นลงมือซ่อม แล้วผังนี้จะแม่นขึ้นเอง*

**ลำดับคำเดาใน `BONE_WORDS` สำคัญ** (measure → man → method → material → env → machine):
คำกว้างของแกนหลังจะกลืนประโยคของแกนหน้า — `"ขาด"` (material) กลืน `"ขาดทักษะ"` (man) ·
`"ช่าง"` อยู่ท้ายสุดใน machine (ประโยคที่พูดถึงช่างมักกำลังเล่าอาการเครื่อง)

---

## แหล่งข้อมูล + egress

| แหล่ง | ตาราง | ปริมาณ (22/09) | มีอะไร |
|---|---|---|---|
| 🛠️ ใบซ่อม MO | `mtn_orders` (DR) | 473 | สาเหตุ · วิธีแก้ · ค่าใช้จ่าย · TTR |
| ⏱ เครื่องหยุด | `downtime_logs` (DR) | 9,518 | ปริมาณเยอะกว่ามาก แต่ไม่มีสาเหตุเชิงโครงสร้าง |

- **ไม่ poll ไม่ subscribe** — โหลดตอนเปิด / กด ↻ / เปลี่ยนช่วงเวลาเท่านั้น
  (หน้าวิเคราะห์ ไม่ใช่บอร์ดหน้างาน ⇒ กฎ `useLiveBoard` ไม่ใช้ที่นี่)
- `select` เฉพาะคอลัมน์ที่ใช้จริง (`MO_COLS` 20 คอลัมน์ จาก 116 · `DT_COLS` 8 คอลัมน์)
  **เพิ่มฟิลด์ใหม่ต้องเติมในลิสต์นั้น** — กฎเพดาน egress (CLAUDE.md ข้อ 11)
- ช่วงเวลา default = **90 วัน** (เลือก 30/90/180/365)

---

## สิทธิ์

`page:/mtn-analysis` — migration `20260922_page_mtn_analysis_main.sql` (**MAIN** · apply แล้ว 22/09)
ก๊อป role ชุดเดียวกับ `page:/mtn-repair` (12 role) เพราะ **ไม่มีข้อมูลใหม่โผล่ให้ใครที่ยังไม่เคยเห็น**
— หน้านี้คือบทสรุปของใบซ่อมชุดเดิม

ย้อนกลับ: `delete from role_permissions where permission_key = 'page:/mtn-analysis';`

---

## 📌 ยังไม่ทำ (ห้ามหยิบไปทำเองจนกว่า user สั่ง)

- **เชื่อมเข้า 8D / Kaizen** — กดจากแท่งพาเรโตแล้วเปิดใบ 8D หรือโปรเจคปรับปรุงได้เลย
  (ของที่มีอยู่: `docs/CLOSED-LOOP-8D-PE.md` · `/improvements`)
- **`downtime_logs` ยังไม่มีไลน์** — ต้อง join `production_sessions` ⇒ แท็บ "เครื่องหยุด"
  ตอนนี้มิติ "ไลน์" ขึ้น `(ไม่ระบุไลน์)` ทั้งหมด · แก้ได้ถ้ายอมจ่าย query เพิ่ม
- **export ใบ QC7 เป็นเอกสาร** — ถ้าทำต้อง register เข้า `/doc-forms` ตามกฎเอกสาร
- **ให้ QA/ผลิตใช้ `Qc7Charts` ด้วย** — component พร้อมแล้ว รอ user สั่งว่าจะเอาไปไว้หน้าไหน

---

## 🗑️ การจัดประเภท — ทำไม "ไม่ระบุ/อื่นๆ" เคยขึ้นอันดับ 1+2 (2026-09-23)

user แจ้ง: *"ไม่ระบุกับอื่นๆ มาอันดับ 1 กับ 2 การวิเคราะห์จะไม่มีประโยชน์เลย"* (65% + 33% = **98%**)

### ต้นเหตุ 3 ข้อ (วัดจากข้อมูลจริง 493 ใบ) — **ไม่ใช่ปัญหาที่ต้องใช้อัลกอริทึมจับกลุ่มข้อความ**
1. **โค้ดเขียนทับค่าที่รู้อยู่แล้ว** — `handleCreateMoFromDt` (DailyReport) hardcode
   `problem_characteristic:'อื่นๆ'` และไม่เซ็ต `problem_group` เลย ทั้งที่ประเภทดาวน์ไทม์
   อยู่ในมือตั้งแต่กดปุ่ม ⇒ **325 ใบ** · บั๊กคลาสเดียวกันที่ `PMCheckData` (ใบจากผลตรวจ PM/AM)
2. **ทีม `production` ไม่มีอาการในทะเบียนเลยสักแถว** ทั้งที่เปิด 288/325 ใบ
   (`mtn_problem_types` มีแต่ของ die_maintenance 22 · maintenance 17 · jig_maintenance 13)
   ⇒ ช่างเลือกอะไรไม่ได้นอกจาก "อื่นๆ"
3. **"เปลี่ยนตามรอบ PM" 54 ใบถูกนับเป็นปัญหา** ทั้งที่เป็นงานตามแผน

### ที่แก้ไปแล้ว
- คอลัมน์ `dr_downtime_types.mo_problem_group` = จับคู่ประเภทดาวน์ไทม์ → กลุ่มปัญหา
  **32 ประเภท → 11 กลุ่ม · user ตรวจและยืนยันเองในแชท 23/09 (ห้าม AI เดา taxonomy ของโรงงาน)**
  · "หัว Mandrel" = tool die ของเครื่อง Bending (user ชี้) จึงอยู่กลุ่มเครื่องขึ้นรูป ไม่ใช่กลุ่มงานเชื่อม
- seed ทะเบียนอาการของทีม `production` จากประเภทที่เกิดจริง
- backfill ย้อนหลัง + สำรองค่าเดิมใน `archive.mtn_orders_problem_20260923`
- migration `20260923_mo_problem_group_from_downtime_dr.sql` (**apply แล้ว 23/09** · มีบล็อก ROLLBACK)

**ผลวัดจริงหลังแก้:** ไม่ระบุกลุ่ม **65% → 4.3%** · อันดับ 1 กลายเป็น เลเซอร์ 17% → เชื่อม-ของสิ้นเปลือง 17%

### 🔴 กฎที่ต้องรักษา
- **ห้าม hardcode `'อื่นๆ'` ตอน insert** — มีด่าน `regressionGuards` กฎ `no-hardcoded-other-category`
- **แยกงานตามแผนออกจากพาเรโตปัญหา แต่ต้องบอกบนจอว่ากันไปกี่ใบ** (`utils/unclassified.js`)
- **ห้ามเดา *taxonomy*** (สร้าง/ตั้งชื่อหมวดเอง) — ต่างจาก "เดาว่าใบนี้เข้าหมวดไหน" ที่ทำได้ ดูรอบ 2 ข้างล่าง
- audit 23/09 จุดอื่น: ของเสีย (defect) ✅ สะอาด · 4M ✅ สะอาด · downtime 🟡 ถังขยะ ~6.3% → **แก้แล้วรอบ 3 (ดูท้ายไฟล์)**

---

## 🔎 รอบ 2 — ตัวเดาหมวดจากคำ (`src/utils/autoCategory.js` · 2026-09-23)

user ดูจอหลัง backfill แล้วแจ้งอีกรอบ: *"อื่นๆ ยังมาอันดับ 1 เช็คอัลกอริทึมให้ที
คือต้องหาคำอื่นเพื่อจับหมวดให้ได้ก่อน · อื่นๆ ต้องหาที่ลงไม่ได้จริงๆ"*

### ทำไมรอบแรกยังเหลือ
backfill รอบแรกแก้ได้เฉพาะใบที่**เปิดจากดาวน์ไทม์** (ระบบรู้ประเภทอยู่แล้ว) · ที่เหลือคือใบที่
**ช่างกดเลือก "อื่นๆ" เองในฟอร์ม** — 103 ใบ (23.9% · อันดับ 1) โดย **89 ใบเลือก "อื่นๆ" ที่ช่องอาการด้วย**
แต่ `report_note` / `root_cause` เขียนไว้ชัดแทบทุกใบ:
`"Alarm Observeline ตัดขาด"` · `"พาเลทไม่ไหล / Sensor on ค้าง"` · `"ลวดเชื่อมละลายติด Contact tip"` ·
`"Bending Alarm"` · `"ลมรั่ว"` · `"เปลี่ยน Guide Pin ตามรอบ"`
⇒ ข้อมูลมีครบ ขาดแค่ตัวจับคำ — `unclassified.js` เดิมแค่**แยกกอง** ไม่เคย*จับ*ให้

### พจนานุกรมมาจากไหน (🔴 ข้อสำคัญที่สุดของไฟล์นี้)
**ไม่มีคำศัพท์ hardcode ในโค้ดสักคำ** — คำทุกคำถอดจากข้อมูลที่โรงงานเขียนเอง 2 แหล่ง:

| แหล่ง | ได้อะไร | เงื่อนไข |
|---|---|---|
| ทะเบียน taxonomy (`mtn_problem_types` + `dr_downtime_types.mo_problem_group`) | ศัพท์ทางการ: `laser` · `conveyor` · `bending` · `reed` · `contact tip` | เชื่อทันที (คนตั้งใจเขียน) |
| **ใบที่ช่างจัดกลุ่มไว้แล้ว** + ข้อความอิสระของใบนั้น | ศัพท์หน้างานที่ไม่มีวันอยู่ในทะเบียน: `observeline` · `พาเลทไม่ไหล` · `หัวทิปตัน` · `m23` | ต้องเจอซ้ำ ≥ 2 ใบ |

⇒ โรงงานกรอกมากขึ้น / เพิ่มแถวในทะเบียน = ตัวเดาเก่งขึ้นเอง **ไม่ต้องแก้โค้ด**
· `STOP`/`FILLER` ในไฟล์ใส่ได้เฉพาะ **คำกลางของภาษา** (หัก · แตก · เปลี่ยน · the · from · error)
  **ห้ามใส่ชื่ออุปกรณ์/อาการเด็ดขาด** — นั่นคือการเดา taxonomy ซึ่งผิดกฎเหล็ก
  (ที่ต้องมีเพราะวัดแล้วคำพวกนี้ "ดูเหมือนชี้กลุ่มได้" จากความบังเอิญของกลุ่มตัวอย่าง เช่น "หัก"
   บังเอิญอยู่ในใบกลุ่มเชื่อม 11 ใบ · `the`/`system` มาจากข้อความ alarm อังกฤษของเครื่องเลเซอร์)

### วิธีตัดสิน
1. **งานตามแผนมาก่อนเสมอ** (`ตามรอบ` · `ถึงรอบ` · `ถึงระยะเปลี่ยน` · `ตามแผน`) — ไม่งั้น
   "เปลี่ยน Guide Pin **ตามรอบ**" จะถูกจับเป็นปัญหางานเชื่อมเพราะมีคำว่า guide pin
2. หาคำในพจนานุกรมที่ปรากฏในข้อความ — ไทยเทียบแบบ substring (เขียนติดกันไม่มีช่องว่าง)
   · อังกฤษเทียบแบบขอบคำ (กัน `pin` ไปโดน `spin`)
3. คำที่ชี้หลายกลุ่มไม่ชัด (< 80%) **ตัดทิ้ง ไม่ให้คะแนนใคร** — คำยาวกว่าจึงชนะเอง
   (`ลมรั่ว` อยู่รอด · `ลม` ที่อยู่ทั้ง "ระบบลม"/"สาธารณูปโภค" ถูกตัด)
4. น้ำหนัก = ความยาวคำ (สูงสุด 12) · ผู้ชนะต้องได้ ≥ 4 **และทิ้งอันดับ 2 ≥ 1.5 เท่า**
   ไม่งั้น **คืน `null` ปล่อยให้ตกถัง "อื่นๆ" ตามจริง** (ตกถังดีกว่าเดาผิด)

### ผลวัดจริง (ใบซ่อม 90 วัน · 23/09)
| | ก่อน | หลัง |
|---|---|---|
| "อื่นๆ" | **103 ใบ · 23.9% · อันดับ 1** | **36 ใบ · อันดับ 4** |
| เดาหมวดให้ได้ | — | 58 ใบ |
| ย้ายเข้า "งานตามแผน (PM)" | — | 9 ใบ (เขียนว่า "ตามรอบ" แต่เลือกกลุ่มอื่นๆ) |

ตัวอย่างที่จับได้: `Alarm Observeline ตัดขาด` ×12 → เลเซอร์ · `พาเลทไม่ไหล/Conveyor` ×10 → ราง/ลำเลียง ·
`Contact tip / ลวดเชื่อมละลายติด` ×13 → เชื่อม-ของสิ้นเปลือง · `Bending Alarm` ×4 → เครื่องขึ้นรูป

### 🔴 กฎที่ต้องรักษา
- **ห้ามเขียนผลเดากลับฐานข้อมูล** — เป็นการอ่านของจอเท่านั้น (`fillCategories` คืนแถวใหม่ ไม่แก้ของเดิม)
- **จอต้องบอกว่าเดากี่ใบ และเดาจากคำไหน** — แถบใต้หัวพาเรโต + มิติเจาะลึก `🔎 ที่มาของหมวด`
  (ค่าเป็น `👷 ช่างเลือกเอง` หรือ `🔎 ระบบเดาจากคำ: …`) · กฎความซื่อสัตย์ของจอ
- **เรียนจากใบ MO เท่านั้น** — แถวดาวน์ไทม์ที่ยังไม่มีใบใช้ข้อความดิบเป็นชื่อกลุ่ม เอามาสอนจะได้กลุ่มปลอม
- ยังไม่ขยายไปจอของเสีย/4M/downtime (util เป็น generic รับ `labelOf`/`textOf` ไว้แล้ว) —
  3 จอนั้น audit แล้วถังขยะไม่ติด top-3 จึงยังไม่เร่ง
- **งานค้าง:** `mtn_problem_types` ทีม `jig_maintenance` 13 แถวยังไม่มี `group_name`
  (พาเลท/สต็อปเปอร์/โลเคเตอร์ ฯลฯ) ⇒ คำพวกนี้สอนตัวเดาไม่ได้เลย — ต้องให้คนของโรงงานตั้งชื่อกลุ่ม


---

## 🗑️ รอบ 3 — ถังขยะของพาเรโต **Downtime** (`src/utils/downtimeCategory.js` · 2026-09-23)

คำสั่ง user ต่อจากรอบใบซ่อม MO: *"ทำข้อ 2 ต่อเลย downtime"*

### วัดจริง 90 วัน ก่อนแก้
| ถัง | ใบ | นาที | อันดับในพาเรโต**นอกแผน** |
|---|---|---|---|
| `อื่นๆ (ในแผน)` | 168 | 7,332 | (ตามแผน ไม่นับเป็น loss) |
| `เครื่องแจ้งเตือน Alarm (ไม่ระบุสาเหตุ)` | 139 | 3,613 | **อันดับ 8** (4.6%) |
| `อื่นๆ (นอกแผน)` | 131 | 4,488 | **อันดับ 5** (5.7%) |
| รวม | **438** | **15,433** | 2 ถังนอกแผนรวมกัน 10.3% = **อันดับ 2 ถ้ายุบเป็นแท่งเดียว** |

### 🔴 ทางแก้ที่เลือก — แตกตาม "เครื่อง" ไม่ใช่เดาจากคำ
**401 จาก 438 ใบ (92%) กรอก `machine_no` ไว้แล้ว** (ถัง Alarm 135/139 = 97%)
⇒ ถังขยะไม่ได้ "ไม่รู้อะไรเลย" — มันรู้ว่าเครื่องไหนหยุด **แต่จอโยนค่านั้นทิ้งเอง**
เคสที่โผล่ทันทีที่แตก: **HDF-02 = 66 ใบ / 2,619 นาที** — มากกว่าแท่งอันดับ 3 ของพาเรโตนอกแผน
ทั้งพาเรโต และวันนี้ยังไม่มีจอไหนเห็น เพราะกระจายอยู่ใน 2 ถัง

`dtBucketName(d)` → ประเภทปกติได้ชื่อเดิมเป๊ะ · เฉพาะถังขยะกลายเป็น `"<เลขเครื่อง> · <ชื่อประเภท>"`
· **เลขเครื่องมาก่อนเสมอ** — บอร์ด TV ตัดป้ายแกน X ที่ ~8 ตัวอักษร ถ้าเอาชื่อประเภทขึ้นก่อน
ทุกแท่งจะถูกตัดเหลือ `"อื่นๆ (นอ…"` เหมือนกันหมด = แตกแล้วก็ยังอ่านไม่ออก

**จอที่แก้แล้ว (มีด่าน `regressionGuards` กฎ `downtime-bucket-not-raw-type-name`):**
`/oee-analytics` (พาเรโตสาเหตุ + เจาะรายใบ + มูลค่าเป็นบาท) · `/dept-dashboard` ·
`/obeya` SQDCM · `/line-oee-board` · `/morning-meeting` · Daily Report · สไลด์ Monthly Review
· โหมด **ปี** ของ SQDCM แตกฝั่ง SQL ด้วย (migration `20260923_obeya_year_rollup_dt_machine_dr.sql`
· **apply แล้ว 23/09** · ตรวจกลับ: sessions/defects/orders hash เท่าเดิม · นาทีรวม 239,896 เท่าเดิม ·
แถว downtime 828→900 = +72 เฉพาะถังขยะที่ถูกแตก) — ไม่งั้นจอเดียวกันตอบคนละเลขระหว่างเดือนกับปี

### ⚠️ ทำไม *ไม่* เอาตัวเดาจากคำ (`autoCategory.js`) มาใช้ที่นี่ — ลองแล้ว วัดแล้ว ตก
| แบบ | เดาได้ | คุณภาพ |
|---|---|---|
| ทะเบียน + เรียนจากใบที่จัดประเภทแล้ว 180 วัน | 223/438 ใบ | **~ครึ่งหนึ่งผิด** |
| เฉพาะคำจากทะเบียน | 96/438 ใบ | ดีขึ้น แต่ยังมีผิดหนักๆ |

ตัวอย่างที่ผิด: `"น้ำมันรั่ว"` → ปืนยิงรีเวท · `"พนักงานไม่เพียงพอ"` → อบรม · `"กล้อง"` → ระบบ PLC ·
`"line"` → **Improved line (990 นาที!)** เพราะทะเบียนมีประเภทชื่อนั้นจริง
และคำที่มีค่าที่สุดกลับตายเพราะ **ทะเบียนเองมีป้ายซ้ำซ้อน**: `conveyor` อยู่ทั้ง
"ชิ้นงานเต็มราง Conveyor" และ "ราง Conveyor มีปํญหา" · `laser` อยู่ทั้ง "เครื่อง Laser มีปัญหา"
และ "รอชิ้นงาน (HDF, Laser)" ⇒ ตกกฎ `DOMINANCE` (คำกำกวม = ไม่ให้คะแนน) ซึ่งถูกต้องแล้ว

⇒ ตัดสินตาม CLAUDE.md: **"ก้ำกึ่ง → null · ตกถังดีกว่าเดาผิด"** — เดาผิดที่นี่คือโยนนาที
ไปใส่ *เครื่องผิดตัว* แล้วช่างไปไล่ผิดจุด ซึ่งแย่กว่าไม่เดา
🔴 **อย่ารื้อมาลองใหม่โดยไม่แก้ที่ทะเบียนก่อน** (ดูงานค้างข้างล่าง) — ตัวเดาเก่งขึ้นเองเมื่อทะเบียนสะอาด

### 📌 งานค้างฝั่งทะเบียน — ต้องให้คนของโรงงานตัดสิน (ห้าม AI เดา taxonomy)
1. **ป้ายซ้ำซ้อน ควรยุบ:** `รอภาชนะ` / `รอภาชนะ/Rack` / `รอพาเลท / ภาชนะเต็มราง (ในแผน)` /
   `ชิ้นงานเต็มราง Conveyor` · `อื่นๆ (นอกแผน)` กับ `อื่นๆ (นอกแผน-Forming)` ·
   `แก้ไขปัญหาคุณภาพ` / `แก้ไขปัญหาคุณภาพ (Forming)` / `แก้ไขคุณภาพระหว่างผลิต`
2. **ประเภทที่ยังไม่มีในทะเบียน** (เห็นจากข้อความในถังขยะ): เติมไนโตรเจน · น้ำมัน/ไฮดรอลิกรั่ว ·
   กำลังคนไม่พอ/ไปช่วยไลน์อื่น · ลมตก — ตอนนี้ช่างเลือกอะไรไม่ได้นอกจาก "อื่นๆ"
3. **เลขเครื่องเขียน 2 แบบ** ในคอลัมน์เดียวกัน: `LS-04` กับ `เลเซอร์04` (625 นาทีอยู่ฝั่งชื่อไทย)
   ⇒ แตกเป็นคนละแท่งทั้งที่เป็นเครื่องเดียวกัน · ต้นเหตุ = ช่องนี้ยังพิมพ์เองได้
   (UI-CONVENTIONS §5.1.2 บังคับให้ใช้ `MachineSelect`) — งานแยกอีกใบ
4. `อื่นๆ (นอกแผน)` 15 ใบ / `Alarm ไม่ระบุสาเหตุ` 4 ใบ **ไม่กรอกเครื่องเลย** = ชี้เป้าไม่ได้จริงๆ
   จอบอกจำนวนนี้ไว้แล้ว (บรรทัดใต้กราฟใน `/line-oee-board`) — ต้องไปแก้ที่การกรอก ไม่ใช่ที่จอ


---

## 🇹🇭 รอบ 4 — ชั้นภาษา + ชั้นสถิติ (`thaiText.js` · `termStats.js` · 2026-09-24)

คำสั่ง user: *"หาหลักการหรือทฤษฎีจากภายนอกมาช่วยสนับสนุนการจับกลุ่มคำ ให้เก่งกว่านี้ได้มั้ย
เรื่องภาษา แก้เลย ทำยังไงให้รวมได้"*

### โจทย์ที่รอบ 3 แพ้ — ไม่ใช่ปัญหาอัลกอริทึม แต่เป็นปัญหา **ภาษา**
ช่างพิมพ์ศัพท์เครื่องจักรเป็น **คำอังกฤษที่เขียนด้วยอักษรไทย** และเขียนติดกันไม่มีช่องว่าง

| ที่ช่างพิมพ์ | ที่จริงหมายถึง |
|---|---|
| `คอนเวเย่ออาราม` | conveyor alarm |
| `เบนดิ่ง06อารามแกนx` | bending 06 alarm แกน X |
| `เลเซอร์05อารามtc` | laser 05 alarm TC |
| `สายไฮดรอลิคเเตก` | สายไฮดรอลิก**แ**ตก (พิมพ์ เ+เ แทน แ) |
| `คอนเวเย่า` · `คออนเวเย่อ` · `conyeyor` · `bemding` | พิมพ์ผิดของคำเดิม |

ตัวจับคำเดิมเทียบ **ตัวอักษร** ⇒ "คอนเวเย่ออาราม" กับ "conveyor alarm" เป็นคนละคำสนิท

### ทฤษฎี/มาตรฐานจากภายนอกที่เอามาใช้ (ไม่เพิ่ม dependency สักตัว)
| # | ทฤษฎี | ใช้แก้อะไร |
|---|---|---|
| 1 | **UAX #29 + ICU dictionary-based Thai word break** ผ่าน `Intl.Segmenter('th')` | ไทยไม่มีช่องว่าง — `.split(' ')` ได้ 1 token เสมอ · Chromium 87+ (เพดานจอ TV = Cr 94 ⇒ ใช้ได้) |
| 2 | **Phonetic matching (Soundex 1918 / Metaphone 1990)** ดัดแปลงเป็น **ข้ามสคริปต์** | ย่อทั้งไทยและอังกฤษลงโครงพยัญชนะเดียวกัน ⇒ ทับศัพท์ชนคำอังกฤษได้ |
| 3 | **Approximate string matching (Levenshtein 1965 / Damerau 1964)** + normalized ratio | คำพิมพ์ผิด/สลับตัว |
| 4 | **SymSpell (Garbe 2012)** — ดัชนี "ลบ 1 ตัว" | หาคำใกล้เคียงใน O(ความยาวคำ) แทนวนเทียบทั้งพจนานุกรมทุกแถว |
| 5 | **Log-odds ratio with informative Dirichlet prior** (Monroe, Colaresi & Quinn 2008) | แทนกติกา "คำนี้อยู่กลุ่มเดียว 80% ⇒ เชื่อ" ที่ไม่สนใจว่าเห็นมากี่ใบ |
| 6 | **กฎอักขรวิธีไทย** (ห นำ · ทัณฑฆาต · เเ→แ · ๆ) | ไม่ใช่การเดา — เป็นกฎของภาษา |

ตัวอย่างคีย์เสียงที่ได้: `เบนดิ่ง`=`bending`→**bndn** · `ไฮดรอลิค`=`hydraulic`→**hdrk** ·
`เซ็นเซอร์`=`sensor`=`senser`→**snsr** · `สต็อปเปอร์`=`stopper`→**stpr** · `เมนเดล`=`mandrel`→**mndr**

### 🔴 3 กฎที่ได้จากการวัด (พังมาก่อนทั้งคู่ ห้ามถอด)
1. **เทียบเสียงเฉพาะ "ข้ามสคริปต์"** — ในสคริปต์เดียวกันต้องเทียบตัวอักษร
   คีย์เสียงตัดสระทิ้ง ซึ่งไทยพึ่งสระหนักมาก ⇒ ปล่อยให้เทียบเสียงไทย↔ไทย แล้วพังทันที:
   `คอนเวเย่ออาราม` → *Meeting ก่อนเริ่มงาน* · `แก้ไขงานยุบ` → *รอ Kanban* · `ช่างใหม่แควหัว` → *ราง Conveyor*
2. **ข้ามสคริปต์ต้องคีย์ตรงเป๊ะ ห้ามเผื่อแม้แต่ตัวเดียว** — โครงพยัญชนะสั้น (3-5 ตัว)
   เผื่อ 1 ตัว = `เครื่อง`(krn)↔`clearance`(krns) · `คอนโทน`(kntn)↔`kanban`(knbn) · `สต็อปเป่อ`↔`strip`
   ราคาที่จ่าย: เสีย `คอนเวเย่อ`(knw)↔`conveyor`(knwr) ไป — ยอมตามกฎ "ตกถังดีกว่าเดาผิด"
3. **คำต้นฉบับต้องยาว ≥ 4 ตัวอักษร และคีย์ต้องมีพยัญชนะ ≥ 3 ตัว**
   ไม่งั้น `ลม`(air) ได้คีย์ **rm** เท่ากับ `อาราม`(alarm) เป๊ะ ⇒ "ลมตก" กลายเป็น alarm ·
   `nut/not` · `tip/top` · `ราง/รอง` · `pin/pen` ชนกันหมด

เพิ่มเติม: **หลักฐานจาก "เสียง" มีน้ำหนัก 0.6 ของหลักฐานจาก "ตัวอักษร"** (มันคือการเดาว่าคำน่าจะ
สะกดแบบนั้น ไม่ใช่การเห็นคำนั้นจริง) ⇒ คำสั้นที่จับได้ด้วยเสียงอย่างเดียวตัดสินลำพังไม่ได้
· **ป้ายในทะเบียนตัดคำด้วยช่องว่างเท่านั้น** ห้ามเอา ICU ไปซอยอีก (ป้ายเขียนมาดีแล้ว — ซอยแล้วได้
"เศษคำ" อย่าง `เปอร์` `อาด` `ทำความสะ` ที่ไปจับข้อความมั่ว) · ICU + n-gram ใช้กับ**ข้อความอิสระ**เท่านั้น

### ชั้นสถิติ — เลิกใช้ "ชนะ 80%"
| คำ | เจอ | อยู่กลุ่มเดียว | กติกาเดิม | z (ใหม่) | ผล |
|---|---|---|---|---|---|
| conveyor | 125 | 88% | ✅ | **16.5** | ✅ ถูก |
| งานเต็มราง | 210 | 89% | ✅ | **21.3** | ✅ ถูก |
| senser | 13 | 85% | ✅ | **5.5** | ✅ ถูก |
| line | 14 | 100% | ✅ | **1.2** | ❌ ตัดออก (เคยกิน 990 นาทีเข้า "Improved line") |
| กล้อง / เหล็ก / ข้าง | 2-4 | 100% | ✅ | **0.5-0.7** | ❌ ตัดออก |

🔴 **ต้องมีทั้ง z ≥ 1.96 และพื้นขั้นต่ำ 3 ใบ** — z เทียบกับคลังที่มีอยู่ ถ้าคลังเล็ก (กรองแคบ/
เพิ่งเริ่มใช้) คำที่เจอ 2 ครั้งอาจได้ z ผ่านได้เพราะคลังเล็ก ไม่ใช่เพราะมันชี้กลุ่มจริง (มีเทสล็อกไว้)

### ผลวัดจริง — ถังขยะ downtime **นอกแผน** 270 ใบ / 8,101 นาที (90 วัน)
| | ใบที่จับกลุ่มได้ | นาที |
|---|---|---|
| ตัวอักษรอย่างเดียว (ก่อน) | 32 | 715 |
| **+ ชั้นภาษา (หลัง)** | **63** | **1,909** |

ที่เพิ่มมาเกือบทั้งหมดคือคำทับศัพท์: `เบนดิ่ง`→เครื่องBending **22 ใบ / 1,065 นาที** ·
`เลเซอร์`→เลเซอร์ 27 ใบ · `ไฮดรอลิค`→ระบบ Hydraulic · `โรบอท`→Robot · `สแค็ป`→เปลี่ยนถัง Srcap
· ใบที่เดาไม่ได้ **ยังถูกแตกตามเครื่องเหมือนเดิม** (รอบ 3) ไม่หายไปไหน

### เปิดใช้ที่ไหน
ทุกจอที่เรียก `dtBucketName(d, index)` — index สร้างด้วย `buildDtIndex(rows)` จากชื่อประเภท
ที่อยู่ในชุดข้อมูลนั้นเอง (**ไม่ยิงคิวรีเพิ่ม**) · `/oee-analytics` · `/dept-dashboard` · `/obeya` ·
`/line-oee-board` (โชว์ "🔎 เดาจากคำ n ใบ") · `/morning-meeting` · Daily Report · Monthly Review
· ฝั่ง MO (`/mtn-analysis`) ได้ชั้นภาษานี้ไปด้วยอัตโนมัติ เพราะใช้ `autoCategory` ตัวเดียวกัน

### ⚠️ ข้อจำกัดที่รู้ตัว (อย่าเพิ่งไปแก้ด้วยการเติมศัพท์)
- `อาราม`↔`alarm` match แต่ไม่มีผล เพราะ `alarm` เป็นคำโหล (อยู่ใน STOP) — ถูกต้องแล้ว
- คำที่ ICU ตัดเป็นคำเดียวและเสียงไปชนคำอังกฤษ (`ไม่ทัน`↔`meeting`) ต้องใส่ STOP —
  ใส่ได้เฉพาะ**วลีกลางของภาษา** ห้ามใส่ชื่ออุปกรณ์ (มีด่าน `no-factory-vocabulary-in-language-layer`)
- `feed` หายไปจากผล เพราะอยู่ทั้ง "Feed nut ติด…" (ทะเบียน) และ "Feeder มีปัญหา" (การใช้จริง)
  ⇒ กำกวมจริง ตัดทิ้งถูกต้อง · จะได้คืนเมื่อทะเบียน/การกรอกชัดขึ้น

---

## 🔧 รอบ 4.1 — ปิดช่องที่เหลือ 2 ข้อ (2026-09-24 · user: "แก้เลย")

### ① `คอนเวเย่อ` ↔ `conveyor` — กฎ **r ท้ายคำเป็นตัวเลือก**
ไทยเป็นภาษา **non-rhotic** ในคำยืม: เสียง /r/ ท้ายพยางค์ของอังกฤษไม่ออกเสียง
เขียน **ร์** (ทัณฑฆาต) หรือ **ตัดทิ้งเลย** ก็ได้ — และช่างเขียนทั้ง 2 แบบจริง
`เลเซอร์`(เก็บ ร์)↔`laser` · `คอนเวเย่อ`(ตัดทิ้ง)↔`conveyor`
⇒ ยอมให้คีย์ต่างกันได้ **เฉพาะ r ตัวสุดท้ายตัวเดียว** (`ka === kb+'r'`)
🔴 **ไม่ใช่การ "เผื่อ 1 ตัว"** ซึ่งเคยทำแล้วพัง (เครื่อง krn ↔ clearance krns) — จำกัดตำแหน่งชัด

### ② คำที่ **กำกวมในทะเบียน** ต้องตัดสินด้วย **การใช้งานจริง**
> ⚠️ **แก้ความเข้าใจผิดของรอบก่อน:** เคยเขียนไว้ว่าทางแก้คือ "ยุบป้ายซ้ำในทะเบียน" — **ผิด**
> `ราง Conveyor มีปํญหา` (รางเสีย = นอกแผน) กับ `ชิ้นงานเต็มราง Conveyor` (งานเต็มราง = รอ/ในแผน)
> เป็น**คนละเรื่องกันจริงๆ** ห้ามยุบ · มันแค่ใช้คำว่า conveyor ร่วมกัน

ทางแก้ที่ถูก: `buildDtIndex()` ส่ง **ใบที่ช่างเลือกประเภทไว้แล้ว** เข้าไปเรียนด้วย (ไม่ใช่ทะเบียนอย่างเดียว)
วัดจริง 180 วัน: `conveyor` 110 จาก 125 ครั้งอยู่ "ราง Conveyor มีปํญหา" ⇒ **z = 22.1** ผ่านสบาย
· ไม่ยิงคิวรีเพิ่ม — เรียนจากแถวที่จอโหลดมาอยู่แล้ว · ผ่าน log-odds + พื้นขั้นต่ำ + DOMINANCE ครบ

**คำโหลที่ต้องเข้า STOP เพิ่ม** (วัดจากคลังจริง 16,776 token — เป็นหน่วย/คำบอกปริมาณ ไม่ใช่ชื่ออุปกรณ์):
`นาที` (622 ครั้ง) · `หน้า` (197) · `รอบ` (107 — ผ่านทั้ง z และ DOMINANCE ไปลงที่ *Set up Machine*
ทั้งที่แปลว่า cycle เฉยๆ) · `ชม` · `ชั่วโมง` · `ครั้ง` · `ประมาณ`

### ผลรวมทั้ง 4 รอบ — ถังขยะ downtime นอกแผน 270 ใบ / 8,101 นาที
| ขั้น | ใบ | นาที |
|---|---|---|
| ตัวอักษรอย่างเดียว (ก่อนมีชั้นภาษา) | 32 | 715 |
| + ชั้นภาษา (ทะเบียนอย่างเดียว) | 63 | 1,909 |
| **+ เรียนจากใบที่จัดประเภทแล้ว + กฎ r ท้ายคำ** | **82** | **3,115** |

**×2.6 ใบ · ×4.4 นาที** จากจุดเริ่ม · แท่งที่โผล่ใหม่: **ราง Conveyor 25 ใบ / 1,233 นาที**
(รวม `คอนเวเย่ออาราม` · `คอนเวเย่าอาราม` · `คออนเวเย่ออาราม` · `conveyor alarm` · `conyeyor alarm`
= คำเดียวกันที่สะกด 5 แบบ) · `แคะสต็อปเป่อ`→stopper · คำอ่อน (`line`/`กล้อง`/`ข้าง`/`marking`)
ถูกตัดครบ ไม่โผล่ในผลเลย
