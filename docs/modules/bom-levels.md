# 🌳 ชั้น BOM ที่แก้ได้ + ผูกขั้นตอน (PFC/OP) — `/products` แท็บ BOM (2026-09-16)

> คำสั่ง user 2026-09-16:
> *"เรื่อง bom level ตอนนี้ เหมือนปรับแก้อะไรไม่ได้ เหมือน SAP เพราะ SAP จะมีเลเวลควบคุม หรือเราตกเรื่องนี้ไป"*
> *"ถ้าทำเลเวล bom ได้แบบ pfc มันจะแก้หลายจุดเลย แถมเหนือกว่า sap ตรงที่เรามี OP ด้วย"*

## สิ่งที่ "ตก" ไปจริง — ไม่ใช่การแสดงชั้น แต่คือการ **ควบคุม** ชั้น

ตัวกางโครงแบบ SAP CS12 (`explodeBom` · `.1`/`..2`/`...3` · จับ cycle · จับแถวนับซ้ำ) มีมาตั้งแต่ 2026-09-02
**แต่สร้างชั้นจากหน้าจอไม่ได้เลย** เพราะทุกหน้าหาตัวแม่ด้วย `matOf[row.product_id]`:

> `bom_items.product_id` → `dr_products.id` ⇒ **ของที่จะ "มีลูก" ได้ ต้องเป็นแถวใน `dr_products`**
> อยู่แค่ `parts_master` = **ใบไม้ตลอดกาล**

**วัดจริง 16/09 (DR):**
| | |
|---|---|
| BOM ทั้งหมด | 506 แถว / 98 สินค้า |
| แถวที่ลูกมี BOM ของตัวเอง (= มีชั้นจริง) | **57 แถว (11%)** |
| ลูกที่เป็น `dr_products` | 26 ตัว |
| เคส `10100817` | ลูก 5 ตัว เป็น product แค่ `20067039` · อีก 4 (รวมวัตถุดิบ `50028183`) ต่อชั้นไม่ได้ |

⇒ ~90% ของโครงตรึงชั้นเดียว **เพราะระบบไม่ให้สร้างชั้น ไม่ใช่เพราะของจริงมีชั้นเดียว**
(จอถึงเตือน "วัตถุดิบ 5xx ปนอยู่กับชิ้นส่วน 2xx" ได้ แต่คนแก้ไม่ได้ — `checkBomFlow` ชี้ได้อย่างเดียว)

## โมเดลใหม่ (migration `20260916_bom_level_parent_mat.sql` — **apply แล้ว 16/09 · ตรวจกลับ 0/0/506**)

| คอลัมน์ | ความหมาย |
|---|---|
| **`parent_mat`** (text · null) | ตัวแม่ของบรรทัดนี้เป็น **mat_no** — ใครก็เป็นแม่ได้ ไม่ต้องเป็น `dr_products` · **null = พฤติกรรมเดิมเป๊ะ** |
| **`op_no`** (text · null) | ขั้นตอน (เลข OP ตาม PFC) ที่ชิ้นนี้ถูกใส่เข้าไป · ว่าง = ยังไม่ระบุ **ห้ามเดา** |

- **`product_id` ยังต้องมีเสมอ** = "บรรทัดนี้อยู่ในใบ BOM ของ FG ตัวไหน" (root ของต้นไม้)
  ⇒ ทุกจอที่ filter ด้วย `product_id` วันนี้ **ยังได้ผลเท่าเดิมทั้งหมด ไม่ต้องแก้พร้อมกัน**
- แถวเดิม 506 แถว `parent_mat` เป็น null ทั้งหมด ⇒ **ไม่มีจอไหนเปลี่ยนพฤติกรรมจาก migration นี้**

### 🔴 กฎเหล็ก — ต้นไม้ BOM ต้องผ่าน `buildBomIndex` เท่านั้น

```js
import { buildBomIndex } from '../utils/bomTree';
const ix = buildBomIndex(bomRows, matOfProductId);   // parent_mat ชนะ product_id
ix.bomOf('10100817')   // ลูกชั้นถัดไป
ix.parentOf(row)       // ตัวแม่ของบรรทัด
ix.orphans             // แถวที่หาตัวแม่ไม่ได้ — ⚠️ ต้องโชว์ ห้ามกลืน (บรรทัดหายจากต้นไม้เงียบๆ)
```

- **ห้ามเขียน `matOf[b.product_id]` เองในหน้าอีก** — มี **ด่านสแกนทั้งรีโป** แล้ว
  (`regressionGuards.test.mjs` กฎ `bom-tree-via-buildBomIndex` · ตกด่าน = build ล่ม)
  เหตุผล: เขียนเองตั้งแต่มี `parent_mat` แล้ว = **มองข้ามชั้นที่คนตั้งไว้เงียบๆ** คนละจอเห็นโครงคนละแบบ
- ย้ายชั้นผ่าน **`moveBomLine(row, toMat, bomOf)`** — กันย้ายใต้ตัวเอง/ใต้ลูกหลานตัวเอง (วนลูป) · pure + มีเทส

### หน้าจอ

- `/products` แท็บ BOM → คอลัมน์ **"ชั้น / ขั้นตอน"**: dropdown ย้ายบรรทัดไปเป็นลูกของบรรทัดอื่นในใบเดียวกัน
  (สิทธิ์ `products:edit` · ไม่มีสิทธิ์ = เห็นข้อความ "ชั้น 1" / "↳ ใต้ …" อย่างเดียว) + ชิป `OP xxx`
- โมดัลแก้ไขบรรทัด → ช่อง **"ขั้นตอนที่ใส่เข้าไป (เลข OP ตาม PFC)"**
- **ตัวเลือกตัวแม่ = บรรทัดอื่น "ในใบเดียวกัน" เท่านั้น** (ข้ามใบ = คนละ BOM ห้ามย้าย)
- ⚠️ RLS ปฏิเสธ UPDATE = 0 แถว ไม่มี error ⇒ `doMoveLine` ใช้ `.select('id')` แล้วนับแถว (กฎเหล็กข้อ 2)

## ทำไม "เหนือ SAP" — เพราะเรามีชั้น OP

SAP แยก **BOM** (ต้นไม้วัสดุ) กับ **Routing** (ขั้นตอน) การผูก component→operation เป็น optional
ที่แทบไม่มีใคร maintain · ของเรา **BOM บรรทัดเดียวตอบทั้ง 2 คำถาม**: ประกอบจากอะไร + ใส่ตอนขั้นไหน

⇒ จุดที่ได้ประโยชน์ทันทีเมื่อกรอก `op_no` ครบ:
ของเสียหลุดขั้นไหนก็ตัดถูก (`scrapExplode`) · WIP ต่อขั้น (`wipChain`) · part-call ส่งของถูกขั้น/ถูกไลน์
(`bom_items.source_line` ว่างทั้ง 408 แถว = สโตร์ยังเดาเอง) · ต้นทุนต่อขั้นในอนาคต

## สถานะงานถัดไป — user เคาะแล้ว 16/09

1. ~~**ผูก PFC ↔ MAT**~~ — ❌ **user สั่งไม่ต้องทำ** (*"ไม่ต้องผูก แค่แชร์ไอเดียเฉยๆ"*)
   `pe_doc_sets.mat_no` ว่างทั้ง 3 ใบเหมือนเดิม · **ห้าม session ถัดไปหยิบไปทำเองโดยไม่ถาม**
2. ~~**sync `pe_processes.child_parts` → `bom_items`**~~ — พับตาม (1) · **คนกรอก `op_no`/ชั้นเอง**
   (*"เดี๋ยวให้คนไล่ปรับ"*) ⇒ ของที่ระบบต้องมีคือ**เครื่องมือให้คนปรับ** ซึ่งทำครบแล้ว
   (คอลัมน์ "ชั้น / ขั้นตอน" + ช่อง OP ในโมดัล + worklist OP ที่ยังไม่ผูก component)
   · **ห้าม seed / เดาสูตรแทนคน** — ผิดแล้วความต้องการวัตถุดิบเพี้ยนทั้งโรงงาน
3. **แถวแบนที่นับซ้ำ** — user ยืนยันว่าคือแถวพวกนี้ *"เดี๋ยวน่าจะต้องเอาออกซักจุดนึง"* → **ยังไม่ลบ รอสั่ง**

### 📏 แถวที่นับซ้ำ — วัดจริง 16/09 (DR · ยังไม่แตะแถวไหน)

"นับซ้ำ" = แถวชั้น 1 ที่ของชิ้นเดียวกันไปโผล่เป็น**หลาน**ของใบเดียวกันด้วย ⇒ กางต้นไม้แล้วได้ยอด 2 เท่า

| | |
|---|---|
| คู่ที่ซ้ำ (fg × หลาน) | **165 คู่** |
| แถวชั้น 1 ที่เป็นตัวซ้ำ | **141 แถว** จาก 506 แถว (28%) |
| FG ที่กระทบ | **24 ใบ** |

หนักสุด: `10086024` · `10100814` · `10101158` · `10101159` (SUPT ASY RAD ทั้ง 4 ลูกค้า) = **19 แถวซ้ำ
จาก ~21 แถว/ใบ** = เกือบทั้งใบ · รองมา `10100381` (13) · `20067545` (7) · `10102017` (6)
ที่เหลือ 1-4 แถว/ใบ (ตระกูล REINF FRT FNDR / RAD SUPT LWR)

### 🐞 บั๊กข้อมูลแยกออกมา 1 แถว — พาร์ทเป็นลูกของตัวเอง

`bom_items.id = b7f13062-75a5-467c-8f35-aeb4ccdc9a46` : `20067545` → ลูกคือ `20067545` เอง
(REINF FLR TNL LWR ก่อนชุบ · qty 1 PC · คีย์โดย อัจฉรา โสภาบุญ 14/08)

- นี่**ไม่ใช่เรื่องแบนจาก SAP** แต่เป็นวงวนที่ของจริงเป็นไปไม่ได้ (ชิ้นหนึ่งประกอบจากตัวมันเอง)
- แถวเดียวนี้ทำให้เกิดคู่ซ้ำ **32 คู่** (165 → 133 ถ้าเอาออก) และ `20067545` หลุดออกจาก 24 ใบทันที
- `explodeBom` มีตัวจับ cycle อยู่แล้ว ⇒ จอไม่ค้าง แต่ยอดยังเพี้ยน
- ⚠️ **ยังไม่ลบ** — รอ user/PE สั่ง (กฎเดียวกับข้อ 3)

### SQL ที่เตรียมไว้ (ถอยได้ · **ยังไม่รัน** — รันเมื่อ user สั่งเท่านั้น)

รันที่ **DR — ชื่อในจอ "Product DB"** (`eyhclzkifitbhbljgoav`) · สำรองก่อนเสมอ ไม่ลบจริง ใช้ปิด `is_active`

```sql
-- 0) สำรองสภาพปัจจุบันไว้ก่อน (ตารางนี้คือทางถอย)
create table if not exists public.bom_items_backup_20260916 as
  select * from public.bom_items where is_active;

-- 1) แถวที่พาร์ทเป็นลูกของตัวเอง (บั๊กแน่ชัด 1 แถว)
update public.bom_items set is_active = false
 where id = 'b7f13062-75a5-467c-8f35-aeb4ccdc9a46';

-- 2) แถวชั้น 1 ที่ซ้ำกับหลาน (141 แถว) — **ต้องให้ PE/Planning เคาะก่อน**
--    ปิดทีละใบได้โดยใส่ and p.mat_no = '<FG>' เพื่อทยอยดูผล
-- update public.bom_items b set is_active = false
--  where b.is_active and exists (
--    select 1 from dr_products p
--      join bom_items b1 on b1.product_id = p.id and b1.is_active
--      join dr_products c on c.mat_no = b1.mat_no
--      join bom_items b2 on b2.product_id = c.id and b2.is_active
--     where p.id = b.product_id and b2.mat_no = b.mat_no and b2.id <> b.id);
```

ถอย: `update bom_items b set is_active = true from bom_items_backup_20260916 k where k.id = b.id;`

## 🧹 ปุ่มลบ "แถวนับซ้ำ" ในจอต้นไม้ (2026-09-16)

> user: *"ตัวที่นับซ้ำ ที่ถูก parent ไว้แล้ว แบบสีแดงนี้ ลบให้ได้มั้ย ใช้ตัวเดียวได้ไม่งง เราไม่รู้จะลบยังไง"*

เดิมจอต้นไม้ **อ่านอย่างเดียว** ⇒ เห็นแถวแดง "นับซ้ำ" แต่ต้องไปไล่หาแถวเดียวกันในตารางล่างเอง

- `BomTreeView` รับ prop **`onDeleteDupes(rows)`** (ไม่ส่ง = อ่านอย่างเดียวเหมือนเดิมเป๊ะ)
  → ปุ่ม **🧹 ลบแถวชั้น 1 ที่นับซ้ำทั้งหมด (N)** ในแถบแดง + **🗑 ลบแถวนี้** รายแถว
- **ลบได้เฉพาะแถวที่ `isDupeRow` เท่านั้น** — แถวอื่นไม่มีปุ่ม (ลบผิด = ความต้องการวัตถุดิบหาย)
- `explodeBom` ส่ง **`id`** ของบรรทัด `bom_items` ออกมาแล้ว · **ไม่มี id = ซ่อนปุ่ม ห้ามเดา**
  ⇒ ตัวเรียก `bomOf` ต้อง select `id` มาด้วย (ProductMaster เพิ่มใน `BOM_FULL`/`BOM_SLIM` แล้ว)
- ปุ่มนี้ **ปิด `is_active` ไม่ลบจริง** (ต่างจากปุ่ม 🗑 รายแถวในตารางที่ `.delete()` มาแต่เดิม)
  เพราะเป็นการลบทีละหลายแถว — พลาดแล้วต้องกู้ได้ · ถอย: `update bom_items set is_active = true where id in (…)`
- สิทธิ์ `products:delete` · นับแถวจาก `.select('id')` (RLS ปฏิเสธ UPDATE = 0 แถว ไม่มี error)

### เคสต้นแบบ — งานปั๊มคู่ RH/LH กินคอยล์คนละครึ่งแผ่น

`10100381` คอยล์ `50027085` ถูกเขียนไว้ **3 ที่**:

| ที่อยู่ | qty | ความหมาย |
|---|---|---|
| ชั้น 1 ของ FG | 0.642 KG | **แถวแบนจาก SAP** = ทั้งแผ่น |
| ใต้ `20058490` (RH) | 0.3205 KG | ครึ่งแผ่น |
| ใต้ `20058491` (LH) | 0.3205 KG | ครึ่งแผ่น |

แผ่นเดียวปั๊มได้ทั้งซ้าย-ขวา (= งานคู่ `pair_mat_no` ที่ตั้งไว้แล้วทั้ง 2 ทาง) จึงหารครึ่ง — **ครึ่งแผ่นคือความจริง**
กางทั้งสายแล้วได้ **1.283 KG** ทั้งที่ของจริงกิน ~0.642 ⇒ ลบแถวชั้น 1 ทิ้ง เหลือเส้นทางเดียว จบ

⚠️ **0.3205 × 2 = 0.641 ไม่ใช่ 0.642** (0.642 ÷ 2 = 0.321 พอดี) — ฝั่ง PE คีย์ `0.321` ไว้แล้วใน
`10105769/10105770` ต่างกัน 0.5 กรัม/ชิ้น (0.16%) · **ไม่แก้ให้เอง** เทสตรึงเลขจริงไว้ที่ 0.641
ถ้าจะให้ตรงกันทั้งฐานต้องให้ PE เคาะว่าใช้ 0.3205 หรือ 0.321

⚠️ **ยังไม่มีโมเดล "co-product" จริง** — BOM ปัจจุบันตอบได้แค่ "ชิ้นนี้กินครึ่งแผ่น" ยังตอบไม่ได้ว่า
"สั่งผลิตเฉพาะ RH แล้วได้ LH ติดมาเป็นของแถม" (ของจริงกินทั้งแผ่นเสมอ) · ฝั่งยอดผลิตมี `pair_mat_no`
+ `pairTotals.js` คุมอยู่แล้ว (1 stroke = 1 คู่) แต่ฝั่ง BOM ยังไม่รู้จักคู่ — ถ้าจะทำ MRP จริงต้องต่อตรงนี้

## 🔩 ลิสต์แท็บ BOM แยกพาร์ท / ขั้นตอน (OP) (2026-09-16)

> user: *"ยังปนอยู่ใน BOM เลย"* (ขั้นตอน 🔩 90031601/02 โผล่ปนกับพาร์ทจริงในลิสต์ซ้าย)

ชิปเหนือลิสต์: **พาร์ท (default)** · **🔩 ขั้นตอน (n)** · **ทั้งหมด**

- **ห้ามกรอง OP ทิ้งถาวร** — ขั้นตอนต้องมีสูตรของตัวเอง ไม่งั้นตัดของเสียเป็นเลข SAP ไม่ได้
  (`src/utils/scrapExplode.js`) · แค่ "ไม่ปนในลิสต์เดียว" เท่านั้น
- ลิงก์ `?tab=bom&mat=<OP>` จาก worklist **สลับชิปเป็น 🔩 ให้เอง** — ไม่งั้นกดแล้วแถวหายเงียบ

## 🔑 mat เดียวกันอยู่ได้หลายชั้นในใบเดียวกัน (2026-09-21 · feedback หน้างาน)

> หน้างานแจ้ง: *"ในระบบ ไม่สามารถเพิ่ม 50027085 ใต้ 20058491 ได้ เนื่องจากเลข Mat ซ้ำครับ"*

**ของจริงใน SAP** (CS12 ของ `10100381` — user ส่งจอมาให้):

```
...3  0090  20058490  REINF FRT S/M @FR MTNG RH    1 PC
....4 0010  50027085  WSS-M1A367-A36 … COIL      0.321 KG   ← ใต้ RH
...3  0100  20058491  REINF FRT S/M @FR MTNG LH    1 PC
....4 0010  50027085  WSS-M1A367-A36 … COIL      0.321 KG   ← ใต้ LH
```

คอยล์ม้วนเดียวกันถูกใช้ทั้งซ้าย-ขวา (ปั๊มคู่ · แผ่นละ 0.642 ÷ 2 = **0.321 พอดี**)
**SAP เก็บ 2 แถว** เพราะคีย์ของ BOM คือ **(ใบของตัวแม่, component)** ไม่ใช่ (FG, component)

### ต้นเหตุ 2 ชั้น — แก้ทั้งคู่แล้ว

| ชั้น | ของเดิม | แก้เป็น |
|---|---|---|
| **DB** | `bom_items_product_id_mat_no_key` = UNIQUE (product_id, mat_no) | **`bom_items_product_parent_mat_uniq`** = UNIQUE (product_id, `coalesce(parent_mat,'')`, mat_no) |
| **จอ** | picker **ซ่อน** mat ที่มีในใบแล้วทั้งใบ + insert ไม่เคยใส่ `parent_mat` | เลือก **"เพิ่มเข้าไปใต้:"** ในหัว modal · ซ่อนเฉพาะที่มีใต้แม่ตัวนั้นแล้ว · insert พร้อม `parent_mat` |

- **🔴 ห้ามกลับไป unique (product_id, mat_no)** — เป็นตัวบล็อกโครงจริงของ SAP
  (`coalesce(parent_mat,'')` จำเป็น เพราะ null ใน unique index ไม่ชนกันเอง ⇒ ชั้น 1 จะซ้ำได้ไม่จำกัด)
- ข้อจำกัดใหม่**อ่อนกว่าเดิมเสมอ** ⇒ ไม่มีแถวเดิมแถวไหนผิดกติกา ไม่ต้องล้างข้อมูลก่อน apply
- `doMoveLine` เจอ `23505` แล้วบอกตรงๆ ว่า *"ใต้ X มี Y อยู่แล้ว (รวม QTY ที่บรรทัดนั้นแทน)"*
- migration `20260921_bom_items_unique_per_parent.sql` (**apply แล้ว 21/09**)

⚠️ **ของที่คีย์ไว้ตอนถูกบล็อก ยังต้องให้คนไล่แก้**: `10100381` ตอนนี้มี `50027085` แถวเดียว
0.642 KG ใต้ `20058490` — ตาม SAP ต้องเป็น **0.321 ใต้ 20058490 + 0.321 ใต้ 20058491**
(ตอนนี้เพิ่มได้แล้ว) · **ห้าม session ไหนไปแก้ qty ให้เอง** — PE/Planning เคาะ

## 🏭 เพิ่ม "พาร์ทหลัก" (ของที่เราผลิตเอง) เข้า BOM ได้แล้ว (2026-09-22)

> user: *"BOM ยังเพิ่มพาร์ทหลักไม่ได้"*

**ต้นเหตุ:** picker ของ "+ เพิ่มพาร์ทย่อย" อ่านจาก **`parts_master` เท่านั้น**
⇒ พาร์ทที่เป็นสินค้าผลิตเอง (2xxxx ใน `dr_products`) ที่ยังไม่ได้ลงทะเบียน **เลือกไม่ได้เลย**
วัดจริง 21/09: **20 สินค้าไม่อยู่ใน `parts_master`** (ตระกูล `2005xxxx` TSRA ก่อนชุบ · `20070470/71` GWM)
⇒ เอาไปเป็นลูกใน BOM ของใครไม่ได้สักใบ — ตรงกับที่หน้างานเจอ

**แก้:**
- `pickerSource` = **`parts_master` ∪ `dr_products`** (ไม่ใช่ OP · ไม่ใช่ตัวมันเอง · ตัดที่มีในทะเบียนแล้วออก)
  ตัวที่มาจาก `dr_products` ติดชิป **🏭 ผลิตเอง**
- **ชั้น OP ไม่เข้าลิสต์** — กฎเหล็กเดิม "OP ห้ามเป็นลูกใน BOM ของใคร" (`docs/modules/oee.md`)
- ตอนบันทึก: พาร์ท 🏭 ที่ถูกเลือก **ถูก `upsert` ลง `parts_master` ให้เลย**
  (`onConflict: 'mat_no'` · `ignoreDuplicates`) ⇒ **step 1 ของ workflow ไม่โหว่ต่อไปเรื่อยๆ**
  · ทะเบียนล้ม **ไม่ล้ม BOM** — ขึ้น toast บอกแล้วไปต่อ (ห้ามล้มเหลวเงียบ แต่ห้ามบล็อกงานหลัก)

### workflow ที่ user วางไว้ (21/09) — ใช้เป็นเกณฑ์ตัดสินของทุก session ต่อจากนี้

```
step 1  Parts Master   = ทะเบียนเดียวของ "ของทุกชิ้น"
step 2  BOM            = ประกอบโครงจากของในทะเบียน
step 3  Product        = เลือกจากของที่อยู่ใน BOM แล้ว (ไม่พิมพ์เลขใหม่ซ้ำซ้อน)
```

> **🔴 ต้นเหตุของ "แถวนับซ้ำ" ทั้งฐาน ไม่ใช่แค่ import แบนจาก SAP — ระบบเราผลิตมันเพิ่มเองด้วย**
> user อธิบาย: *"สมมติ 10011 มีลูก 20011 30011 ไปสร้าง product เบอร์ 10011 ก่อน ก็จะมีบอม 2 ตัว
> และพอไปสร้าง product เบอร์ 20011 ก็จะมีบอม 30011 เป็นลูกซ้ำ ซึ่งผิด"*
> ⇒ ทุก product ถูกคีย์ BOM แยกใบของใครของมัน **ไม่มีใครรู้ว่าของชิ้นนั้นถูกคีย์ไปแล้วในใบอื่น**

**ค้างทำ (ตามลำดับที่ user เคาะ):**
1. ✅ step 2 — เพิ่มพาร์ทหลักได้ + ลงทะเบียนอัตโนมัติ (22/09)
2. ⏳ step 2 ต่อ — **เตือนตอนเลือก** ถ้าพาร์ทนั้นอยู่ใต้บรรทัดอื่นในใบนี้แล้ว (กันที่ต้นทาง)
3. ⏳ step 3 — "+ เพิ่มสินค้า" เปิด picker จากทะเบียน/BOM ก่อน แทนช่อง MAT ว่าง
4. ⏳ เรียงแท็บตามลำดับงาน 1️⃣ Parts Master → 2️⃣ BOM → 3️⃣ Products

## 🔢 ลำดับแท็บ + 📤 Export ทุกตาราง (2026-09-22 · คำสั่ง user)

**ลำดับแท็บ `/products` = ลำดับงานจริง ห้ามเรียงตามที่ทำฟีเจอร์มาก่อนหลัง:**

```
1️⃣ Parts Master → 2️⃣ BOM → 3️⃣ Products → Routing → Packaging → Kanban Std
→ ลูกค้า → Supplier → ทบทวน CT → Export
```

- เดิม Products เป็นแท็บแรก ⇒ คนเริ่มจากสร้าง product แล้วคีย์ BOM แยกใบของใครของมัน
  = ต้นเหตุ "แถวนับซ้ำ" ทั้งฐาน (ดูหัวข้อ workflow ด้านบน)
- `useTabParam` default ยังเป็น `'products'` ⇒ **ลิงก์/บุ๊กมาร์กเก่าไม่เปลี่ยนปลายทาง**

**🔴 กันซ้ำที่ต้นทาง (step 2):** ตอนเลือกพาร์ทเข้า BOM ถ้าพาร์ทนั้น**อยู่ชั้นลึกของใบนี้อยู่แล้ว**
จะขึ้นชิปแดง `🔴 อยู่ใต้ <mat> แล้ว` + ถามย้ำตอนกดเพิ่ม (เฉพาะตอนใส่ที่ชั้น 1)
· **เตือน ไม่บล็อก** — ของบางตัวใช้ทั้งใน sub-assy และที่ FG ตรงๆ ได้จริง (SAP แยกด้วยเลข Item)
· ใช้ `explodeBom` + `sheetFor` ⇒ ขอบเขต "ใบเดียวกัน" เหมือนจอต้นไม้

**📤 Export:** ทะเบียนเดียว `EXPORTS` ใน `ProductMaster.jsx` — ครอบทุกตารางของหน้านี้
(`parts_master` · `bom_items` · `dr_products` · `part_routings` · `product_packaging` ·
`container_types` · `kanban_standards` · `customers` · `suppliers`)
- **ห้ามเพิ่มปุ่ม export รายตัวกระจายในโค้ด** — เพิ่มแถวใน `EXPORTS` แถวเดียวจบ
- `cols: []` = ดึงทุกคอลัมน์จากคีย์ของแถวแรก (ตารางเพิ่มคอลัมน์แล้วไม่ต้องตามแก้)
- **ไล่ดึงเป็นหน้า 1,000 แถว** (กฎเหล็กข้อ 5) — ไม่งั้นไฟล์ขาดกลางแบบเงียบ
- นับแถวด้วย `head: true` + `count: 'exact'` ไม่ดึงข้อมูลจริงตอนเปิดจอ (กฎ egress ข้อ 11)
- ตารางที่อ่านไม่ได้ **โชว์ ⚠️ อ่านไม่ได้ ห้ามซ่อนปุ่ม**

## 🔴 จอพังตอนกด "+ เพิ่มพาร์ทย่อย" + เปิดใบ BOM ให้พาร์ทจากทะเบียน (2026-09-22)

**บั๊ก:** `onClick={openPicker}` — React ส่ง **click event เป็น argument ตัวแรก** เสมอ
พอผมเพิ่มพารามิเตอร์ `openPicker(parentMat)` เข้าไป (21/09) event เลยถูกเก็บลง `pickerParent`
⇒ `(m || '').trim()` ระเบิดทั้งจอ *"(e || "").trim is not a function"*
· **build / lint / เทส / crashsweep ผ่านหมด** (crashsweep กดปุ่มบนหัวเพจ แต่ปุ่มนี้ต้องเลือก product ก่อน)

- แก้: `onClick={() => openPicker('')}` + `openPicker` รับเฉพาะ string (`typeof x === 'string'`)
- **ด่านใหม่ใน `regressionGuards`:** `onClick={fn}` บน element จริงของ DOM ที่ `fn` **รับ argument**
  = ตกด่าน · เทียบกับคำประกาศของฟังก์ชันในไฟล์เดียวกัน ไม่ใช่ regex กว้าง (handler ที่ไม่รับ arg
  อย่าง `openNew`/`openCreate` ต้องไม่ถูกฟ้อง ไม่งั้นด่านกลายเป็นที่รำคาญ — กติกาข้อ 1 ของไฟล์นั้น)
  · เจอของเดิมติดมาด้วย 1 จุด: `QualityControl.jsx` `onClick={save}` ที่ `save(extra = {}, …)`
    ⇒ event ถูก spread เข้า payload — แก้แล้ว

### ➕ เปิดใบ BOM ให้พาร์ทจากทะเบียน (ปุ่มในลิสต์ซ้ายแท็บ BOM)

> user: *"ในพาร์ท BOM ก็ยังเพิ่มพาร์ทหลักที่จะสร้าง BOM step ถัดไปไม่ได้"*

ข้อจำกัดจริง: **`bom_items.product_id` → `dr_products.id`** ⇒ ของที่จะเป็น *หัวใบ* ต้องมีแถวใน
`dr_products` · พาร์ทที่อยู่แค่ `parts_master` จึงเปิดใบ BOM ของตัวเองไม่ได้เลย
(= step 2 ของ workflow ทำไม่ได้จริงสำหรับพาร์ทกลางทาง)

- ปุ่ม **➕ เปิดใบ BOM ให้พาร์ทจากทะเบียน** → เลือกจาก Parts Master → สร้างแถว `dr_products`
  **ขั้นต่ำ** (mat_no + ชื่อ + p_no) แล้วเลือกใบให้เลย
- **ไม่ตั้ง `line_name`/CT/เป้า โดยตั้งใจ** ⇒ ไม่โผล่บนบอร์ดผลิต (ทุกจอกรองด้วยไลน์)
  ค่าการผลิตไปเติมที่แท็บ 3️⃣ Products — ตรงกับ step 3 ที่ user วางไว้
- **ไม่ติดธง OP** — นี่คือพาร์ทจริง (ชั้น OP ต้องติ๊กเองตามเดิม)
- mat ที่มีใบอยู่แล้ว = เลือกใบเดิมให้ ไม่สร้างซ้ำ

---


## ✅ แถบความครบต่อสินค้า — "งานใหม่มา ยังขาดอะไร" (2026-09-22)

> user: *"audit work flow ที เท่ากับว่าตอนนี้ ได้งานใหม่มา ต้องทำอะไรก่อน สร้างตาม flow tab ที่เรียงเลยถูกมั้ย"*
> → **ใช่** · แล้วตอบกลับ: *"1.อนาคตต้องบังคับ 2.กำลังไล่ลงข้อมูล"*

**ปัญหา:** ลำดับงานถูกเรียงเป็นแท็บแล้ว (21/09) แต่ **ไม่มีใครรู้ว่าพาร์ทไหนทำถึงขั้นไหน** —
วัดจริง 21/09: 106 สินค้า · ไม่มี BOM 8 · ไม่มี routing 101 · ไม่มี packaging 106 · ไม่มี CT 51 ·
ไม่มี kanban 3 ⇒ ต้องเปิดทีละแท็บไล่เทียบเอง

**ที่ทำ:** `src/utils/productReadiness.js` (pure + มีเทส) → ชิปบนการ์ดสินค้าในแท็บ 3️⃣ Products
`📦 BOM ✓ · ⏱ CT ✗ · 🔀 Routing … · 🧳 Packaging … · 🎴 Kanban ✓` + แถบสรุปหัวลิสต์ + ตัวกรอง

- **🔴 Routing / Packaging = "อนาคตต้องบังคับ" ⇒ วันนี้ขึ้นเป็น `wait` (เหลือง `…`) ไม่ใช่ `miss` (แดง `✗`)**
  เตือน **ห้ามบล็อก** — ระหว่างนี้ user กำลังไล่ลงข้อมูล · ทำเป็นบังคับทันที = ทั้งฐานแดงหมด = คนเลิกอ่าน
  **วันที่จะบังคับจริง แก้ `future: true` ที่ `READINESS_STEPS` จุดเดียว** ห้ามไปเขียนกฎซ้ำในหน้า
- ขั้นที่ "ไม่เกี่ยวกับของชิ้นนี้" = `na` (—) **ไม่นับทั้งเศษและส่วน**: packaging เฉพาะ FG (MAT ขึ้นต้น 1) ·
  kanban/packaging ไม่ใช้กับชั้น OP — ไม่งั้นได้เตือนที่**ไม่มีวันหาย** (บทเรียนเดียวกับ worklist OP 16/09)
- **ตัวกรองแยก 2 ระดับ** (`ยังไม่ครบ (ที่บังคับแล้ว)` / `รวมที่อนาคตบังคับ`) — รวมเป็นตัวเดียวจะกรองอะไรไม่ออกเลย
  เพราะ routing/packaging ยังว่างเกือบทั้งฐาน
- **ชิปกดได้** → เด้งไปแท็บที่ต้องไปเติม (BOM เลือกแถวให้ด้วยผ่าน `openBomFor`) — บอกเฉยๆ ว่าขาด = ไม่มีใครไปเติม
- คีย์ต่างกัน **ห้ามสลับ**: routing ผูก `mat_no` · packaging ผูก `product_id`
- โหลดเพิ่มแค่ 2 คิวรีคอลัมน์เดียว (`part_routings.mat_no` · `product_packaging.product_id`) — กฎเหล็ก 11
