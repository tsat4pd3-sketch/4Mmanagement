# ใบรายงานปัญหาการผลิต + ถังเหลือง/ถังแดง (paperless · 2026-08-19 · feedback หน้างาน)

> ย้ายมาจาก `CLAUDE.md` (2026-09-03 — แยกไฟล์เพื่อลด context) · โหลด**เฉพาะเมื่อแตะโมดูลนี้** · แก้ไฟล์นี้แทน CLAUDE.md เมื่อกฎของโมดูลเปลี่ยน


หัวหน้ากลุ่ม Assy2 แจ้งว่ายัง**เขียนมือทุกครั้ง** 3 ใบ (ปัญหาการผลิต · ถังเหลือง · ถังแดง) ทั้งที่ข้อมูลอยู่ในระบบแล้ว → ทำเป็น export/paperless

### 📝 ใบรายงานปัญหาการผลิต — `src/lib/prodProblemReport.js`

ปุ่ม **📝 ใบรายงานปัญหา** ในหัว session ของ `/daily-report` (โผล่เมื่อกะนั้นมี downtime หรือของเสียอย่างน้อย 1 รายการ)
- **generate จากข้อมูลกะนั้นล้วน ไม่ต้องคีย์ซ้ำ:** `defect_logs` → คอลัมน์คุณภาพ · downtime **นอกแผน ≥ 30 นาที** → คอลัมน์เครื่องจักร หรือ การรอ (แยกด้วย `WAIT_WORDS`)
- **⚠️ เกณฑ์ 30 นาที + ตัด planned ออก มาจากคำพูดหัวหน้าเอง** ("หยุด/รอ เกิน 30 นาที ต้องเขียนใบ") — หยุดตามแผนไม่ใช่ปัญหาที่ต้องรายงาน (หลักเดียวกับ Pareto/Andon ที่กัน planned ออกทุกจุด)
- **ติ๊ก checkbox เป็น best-effort จากคำในข้อความ แต่ข้อความจริงถูกพิมพ์ลง "รายละเอียดของปัญหา" เสมอ** → ติ๊กไม่ตรงก็ไม่มีข้อมูลหาย · ตัวนับ "ตัดรายการสั้นกว่าเกณฑ์: N รายการ" พิมพ์ท้ายใบ **ห้ามตัดเงียบ**
- **⚠️ กับดักที่เจอจริงตอนเทส: คำสั้นเกินไปจับผิดตัว** — keyword `'รู'` (จาก `ขอบงาน-รูรั่ง`) ไป match "เสีย**รู**ป" ทำให้ติ๊กผิดช่อง · **keyword ต้องยาวพอที่จะไม่เป็นสับสตริงของคำอื่น** — เพิ่มคำใหม่ต้องเทสกับข้อความจริงก่อนเสมอ
- doc_key **`prod_problem_report`** (A4 แนวตั้ง · `layout_locked=true` — 3 คอลัมน์ตายตัว) · ยังไม่มีเลขฟอร์มทางการ seed `form_code=null` (หน้าตาเดิมเป๊ะ) doc_control ตั้งเองที่ `/doc-forms`

#### ⚠️ กฎเหล็ก — ช่องบนใบพิมพ์ที่ "ไม่มีทางกรอกจากหน้าจอ" = ช่องตาย ต้องอุด (2026-08-28 · feedback หน้างาน)

*"ปัญหาลงตรงไหนได้บ้างครับ"* (Sup Assy2 วงแดงที่ช่อง **ปัญหา :** บนหัวใบ) — ตรวจแล้ว **ลงไม่ได้เลย**
`printProdProblemReport` รับ `extra = {}` แล้วพิมพ์ `extra.problem` ลงช่องนั้น
แต่ **จุดเรียกจริงจุดเดียว (ปุ่ม 📝 ใน `/daily-report`) ไม่เคยส่ง `extra` มาเลย** ⇒ **ว่าง 100% ทุกใบตั้งแต่วันแรก**
และไม่มีอะไรบอกว่าทำไม (ล้มเหลวเงียบ) · ต่างจากช่อง "ส่วนผลิต/แผนก" ที่รอดเพราะมี fallback `section`
- **`buildProblemReport()` คืน `headline` เพิ่ม** = **แถวจริงที่หนักสุดของกะ** (downtime ยาวสุดก่อน — ทั้งเครื่องจักรและการรอ · ไม่มี downtime ค่อยใช้ของเสียมากสุด) → **สืบกลับได้ว่ามาจากแถวไหน ไม่ใช่ข้อความที่ระบบแต่งขึ้น**
- **ไม่มีรายการเข้าใบเลย = คืน `''` ปล่อยว่างให้เขียนมือ ห้ามแต่งข้อความให้** · หยุดตามแผน/รายการสั้นกว่าเกณฑ์ ไม่มีสิทธิ์เป็นหัวเรื่อง (ไม่เข้าใบอยู่แล้ว)
- **กดปุ่มแล้วเปิดโมดัลให้ตรวจ/แก้/ล้างก่อนพิมพ์ ไม่พิมพ์ทันที** — หัวเรื่องคือชื่อที่ทั้งใบถูกยื่นไว้ใต้มัน **ระบบเสนอ คนตัดสิน** (หลักเดียวกับ AI intake / PE change request) · Enter = พิมพ์
- ใบพิมพ์อ่านด้วย **`extra.problem ?? R.headline`** (`??` ไม่ใช่ `||`) — คนที่ตั้งใจล้างช่องทิ้งต้องได้ช่องว่างจริง ไม่ใช่โดนเติมค่าเสนอกลับ
- เทสล็อกไว้แล้ว `src/lib/__tests__/prodProblemReport.test.mjs` (5 เคส · bundle ด้วย rolldown + stub docForms/PNG)
- **บทเรียนที่ใหญ่กว่าตัวบั๊ก: ช่องในฟอร์มพิมพ์ที่รับค่าผ่าน `extra`/prop ที่ไม่มีใครส่ง = ช่องตาย** ผู้ใช้จะไม่มีวันรู้ว่าต้องกรอกที่ไหนเพราะ "ที่ไหน" ไม่มีอยู่จริง — **เพิ่มช่องใหม่ในใบพิมพ์ ต้องมีจุดกรอกในหน้าจอเสมอ หรือไม่ก็อย่าใส่ช่องนั้น**

#### 🛠 วิธีแก้ไข + ผลตรวจติดตาม ลงในระบบได้ (2026-08-19 รอบ 2 · หัวหน้ากลุ่มขอเพิ่ม)

*"วิธีแก้ไข และผลตรวจ ให้ปริ้นมาเขียนเองใช่มั้ย"* → *"ถ้าเกิน 30 นาที ให้มีไอคอนขึ้นมาให้หัวหน้ากลุ่มลงวิธีแก้ไข และผลตรวจติดตาม"*
- **คอลัมน์ `fix_action`/`fix_by`/`fix_at` + `followup_result`/`followup_by`/`followup_at` บน `downtime_logs` และ `defect_logs`** (migration `20260819_problem_fix_followup.sql` DR · **apply แล้ว 2026-08-19**)
- **⚠️ ไม่สร้างตารางแยกโดยตั้งใจ** — "วิธีแก้ปัญหานี้" เป็นคุณสมบัติของปัญหาตัวนั้น · แยกตาราง = บันทึกลอยที่ต้อง join กลับ + ต้องตามลบเองตอนลบรายการ (คอลัมน์ในตารางเดิมหายไปพร้อมกันเสมอ)
- **เกณฑ์อยู่ที่ `PROBLEM_MIN_MINUTES` (`prodProblemReport.js`) จุดเดียว ห้าม hardcode 30 ซ้ำ** — `dtNeedsFix` / `dtMinutes` / `countPendingFix` ใช้ค่าเดียวกับใบพิมพ์
- **ไอคอนนับ downtime ที่ยังเปิดค้างด้วย** (`dtMinutes` คิดจาก `started_at` ถึงตอนนี้เมื่อยังไม่มี `duration_min`) — ตอนเครื่องยังหยุดคือตอนที่หัวหน้ากำลังแก้จริง · **ผลคือไอคอนเตือนเร็วกว่าใบพิมพ์** (ใบนับเฉพาะรายการที่ปิดแล้ว ≥30 นาที) ตัวเลข 2 ที่ไม่เท่ากันเป็นเรื่องปกติ ไม่ใช่บั๊ก
- **ของเสียมีปุ่มทุกแถว ไม่มีเกณฑ์เวลา** (ของเสียทุกรายการเข้าใบเสมอ) — ถ้าไม่ทำคอลัมน์คุณภาพในใบจะว่างอยู่ดี
- **ยังไม่ลง = ส้ม · ลงแล้ว = เขียวเงียบ · ห้ามกระพริบ** (เป็นงานค้าง ไม่ใช่ alarm ตาม Andon convention) · ป้ายนับ `🛠 N` ข้างปุ่ม 📝 + บรรทัดท้ายใบพิมพ์ "⚠ ยังไม่ได้ลงวิธีแก้ไขในระบบ N รายการ" — **ห้ามให้ใบออกมาว่างเงียบ**
- **💡 Knowledge management โดยไม่สร้างคลังแยก (คำถาม user 2026-08-19):** โมดัลดึง **วิธีแก้ครั้งก่อนของปัญหาชนิดเดียวกัน** มาให้กดใช้ซ้ำ (เครื่องเดียวกันขึ้นก่อน · ข้อความซ้ำยุบเหลือครั้งเดียว · 6 รายการ) — **"ความรู้" คือบันทึกจริงที่สะสมอยู่แล้ว ที่ขาดคือตัวดึงกลับมาใช้** · **ห้ามตั้งตาราง "วิธีแก้มาตรฐาน" ใหม่** จะกลายเป็นที่ที่ 4 ต่อจาก `mtn_orders.solution` / `improvements` / `pe_fmea_items` แล้ว drift กัน · การยกระดับเป็นมาตรฐานมีบ้านอยู่แล้วที่ `/improvements` + ลูป 8D→PFMEA
- component ร่วม `src/components/ProblemFixModal.jsx` (รับ `kind='downtime'|'defect'`) — **จุดใหม่ที่อยากให้ลงวิธีแก้ไขให้ reuse ตัวนี้** · update นับแถวที่เขียนจริง (`.select('id')`) ตามกฎ RLS-เงียบ
- **⚠️ ข้อความ checkbox (`QUALITY_BOXES`/`MACHINE_BOXES`/`WAIT_BOXES`) ถอดจากฟอร์มกระดาษที่สแกนมา — ยังไม่ได้ตรวจกับใบจริง** ถ้าหัวหน้าทักว่าคำไม่ตรง แก้ที่ 3 const นี้จุดเดียว

### 🗑️ ถังเหลือง / ถังแดง — แท็บใน `/qa` (`src/components/QualityBins.jsx`)

- **ตารางเดียว `quality_bin_records` (DR) แยกด้วยคอลัมน์ `bin` ('yellow'|'red') — ห้ามแตก 2 ตาราง** เพราะหมายเหตุท้ายฟอร์มกระดาษเขียนสายงานไว้เอง: *ถังเหลือง → ซ่อม → NG → ติดแท็กแดง → ลงบันทึกชิ้นงานเสีย → วางตะกร้าแดง* · แตกตารางแล้ว**สืบไม่ได้ว่าชิ้นในถังแดงมาจากใบเหลืองใบไหน** → ปุ่ม **➡️ ย้ายเข้าถังแดง** สร้างแถวแดงพร้อม `from_yellow_id` ชี้กลับ
- ถังเหลือง = ชิ้นงานต้องสงสัย (มีช่องซ่อม → ผลซ่อม OK/NG → วันนำกลับเข้ากระบวนการ) · ถังแดง = ของเสียยืนยันแล้ว (ผู้กำจัดทำลาย + ตำแหน่ง ระดับหัวหน้ากลุ่ม/วิศวกรขึ้นไปตาม DOA)
- **ชื่อคนเก็บเป็นข้อความไม่ใช่ FK โดยตั้งใจ** (snapshot pattern เดียวกับ `ojt_training_attendees.emp_name`) — ใบเก่าต้องอ่านออกแม้คนลาออก
- ลบ = **soft delete (`is_active=false`)** ไม่ลบจริง (บันทึกคุณภาพ)
- **พิมพ์ = `src/lib/qualityBinPrint.js`** (A4 แนวนอน · 15 แถว/หน้า ตามฟอร์มกระดาษ · หัวตาราง 2 ชั้นสำหรับ "ผลการซ่อมชิ้นงาน OK|NG") · doc_key **`qbin_yellow`** / **`qbin_red`**
- **ไม่เพิ่ม permission key ใหม่ — reuse `scrap:record` / `scrap:manage`** (งานของเสียกลุ่มเดียวกัน คนกลุ่มเดียวกันดูแล) เลี่ยงกับดัก seed `enum_range` ที่ทำให้ role ใหม่ fail-closed
- **`scrap_report_id`** เตรียมไว้ผูกใบขออนุมัติทำลายกับ `scrap_reports` — **ยังไม่มี UI เชื่อม** (เฟสถัดไป)

#### 🗑️ ส่งของเสียจาก Daily Report ลงถังโดยตรง — `quality_bin_records.defect_log_id` (2026-08-19 รอบ 3)

*"ถ้าเป็นงานเสียขอผูกกับเอกสารตัวนี้ครับ รวมถึงงานต้องสงสัยด้วยครับ"* — ของเสียถูกลงที่ `defect_logs` อยู่แล้ว แต่เดิมต้องไปคีย์ใบถังใหม่ทั้งใบ = ข้อมูลชุดเดียวพิมพ์ 2 รอบ
- ปุ่ม **🗑️ ลงถัง** ในแถวของเสียของ `/daily-report` (โผล่เมื่อ `qty_ng > 0` หรือ `qty_suspect > 0`) → `src/components/QualityBinLinkModal.jsx`
- **การจับคู่ตามความหมายของช่องบนใบกระดาษ: `qty_suspect` → 🟡 ถังเหลือง · `qty_ng` → 🔴 ถังแดง** · 1 บันทึกงานเสียแตกได้ 2 ใบ
- **⚠️ ทิศทาง FK คือ bin → defect ไม่ใช่ defect → bin** — เพราะเป็น 1-ต่อ-หลาย เก็บฝั่ง bin จึงไม่ต้องมีตารางกลาง · `on delete set null` (ใบถังเป็นบันทึกคุณภาพ ต้องอยู่ต่อแม้บันทึกงานเสียถูกลบ — หลักเดียวกับ `from_yellow_id`)
- **⚠️ ห้ามสร้างใบถังให้อัตโนมัติตอนบันทึกงานเสีย** — "เอาของลงถัง" เป็นการกระทำจริงหน้างาน ต้องมีคนกดยืนยัน (หลักเดียวกับ AI intake / PE change request)
- **ลงซ้ำได้แต่ต้องเตือน** (บางทีทยอยลงถังจริง) — โมดัลบอกว่าเคยลงไปแล้วกี่ชิ้น + ปุ่มเปลี่ยนเป็นเขียว `🗑️ ลงถังแล้ว 🟡n 🔴n` **ห้ามบล็อกเงียบและห้ามลงซ้ำเงียบ**
- **`part_no` (เลขลูกค้า) ไม่มีใน `prod_orders`** → โมดัลไปหาจาก `dr_products.p_no` ให้เอง ไม่ให้ QA ต้องกรอกซ้ำ
- ฝั่ง `/qa` แถวที่มาจากทางนี้ติดชิป **📋 จาก Daily Report** (กันคีย์ซ้ำ) · **`defect_log_id` ไม่อยู่ใน payload ตอนแก้ไข** → แก้ใบแล้วลิงก์ไม่หลุด (เหมือน `from_yellow_id`)
- **⚠️ `qty_repair` ไม่สร้างใบให้** — ของที่ซ่อมมักผ่านถังเหลืองมาแล้ว ให้ไปกรอกผลซ่อม OK/NG ที่ใบเหลืองใบเดิม (โมดัลขึ้นข้อความเตือนไว้) ไม่งั้นจำนวนซ้ำ
- migration `20260819_qbin_link_defect_log.sql` (DR · **apply แล้ว 2026-08-19**)
- **สภาพข้อมูลจริง 30 วัน (2026-08-19):** ของเสีย 71 แถว — มี `qty_ng` 69 แถว (236 ชิ้น) · **มี `qty_suspect` แค่ 2 แถว (14 ชิ้น)** → ถังเหลืองยังแทบไม่ถูกใช้ผ่านทางนี้ ต้องดูว่าหน้างานลงงานต้องสงสัยที่ไหน
- migration: `20260819_quality_bin_records.sql` (DR) + `20260819_doc_forms_prod_problem_qbin.sql` (Main) — **apply แล้วทั้งคู่ 2026-08-19**

---

---

## 📕 เทียบกับ WI จริง — WI-PD3-069 Rev.02 + WI-PD3-087 (2026-09-25 · user ส่ง PDF มาให้อ่าน)

**เอกสารต้นทาง:** `WI-PD3-069 Control of Non-Conforming product (Rework/Repair part in house) Rev.02`
(Effective 04/05/2026 · Dept. Production · 14 หน้า) + `WI-PD3-087 การใช้งาน Tag Green/Yellow/Red Rev.00`

### 🔑 โครงสร้างที่ถูกต้อง — **2 ถัง · 3 แท็ก · ใบปะหน้าถัง** (user ยืนยัน 25/09)
- **แท็ก = ป้ายติดที่ตัวชิ้นงาน** 3 สี: 🟡 `FM-QA-006 Rev.06` SUSPECT · 🔴 `FM-QA-007 Rev.02` REJECT ·
  🟢 `FM-QA-098 Rev.04` AFTER REPAIR
- **ถังมี 2 ใบ** (เหลือง/แดง) · **ใบปะหน้าถัง = `FM-PD2-023`** รายการของในถังนั้น — **ใบเดียวครอบทั้งต้องสงสัยและของเสีย**
  ⇒ โครง ESM ที่ใช้ **ตารางเดียว `quality_bin_records` แยกด้วยคอลัมน์ `bin`** ตรงกับฟอร์มจริงแล้ว ✅
- 🟢 แท็กเขียวไม่มีถัง — ของที่ซ่อมผ่านแล้ว QA เซ็นบนแท็ก แล้ว**กลับเข้ากระบวนการ**

### สายงานตาม WI §5.3 / §5.4 — **เหลืองมาก่อนแดงเสมอ**
```
พบ NG → หยุด-เรียก-รอ → ติดแท็กเหลือง + ลง FM-PD2-023 → วางตะกร้าเหลือง
   → หัวหน้า PD ตรวจปัญหา → ออก FM-PD1-019 ใบรายงานปัญหาการผลิต
   → QA พิจารณา 3 ทาง:  ซ่อม  |  ทำลาย  |  ขอใช้ (FM-QA-042)
        ซ่อม   → ซ่อมตาม SOP/WI (I DO I CHECK) → ติดแท็กเขียว → QA ตรวจ/เซ็น → กลับเข้ากระบวนการ
        ทำลาย → ติดแท็กแดง + ลง FM-PD2-023 → **หัวหน้างานทำ Scrap Report โดยอ้างอิงตามแท็กแดง**
                 → DOA: หัวหน้าแผนก→QC→ผจก.QA→ผจก.ผลิต→ผจก.ทั่วไป → เปิด PO + ตัดยอด SAP
                 → ทำลายสภาพเดิม → HRM ตรวจรับ → ทิ้งถัง Scrap → รายงานสรุปยอดรายเดือน
```
🔴 **ข้อมูลจริงบอกว่าหน้างานข้ามขั้น "เหลือง"** — `defect_logs` 191 แถว: `qty_ng>0` **185** · `qty_suspect>0` **4**
⇒ ถังเหลืองมี 1 แถว · ถังแดง 24 แถว · **ทางแก้คือแก้ที่การกรอก ไม่ใช่ให้ NG ลงถังเหลืองได้** (จะขัด WI)

### ⏱️ อายุแท็ก (§5.6) — **ระบบยังไม่มี**
**แท็กเหลือง 5 วัน · แท็กแดง 1 วัน** นับจากวันที่ระบุใน Tag · เมื่อใช้ Tag ต้องลง `FM-PD2-023` ทุกครั้ง

### 📄 เลขฟอร์มจริง (§7) — เทียบกับ `doc_forms`
| ฟอร์ม | ชื่อ | เก็บ | doc_key ใน ESM |
|---|---|---|---|
| **FM-PD1-019** | ใบรายงานปัญหาการผลิต | 1 ปี | `prod_problem_report` (⚠️ `form_code` ยัง null) |
| **FM-PD2-023** | บันทึกรายการชิ้นงานต้องสงสัยและชิ้นงานเสีย | **3 ปี** | `qbin_yellow` + `qbin_red` (⚠️ ยัง null ทั้งคู่ · Rev ยังไม่ทราบ) |
| FM-PD2-002 | ใบรายงานของเสีย (SCRAP REPORT) | 10 ปี | `scrap_report` ✅ ตรงแล้ว (Rev.06) |
| FM-QA-042 | ใบขออนุมัติใช้ชิ้นส่วนเป็นกรณีพิเศษ ("ขอใช้") | — | ❌ ยังไม่มีในระบบ |

### §6 ทะเบียน QRs ↔ WI การซ่อม (ยังไม่ผูกเข้าระบบ)
`WLS6051 Missing nut → WI-PD3-018` · `WLS6005 missing nut M6 → WI-PD3-048` ·
`WLS6033 Skip process cutting hole → WI-PD3-055` · `WLS05295 Welding wrong position → WI-PD3-065` ·
`W3501083 Feed nut double → WI-PD3-068 + WI-PD3-050` · `XLS00165 Missing weld nut → WI-PD3-071`

### 🔗 เชื่อม "ถังแดง → ใบรายงานของเสีย" แล้ว (2026-09-25)
**บั๊กที่เจอ:** `quality_bin_records.scrap_report_id` มีมาตั้งแต่ 19/08 แต่ **ไม่มีโค้ดไหนอ่าน/เขียนเลย**
(grep ทั้ง `src/` ได้ 0 จุด = คอลัมน์ตาย) · ใบรายงานของเสีย prefill จาก `defect_logs` ตรงๆ **ข้ามถังแดงไป**
ทั้งที่ WI §5.5 สั่งให้อ้างอิงตามแท็กแดง ⇒ **วัดจริง 25/09: ถังแดง 24 แถว · ใบ scrap 6 ใบ · ผูกกัน 0**
- ปุ่มใหม่ **⤵ ดึงจากถังแดง** ใน editor ของ `/scrap-report` (วางก่อน "ดึงจาก Daily Report" ให้เป็นทางหลัก)
  — ดึงเฉพาะใบถังที่ `scrap_report_id is null` **กันของก้อนเดียวถูกขออนุมัติซ้ำ** · 1 ใบถัง = 1 แถวบนใบ (ไม่ยุบรวม ต้องสอบกลับได้)
- บันทึกใบแล้ว **เขียน `scrap_report_id` กลับที่ใบถัง** — ทำ**หลัง** items insert สำเร็จเท่านั้น
  (มาร์คก่อนแล้ว items ล้ม = ของหลุดจากสายขออนุมัติทำลายถาวร) · `.select('id')` นับแถว ตามกฎเหล็ก RLS-เงียบ
- `scrap_report_items.src_bin_id` (migration `20260925_scrap_item_from_red_bin_dr.sql` · **apply แล้ว**) + ชิป 🔴 ในตาราง
- ฝั่ง `/qa?tab=bins` ถังแดงมีคอลัมน์ **"ใบรายงานของเสีย"** — `🧾 <เลขที่ใบ>` เขียว / `⏳ ยังไม่ออกใบ` ส้ม

### 🛠 ลงวิธีแก้ไข/ผลตรวจติดตาม **ย้อนหลัง** ได้แล้ว (2026-09-25)
`ProblemFixModal` เคยอยู่แท็บ Live ที่เดียว ⇒ ปิดกะแล้วลงไม่ได้อีก ⇒ ใบรายงานปัญหาที่ออกย้อนหลัง
มีช่องวิธีแก้ไขว่างและเติมไม่ได้ · ยกมาใช้ในแท็บ 📋 ประวัติ ด้วยแล้ว (เกณฑ์เดียวกัน: downtime ผ่าน `dtNeedsFix` · ของเสียทุกแถว)

### 📝 ปุ่มออกใบรายงานปัญหาย้อนหลัง (2026-09-25)
เดิมปุ่ม 📝 อยู่แท็บ Live ที่เดียว ซึ่งดึงกะด้วย `status in ('open','pending_close')`
⇒ **ปิดกะเมื่อไหร่ ใบนั้นออกใหม่ไม่ได้อีกเลย** — หน้างานต้องปริ้นเก็บกระดาษเองทุกวัน (หน.ต้น ทำอยู่จริง)
เพิ่มปุ่มในแท็บ 📋 ประวัติ ทุกแถวแล้ว (กดได้เลยไม่ต้องกางแถว · `loadDetail` คืนค่าที่โหลดด้วย)

---

## ✅ ปิดช่องว่างกับ WI ครบทั้ง 5 ข้อ (2026-09-25 · user: "เริ่มเลยทั้งหมด")

### 1️⃣ ใบรายงานปัญหาเป็น "บันทึก" — ตาราง `prod_problem_reports` (DR)

**ปัญหาเดิม:** ปุ่มพิมพ์ = generate ใหม่ทุกครั้งแล้วจบ ⇒ หัวเรื่อง "ปัญหา :" ที่คนพิมพ์หายทันที ·
ไม่รู้ว่าใครออกใบ/เมื่อไหร่/กี่ใบ · ข้อมูลต้นทางถูกแก้ทีหลัง = ใบที่พิมพ์ซ้ำ **ไม่เหมือนใบที่ยื่นไปแล้ว**
โดยไม่มีใครรู้ ⇒ หน้างานต้องปริ้นกระดาษเก็บเองทุกวัน (**paperless ไม่จริง** ทั้งที่ WI §7 ให้เก็บ 1 ปี)

- 🔴 **ออกใบ = เขียนทะเบียนก่อน แล้วค่อยพิมพ์** — เขียนไม่สำเร็จ **ห้ามพิมพ์ต่อเงียบๆ**
  (ใบที่ไม่มีเลขที่/ไม่มีใครรู้ว่ามี คือของเดิมที่กำลังแก้อยู่) · ทั้ง 2 จุดเรียกผ่าน `issueProblemReport()`
- 🔴 **`snapshot` jsonb = เนื้อใบ ไม่ใช่ pointer** — พิมพ์ซ้ำได้เหมือนเดิมเป๊ะแม้ downtime/ของเสียต้นทาง
  ถูกแก้/ลบทีหลัง (หลักเดียวกับ snapshot ของ `pe_fmea` / `npi_*`) · `checked` เป็น **Set** ⇒
  `JSON.stringify` ตรงๆ ได้ `{}` เงียบ **ต้องผ่าน `serializeReport()`/`reportFromSnapshot()` เสมอ**
- 🔴 **พิมพ์ซ้ำ = ใบเดิม ไม่ใช่ใบใหม่** (`reprintProblemReport()` นับ `reprint_count` ไม่ออกเลขใหม่)
- 🔴 **snapshot ไม่ตรงข้อมูลปัจจุบัน = เขียนบนจอ** (`docMatchesNow()` เทียบ `reportSignature`) —
  จอบอกว่าปุ่ม 🧾 จะพิมพ์ *ใบเดิมที่ยื่นไป* ไม่ใช่ข้อมูลล่าสุด · อยากได้ใบตามข้อมูลใหม่ = ออกใบใหม่ (เลขใหม่)
  · signature เทียบ**เฉพาะสิ่งที่พิมพ์ลงใบจริง** ไม่รวมตัวนับ
- **กันออกใบซ้ำซ้อน:** กด 📝 ตอนที่มีใบเดิมที่เนื้อ*เหมือนเดิมทุกอย่าง* → confirm ก่อน พร้อมชี้ปุ่มพิมพ์ซ้ำ
- เลขที่ใบ running รายเดือน `PR 0001/09-26` — **"เลขสูงสุดของเดือน + 1" ห้ามใช้ count()+1**
  (ออกใบย้อนวันแล้วเลขชน — บทเรียนเดียวกับ `nextDocNo` ของใบรายงานของเสีย)
- ไฟล์: `src/lib/prodProblemDoc.js` (ตัวห่อ DB) + `src/lib/prodProblemReport.js` (snapshot helpers + ใบพิมพ์)
  · migration `20260925_prod_problem_reports_dr.sql` (**apply แล้ว**)
- ด่าน `regressionGuards`: **`problem-report-must-be-recorded`** — ห้ามเรียก `printProdProblemReport()`
  จากหน้าจอตรงๆ อีก (ต้องผ่านตัวห่อที่บังคับเขียนทะเบียนก่อน)

### 2️⃣ โมดัล 🗑️ ลงถัง — ลงจบในจอเดียว

**วัดจริง 25/09: ถังแดง 24 แถว มี `disposed_by` 0 แถว** — เพราะเดิมโมดัลขึ้นข้อความว่า
"ไปกรอก QA/ผู้กำจัดทำลาย ต่อที่หน้า QA/QC" แล้วปิดตัวเองเงียบ ⇒ ไม่มีใครตามไปกรอก (และหาใบไม่เจอ)
- ยกช่องที่ **รู้ตั้งแต่ตอนลงถัง** มาไว้ที่นี่: ผู้แจ้ง · QA · (ถังแดง) ผู้กำจัดทำลาย + **ตำแหน่ง auto**
  จากทะเบียน positions · ทุกช่องผ่าน `<PersonSelect>` + `useColumnHistory` ตามกฎ picker กลาง
- ⚠️ **ช่องที่ยังไม่รู้ตอนลงถังจริงๆ ไม่ยกมา** (ผลซ่อม OK/NG · วันนำกลับเข้ากระบวนการ · ผลพิจารณา QA)
  — บังคับกรอกสิ่งที่ยังไม่เกิด = ได้ข้อมูลมั่ว **แย่กว่าช่องว่าง**
- บันทึกแล้ว **ไม่ปิดเงียบ** → แผงสรุปว่าสร้างใบอะไรไว้ + ปุ่ม `🗑️ ไปดูใบในถัง →` (`/qa?tab=bins`)

### 3️⃣ นาฬิกาอายุแท็ก (§5.6) — `src/utils/qualityBin.js`

- `TAG_MAX_DAYS = { yellow: 5, red: 1 }` · **"วันที่บนแท็ก" = `work_date`** ที่กรอกอยู่แล้ว
  ⚠️ **ไม่เพิ่มคอลัมน์ `tag_date`** — จะไม่มีจุดกรอก = ช่องตาย (บทเรียน `extra.problem` 28/08)
- 🔴 **`binClosed()` คำนวณจากข้อเท็จจริงที่มีอยู่แล้ว ไม่เพิ่ม `closed_at` ที่ต้องมีคนกดปิด**
  — คอลัมน์แบบนั้นไม่มีใครกด = **ของค้างเทียมเต็มจอ แล้วคนเลิกเชื่อจอ**
  เหลืองปิดเมื่อ: `return_date` · QA ว่า "งานดี" · "ขอใช้" ที่มีเลข FM-QA-042 · ย้ายลงถังแดงแล้ว
  (`from_yellow_id` ชี้กลับ) · แดงปิดเมื่อ: มี `scrap_report_id`
- จอ: คอลัมน์ **อายุแท็ก** `n / limit วัน` · เกิน = ส้ม **ไม่กระพริบ** (งานค้าง ไม่ใช่ alarm — Andon convention)
  · ปุ่ม `⏱ เกินอายุแท็ก N` ในแถบกรอง **โผล่เฉพาะเมื่อมีของค้างจริง** (ปุ่มที่กดแล้วว่างเสมอ = คนเลิกกด)
- ⚠️ `binTagAge()` **รับ `today` เป็นพารามิเตอร์ ไม่อ่านนาฬิกาเอง** (กฎเทสระเบิดเวลา)
  · ข้อมูลไม่พอ = `null` **ห้ามโชว์ 0 วัน**

### 4️⃣ ผลพิจารณา QA ครบ 4 ทาง (§5.4)

`quality_bin_records.qa_decision` · `qa_decision_at` · `special_use_doc_no`
(migration `20260925_qbin_qa_decision_dr.sql` · **apply แล้ว** · แถวเดิม 25 แถว = null ทั้งหมด)

| ค่า | ป้าย | ความหมายตาม WI |
|---|---|---|
| `good` | ✅ งานดี | ตรวจแล้วใช้ได้ → แท็กเขียว FM-QA-098 → กลับเข้ากระบวนการ |
| `repair` | 🔧 ซ่อม | ซ่อมตาม SOP/WI (I DO I CHECK) → QA เซ็นแท็กเขียว |
| `use_as_is` | 📄 ขอใช้ | ใช้เป็นกรณีพิเศษ — **ต้องมีเลขใบ FM-QA-042** ถึงจะปิดสายงาน |
| `scrap` | 🔴 ทำลาย | แท็กแดง → ถังแดง → FM-PD2-002 → DOA |

- 🔴 **`null` = "รอ QA พิจารณา" ไม่ใช่ "ไม่มีผล"** — จอเขียนตรงๆ ห้ามเดาผลให้ · ห้ามบังคับเลือก
- "ขอใช้" ที่ยังไม่มีเลขใบ = **เตือน ไม่บล็อก** (เลขใบอาจยังไม่ออกตอน QA ตัดสิน) แต่ยังนับเป็นของค้าง
- ปุ่ม 🔴 ย้ายลงถังแดง เดิมโผล่เฉพาะเมื่อกรอก "ซ่อมแล้ว NG" — เปิดให้เมื่อ QA ชี้ `scrap` ด้วย
  (WI ให้ทำลายได้โดยไม่ต้องผ่านการซ่อม) · ย้ายแล้วขึ้นป้าย `🔴 ย้ายแล้ว` **กันกดซ้ำ**

### 5️⃣ ทะเบียน QRs ↔ WI ซ่อม (§6) — ตาราง `repair_wi_registry` (DR · seed 6 แถวจาก WI)

- ⚠️ **ไม่ใช่คลัง "วิธีแก้มาตรฐาน"** (ห้ามสร้าง — จะเป็นที่ที่ 4 ต่อจาก `mtn_orders.solution` /
  `improvements` / `pe_fmea_items` แล้ว drift กัน) · เก็บแค่ **"อาการนี้อ้าง WI เล่มไหน"**
- 🔴 **กติกาจับคู่ (`src/utils/repairWi.js` · pure + มีเทส)** ตามกฎ "อื่นๆ ห้ามขึ้นอันดับ 1" ข้อ 4-5:
  1. **เลขพาร์ท/QRs ต้องตรงเป๊ะ** (normalize ช่องว่าง/ขีด/ตัวพิมพ์) ⇒ ป้าย "ตรงเลขพาร์ท"
  2. **คำอาการใช้คำจากทะเบียนเอง** ไม่ใช่พจนานุกรมเขียนมือ · ยาว ≥ 4 ตัว (บทเรียน `'รู'` ชน `เสียรูป`)
     ⇒ ป้าย "ตรงคำอาการ" — คนเห็นเองว่าเป็นการเดา
  3. **จับคู่ไม่ได้ = ไม่โชว์** — แนะนำผิดเล่ม อันตรายกว่าไม่แนะนำ · คืนไม่เกิน 4 เล่ม
- ชิปในโมดัล 🛠 **เติมท้ายข้อความ ไม่ทับของเดิม** · อยู่เหนือ "เคยแก้มาก่อน" เพราะเป็น**ข้อกำหนด**ไม่ใช่ประสบการณ์
- แผงจัดการ: `/qa?tab=bins` → ปุ่ม **📕 ทะเบียน WI ซ่อม** (`<SimpleMasterPanel>`) — doc_control/QA แก้เองได้
- ด่าน `regressionGuards`: **`repair-wi-number-hardcoded`** (ห้ามเขียน `'WI-PD3-xxx'` ในโค้ด) +
  **`quality-bin-rules-hand-built`** (ห้ามประกาศ `TAG_MAX_DAYS` / ลิสต์ผลพิจารณา QA ซ้ำนอก util กลาง)

### 📌 ยังค้างอยู่
- **เลขฟอร์มใน `doc_forms` ยัง null ทั้ง 3 ตัว** — `prod_problem_report` = FM-PD1-019 (รู้แล้ว) ·
  `qbin_yellow`/`qbin_red` = FM-PD2-023 (**Rev ยังไม่ทราบ** — รอ doc_control) ⇒ ยังไม่ seed
- ใบเก่าที่ออกก่อน 25/09 ไม่มี snapshot (ไม่มีใบเลย — ตารางเพิ่งสร้าง) · ใบที่ `snapshot` ว่าง
  ปุ่มพิมพ์ซ้ำจะบอกให้ออกใบใหม่ **ไม่พิมพ์ใบเปล่า**
