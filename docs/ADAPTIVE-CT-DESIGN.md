# Adaptive Cycle Time — เอกสารออกแบบเพื่อ "คุยก่อนลงมือ"

> สถานะ: **📌 ออกแบบ/ถกกันอยู่ ยังไม่ลงมือ** (คำสั่ง user 2026-09-17: *"คุยออกแบบก่อน ยังไม่ลงมือ"*)
> ห้าม session ไหนหยิบไปทำเองจนกว่า user จะสั่ง — กติกาเดียวกับ `IATF16949-GAP-REVIEW.md`

## 1. โจทย์ตั้งต้น (จาก user 2026-09-17)

> *"เราเคยไปดูงานที่ต่างประเทศ เค้ามีระบบ adaptive cycle time ที่จะคอยปรับจาก actual ที่เกิดขึ้นจริง
> ซ้ำๆๆๆ มากพอ เพื่อกันพนักงานที่โกง แบบ รีบทำงานมากเกินไปในช่วงนึงและไปนอนในเวลางาน"*

**อาการที่ทำให้เกิดคำถามนี้ — วัดจริง 17/09 (795 กะที่ปิดแล้ว ตั้งแต่ ส.ค.):**

| ไลน์ | กะที่ %P = 100 | สัดส่วน | %P เฉลี่ยตอนไม่ชนเพดาน |
|---|---|---|---|
| LASER E50 | 36/47 | **77%** | 76.3 |
| SUB APRON | 38/68 | **56%** | 83.5 |
| Assy  LWR | 28/61 | **46%** | 87.8 |
| LASER EXPORT | 11/31 | 35% | 78.0 |
| LASER-789 | 23/68 | 34% | 82.5 |
| HDF1 | 16/57 | 28% | 82.3 |
| **รวมทั้งโรงงาน** | **222/795** | **28%** | — |

`%P = 100` ตรงนี้ **ไม่ใช่ผลวัด แต่เป็นการ cap** — สูตรคำนวณได้เกิน 1 แล้วถูกกดลงมา
(`pRawRatio > 1` → `Math.min(1, …)` ใน `computeOEE`) แปลว่า **งานมาตรฐานที่บันทึกได้ มากกว่า
เวลาเครื่องที่มีจริง** ⇒ CT ใน master ตั้งช้ากว่าความเป็นจริง

**ผลต่อความเชื่อถือ:** หัวหน้าไลน์ LASER E50 เห็น 100% สี่ในห้ากะ ก็รู้ทันทีว่าเลขนี้เชื่อไม่ได้
— เสียความน่าเชื่อถือพอๆ กับเลขที่ต่ำเกินจริง และเป็นอุปสรรคตรงๆ ต่อเป้าหมาย "เลิกใช้ Excel"

---

## 2. ⚠️ กับดัก 3 ข้อที่ต้องแก้ก่อน ไม่งั้นเจ็บกว่าเดิม

### 2.1 CT ที่นี่ไม่ใช่แค่ตัววัดผล — มันเป็นเอกสารคุณภาพด้วย
`dr_products.cycle_time_sec` ตัวเดียวกันนี้ถูกอ้างใน Control Plan / PFC และงานที่ส่งลูกค้า (PPAP)
**ถ้าระบบขยับเอง = เอกสารที่ส่งลูกค้าไปแล้วไม่ตรงกับระบบ** → ปัญหา audit IATF

⇒ ต้อง **แยก 2 ค่าออกจากกันให้ขาด**:
| ค่า | ใครเป็นเจ้าของ | ใช้ทำอะไร |
|---|---|---|
| `ct_standard` (CT วิศวกรรม) | PE | เอกสาร · เสนอราคา · วางแผนกำลังผลิต · **ตัวหารของ %P** |
| `ct_observed` (CT สังเกตได้) | ระบบคำนวณ | **เตือน**ว่าถึงเวลาทบทวนมาตรฐาน · ไม่เข้าสูตร OEE โดยตรง |

### 2.2 ไม่มีประวัติ CT = ซ้ำรอยที่เพิ่งเจอวันนี้
17/09 พยายาม backfill %P ย้อนหลังแบบคำนวณใหม่ **แล้วทำไม่ได้** เพราะ CT เปลี่ยนไปแล้วและ
**ไม่มีใครรู้ว่าตอนปิดกะนั้น CT เป็นเท่าไหร่** (จำลองด้วย CT วันนี้ ตรงกับที่ stamp ไว้แค่ 27-44%)

**ถ้าเปิด adaptive แล้ว CT ขยับทุกสัปดาห์โดยไม่เก็บประวัติ ⇒ OEE เดือนนี้เทียบเดือนที่แล้วไม่ได้เลย
เพราะไม้บรรทัดคนละอัน** — และจะไม่มีใครรู้ตัวด้วย

⇒ **ตาราง `ct_history` เป็นของที่ต้องมีก่อน ไม่ว่าจะเลือกทางไหน** (ใคร · เมื่อไหร่ · จากเท่าไหร่
เป็นเท่าไหร่ · เพราะอะไร · อ้างอิงกี่กะ) · นี่คือสิ่งที่ควรทำแม้จะไม่ทำ adaptive เลยก็ตาม

### 2.3 Ratchet — บันไดขาขึ้นอย่างเดียว
CT ปรับตาม actual เรื่อยๆ = ยิ่งทำเร็ว มาตรฐานยิ่งแน่น พนักงานรู้ทันแล้วจะ **"กั๊กความเร็ว"**
= สอนให้โกงอีกแบบหนึ่ง (และอันตรายกว่า เพราะมองไม่เห็น)

⇒ ของจริงที่โรงงานเขาใช้กันมี **เบรกเสมอ**: ขยับครั้งละไม่เกิน x% · ต้องมีข้อมูล ≥ N กะ ·
ต้องมีคนอนุมัติ · ปรับได้ไม่ถี่กว่าทุก N สัปดาห์ — **ไม่ปล่อยไหลอิสระ**

---

## 3. 🔑 ข้อสังเกตสำคัญ — adaptive CT **แก้เรื่องโกงไม่ได้**

โจทย์ตั้งต้นคือ *"รีบทำ 4 ชม. แล้วไปนอน 4 ชม."*

**CT ที่แน่นขึ้นจับเคสนี้ไม่ได้เลย** เพราะทั้ง CT และ %P เป็น **ค่าเฉลี่ยทั้งกะ**:

| พฤติกรรม | ของดีทั้งกะ | เวลารันทั้งกะ | %P |
|---|---|---|---|
| ทำสม่ำเสมอ 8 ชม. | 400 | 480 นาที | เท่ากัน |
| รีบ 4 ชม. + นอน 4 ชม. | 400 | 480 นาที | **เท่ากันเป๊ะ** |

⇒ ปรับ CT ยังไงก็แยก 2 เคสนี้ไม่ออก

**แต่ระบบนี้จับได้ตรงๆ อยู่แล้ว** เพราะมี `opened_at` / `confirmed_at` **รายการ์ด**:
- การ์ดที่ใช้เวลาน้อยกว่า CT มากผิดปกติ **ติดกันหลายใบ**
- แล้วตามด้วย **ช่องว่างยาวที่ไม่มี downtime แจ้ง**
= ลายเซ็นของ "รีบแล้วไปนอน" ชัดเจน · วัดด้วยการกระจายตัว (เช่น CV ของเวลาต่อการ์ด) ไม่ใช่ค่าเฉลี่ย

> **ข้อเสนอ: แยกเป็น 2 เรื่องคนละเรื่อง — อย่าเอามาปนกัน**
> - **เรื่อง A — CT ตั้งช้า** ⇒ แก้ด้วยจอทบทวน CT (ปัญหาความถูกต้องของมาตรฐาน)
> - **เรื่อง B — จับจังหวะการทำงานผิดปกติ** ⇒ แก้ด้วยตัวตรวจความสม่ำเสมอรายการ์ด (ปัญหาพฤติกรรม)
>
> เอา adaptive CT ไปแก้เรื่อง B = แก้ไม่ตรงจุด และสร้าง ratchet ขึ้นมาฟรีๆ
> · user สั่ง 17/09: เรื่อง B **"ยังไม่ต้อง เอาตัวเลขให้ถูกก่อน"**

---

## 4. ทางเลือกที่เสนอ (ยังไม่เลือก)

### เฟส 0 — ต้องทำก่อนทุกทาง
1. `ct_history` — ทุกการเปลี่ยน CT ถูกบันทึก (ผูก audit ที่มีอยู่แล้ว — ดู `traceability-audit-log.md`)
2. `production_sessions` เก็บ **CT ที่ใช้จริงตอนปิดกะ** (snapshot ต่อ MAT) ⇒ %P ย้อนหลังคำนวณซ้ำได้
   **นี่คือสิ่งที่ถ้ามีตั้งแต่แรก วันนี้จะ backfill ได้ครบทั้ง 145 กะ ไม่ใช่ 117**

### ทางเลือก A — จอทบทวน CT (คนตัดสิน 100%)
รายงานต่อพาร์ท: `ct_standard` · `ct_observed` (มัธยฐานจาก N กะล่าสุด) · จำนวนกะที่ชนเพดาน ·
ส่วนต่าง % → PE/หัวหน้ากดดูแล้วตัดสินใจแก้เอง
- ✅ ไม่ชน 2.1 · ไม่มี ratchet · ทำได้เร็ว
- ❌ ต้องมีคนขยันเข้าไปดู

### ทางเลือก B — A + ปุ่ม "ใช้ค่านี้"
เหมือน A แต่กดปุ่มเดียวเขียนลง master (+ `ct_history` + เหตุผล) — คนยังตัดสิน แค่ไม่ต้องพิมพ์เอง
- ✅ ลดแรงเสียดทาน คนแก้จริง
- ❌ ต้องมีสิทธิ์คุม (`ct:manage`) ไม่งั้นใครก็กด

### ทางเลือก C — Adaptive เต็มรูป
`ct_observed` อัปเดตอัตโนมัติ + เสนอปรับ `ct_standard` เมื่อครบเงื่อนไข (≥ N กะ · ส่วนต่าง ≥ x% ·
เข้าคิวอนุมัติ) · **ยังต้องมีคนกดอนุมัติ ไม่เขียนทับเอง**
- ✅ ตามทันของจริงโดยไม่ต้องรอคนสังเกต
- ❌ งานใหญ่สุด · ต้องออกแบบเพดาน/ความถี่/สิทธิ์ให้ครบ · ต้องกัน ratchet

---

## 5. คำถามที่ user ต้องตัดสิน

1. **`ct_standard` กับ `ct_observed` แยก 2 คอลัมน์ได้ไหม** หรือโรงงานยอมรับ CT ค่าเดียว?
2. **ใครมีสิทธิ์แก้ CT** — PE อย่างเดียว หรือหัวหน้ากลุ่มแก้ได้ด้วย?
3. **%P ควรหารด้วยอะไร** — `ct_standard` (เทียบกับมาตรฐานวิศวกรรม) หรือ `ct_observed`
   (เทียบกับสิ่งที่ไลน์ทำได้จริง)? → **ตอบข้อนี้ก่อน เพราะมันเปลี่ยนความหมายของ OEE ทั้งระบบ**
4. เกณฑ์ขยับ: ต้องมีกี่กะ · ส่วนต่างกี่ % · ขยับได้ครั้งละไม่เกินเท่าไหร่ · ถี่สุดแค่ไหน?
5. CT ที่ใช้กับเอกสาร Control Plan/PPAP ต้อง **freeze** แยกจากตัวที่ใช้วัดผลไหม?

---

## 6. ของที่ทำไปแล้วและเกี่ยวข้อง (17/09)
- แก้บั๊กที่ทำให้ OEE เพี้ยนไปแล้ว 4 ตัว — ดู `docs/modules/oee.md`
- **พิสูจน์แล้วว่า CT master เปลี่ยนไปจริง** (จำลองย้อนหลังตรงแค่ 27-44%) ⇒ ข้อ 2.2 ไม่ใช่ความกังวลลอยๆ
