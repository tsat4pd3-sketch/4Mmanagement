# 🔴 Egress Audit — 2026-09-17 (รอบ "วัดเป็นไบต์")

> **บริบท:** Supabase Pro เหลือ 10 วัน (user: *"ต้องจบ ห้ามเกินนี้เด็ดขาด"*) · เป้าคือกลับไป Free 5 GB/เดือน
> โดยกำลังจะเพิ่มจอ TV 10 จอ (เฟส 1) + แท็บเล็ต 10 + เฟส 2 อีก ~20 ⇒ **~40 เครื่องเปิดค้าง**
>
> เอกสารก่อนหน้า `docs/POLLING-AUDIT-2026-09-15.md` = รอบ "ลดจำนวนครั้งที่ยิง" (ได้ −24%)
> **เอกสารนี้ = รอบ "ลดขนาดต่อครั้ง" ซึ่งใหญ่กว่ามาก**

---

## 1. บทเรียนวิธีวัด — สิ่งที่ทำผิดมา 2 รอบ

รอบ 15-16/09 ผมจัดลำดับความสำคัญจาก **จำนวน request** ต่อ path
แล้วสรุปว่า `production_sessions` (10,952 ครั้ง/วัน) คือตัวใหญ่สุด

**นั่นผิด** — Supabase คิด egress เป็น**ไบต์** ไม่ใช่จำนวนครั้ง
`production_sessions?select=line_name,status` = **0.5 KB/ครั้ง**
`mtn_orders?select=*` = **1,590 KB/ครั้ง** (ต่างกัน **3,000 เท่า**)

⇒ ตารางอันดับตามจำนวน request **ชี้ผิดตัวเกือบทั้งใบ**

### กับดักที่ทำให้มองไม่เห็น

| กับดัก | อาการ | ทางออกที่ใช้จริง |
|---|---|---|
| **PostgREST ไม่ส่ง `Content-Length`** (chunked) | `edge_logs` รวมไบต์ REST ได้ **0.1 MB จาก 68,910 req** ⇒ ดูเหมือนคิวรีไม่กิน egress เลย | วัดเองจากฝั่ง DB: `octet_length(json_agg(t)::text)` ของคิวรีจริง |
| `request.search` ถูกมองข้าม | ไม่รู้ว่าแต่ละ path เลือกคอลัมน์อะไร | group by `(path, request.search)` → ได้ตัว `select=*` โผล่มาเอง |
| เน็ตใน session ของ AI ต่อ `*.supabase.co` ไม่ได้ (proxy 403) | วัด gzip จริงไม่ได้ | ใช้ `pg_column_size()` บน temp table = pglz (อนุรักษ์นิยมกว่า gzip) เป็นตัวแทน |

**กฎที่ตกผลึก:** เวลาไล่ egress ให้จัดอันดับด้วย **(จำนวนครั้ง × ไบต์ต่อครั้ง)** เสมอ
ห้ามจัดอันดับด้วยจำนวนครั้งอย่างเดียว

---

## 2. ภาพรวมที่วัดได้ (เต็มวัน 16/09 เวลาไทย)

| ช่องทาง | requests | ไบต์ที่วัดได้จาก log |
|---|---:|---:|
| DR — STORAGE | 2,206 | **135.7 MB** |
| DR — REST | 68,910 | วัดจาก log ไม่ได้ (ดู §1) — ประเมิน ~1.2 GB |
| MAIN — STORAGE | 1,581 | **70.5 MB** |
| REALTIME (ทั้ง 2 project) | 648 | ~0 |

> **Storage อย่างเดียว = 206 MB/วัน ≈ 6 GB/เดือน ⇒ เกินโควต้า Free 5 GB โดยยังไม่นับคิวรีสักตัว**
> และ Realtime ≈ 0 — **ยืนยันอีกครั้งว่า realtime ถูกมาก การ poll/ดึงก้อนใหญ่ต่างหากที่แพง**

---

## 3. ตัวการอันดับ 1 — `mtn_orders?select=*`

| | ค่า |
|---|---|
| คอลัมน์ในตาราง | **116** |
| แถวในตาราง | 410 (เปิดอยู่ 153) |
| `select=*&order=report_at.desc&limit=1000` | **1,629,076 B (1.59 MB)** · บีบแล้ว **452 KB** |
| เลือกเฉพาะคอลัมน์ที่จอ list ใช้ (19 คอลัมน์) | **209,401 B** · บีบแล้ว **48 KB** |
| จำนวนครั้ง/วัน (16/09) | **1,389** (`select=*`) + 623 (แบบย่อ) |
| **ค่าใช้จ่ายต่อวัน** | **~628 MB/วัน** |

⇒ **คิวรีเดียวกินเกือบครึ่งของ egress ฝั่ง DR ทั้งหมด** · บนโควต้า Free 5 GB = หมดใน ~8 วัน

**ทำไมแถวถึงอ้วน:** ไม่มีคอลัมน์ไหนอ้วนผิดปกติเลย (ตัวหนักสุด `checker_sign` = 42 KB ทั้งตาราง ≈ 100 B/แถว)
มันคือ **116 ชื่อคอลัมน์ถูกส่งซ้ำ 410 รอบ พร้อม `:null` ของช่องที่ยังไม่ได้กรอก**
(ตัดค่า null ทิ้งอย่างเดียวเหลือ 1.14 MB — ยังแพง เพราะปัญหาคือ*จำนวนคอลัมน์* ไม่ใช่ค่าว่าง)

### วิธีแก้ที่ใช้
- `MO_LIST_COLS` (19 คอลัมน์) ใน `src/pages/MtnRepair.jsx` — จอรายการ/ฟิลเตอร์/สถิติ/แท็บ KPI ใช้เท่านี้จริง
- **ใบเต็ม 116 คอลัมน์ดึงตอนเปิดใบทีละใบ** (`fetchFullOrder()` → `.select('*').eq('id', id)`)
- `reloadAll()` รีเฟรชทั้งรายการ + ใบที่เปิดค้างอยู่ (เดิม drawer อ่านแถวจาก `orders` ตรงๆ จึงสดเอง)
- `DeptDashboard.loadMaintenance()` ตัดจาก `select('*').limit(500)` เหลือ 7 คอลัมน์
- `TvBoard` / `DieRegistry` ตรวจแล้ว — **เลือกคอลัมน์ย่ออยู่ก่อนแล้ว** ไม่ต้องแก้

**ผลที่คาด: 628 MB/วัน → ~67 MB/วัน (−89%)**

### ⚠️ กับดักที่เกือบพลาดระหว่างแก้
ตอนแรกใส่ `openDetail` ลงใน deps array ของ `useEffect` ที่อยู่ **เหนือ** บรรทัดประกาศ `const openDetail`
→ deps array ถูก evaluate **ตอน render** ⇒ TDZ `Cannot access before initialization` = **จอขาวทั้งหน้า**
(คลาสเดียวกับที่ทำ FactoryMap พังเมื่อ 15/09) · จับได้ก่อน commit — **`node audit/crashsweep.mjs` ผ่าน 71 หน้า พัง 0**

---

## 4. ตัวการอันดับ 2 — รูปผังเป็น PNG

| ไฟล์ | ขนาดจริง |
|---|---:|
| `layouts/layout_LINE_APRON_ASSY_*.png` | **8.4 MB** |
| `layouts/layout_test_*.png` | 3.0 MB |
| `layouts/layout_LINE_MAIN_TSRA_2_*.png` | 2.5 MB |
| `factory/map_*.png` | 2.4 MB |
| `layouts/layout_HYDROFORM_*.png` | 2.3 MB |
| `jig-images/facility/*.png` (ผังเครื่องจักร) | 1.1 MB |

**สาเหตุ:** โค้ดส่งเข้า `imageCompression({ maxSizeMB: 2.5, maxWidthOrHeight: 2560, initialQuality: 0.9 })`
แต่ **ไม่ระบุ `fileType`** ⇒ ไลบรารีคืนชนิดเดิม = **PNG ซึ่งเป็น lossless** ⇒ `maxSizeMB` แทบไม่มีผล

**สิ่งที่ห้ามทำ:** ลดความละเอียดลง — มีคอมเมนต์เตือนไว้ในโค้ดอยู่แล้วว่า *"อย่าลดกลับไป 1600px/0.5MB เคยเบลอ"*
ผังต้องซูมอ่านชื่อสถานี/เลขเครื่องได้

**วิธีแก้:** `src/utils/layoutImage.js` → `compressLayoutImage()`
**คงความละเอียด 2560px เท่าเดิม แต่แปลงเป็น WebP** (เก็บ alpha ได้ · เล็กกว่า PNG ~70-85% ที่คุณภาพเท่ากัน)
· รองรับจอ TV ที่ใช้จริง (webOS 23 = Chromium 94 · WebP มีตั้งแต่ Chrome 32)
· ⚠️ **กับดัก:** Safari < 16.4 เขียน WebP ไม่ได้แล้ว**เงียบๆ คืน PNG** ⇒ ฟังก์ชัน derive นามสกุลจาก
`blob.type` ที่ได้จริงเสมอ ห้ามตั้ง `.webp` ล่วงหน้า (ไม่งั้นได้ไฟล์ PNG ชื่อ .webp = รูปเสีย)

จุดที่แก้แล้ว: `LineSetup` (ผังไลน์) · `FactoryMap` (ผังโรงงาน) · `MtnMachineLayout` (ผังเครื่องจักร)

> **⚠️ แก้โค้ดช่วยเฉพาะรูปที่อัปใหม่** — รูปเดิมยังเป็น PNG ก้อนโตอยู่
> ต้องมีตัว re-compress ของเดิม (ดู §6 งานค้าง)

---

## 5. cacheControl — ตรวจแล้ว ไม่ใช่ปัญหา

ทุก object ที่ตรวจมี `max-age=31536000` (1 ปี) ครบ — งาน `uploadOpts()` + migration backfill 11/09 ทำงานถูกต้อง
⇒ **ที่ยังเสียไบต์คือ "เครื่องที่ยังไม่เคยโหลด" ไม่ใช่ "โหลดซ้ำ"** ⇒ ทางแก้คือลดขนาดไฟล์ ไม่ใช่ยืด cache
(และนี่คือเหตุผลที่ยิ่งเพิ่มจำนวนเครื่องยิ่งเจ็บ: 40 เครื่อง × 8.4 MB = 336 MB แค่เปิดผังไลน์เดียวรอบแรก)

---

## 5b. ✅ ผลจริงหลัง deploy (ตรวจ 18/09)

| สิ่งที่วัด | ก่อน (16/09) | หลัง (18/09) |
|---|---:|---:|
| `mtn_orders?select=*&limit=1000` (1,000 แถว × 116 คอลัมน์) | **1,389 ครั้ง/วัน** | **0** |
| รูปผัง 22 ไฟล์ รวม | ~33 MB | **4.53 MB (−86%)** |
| ใบใหญ่สุด (LINE APRON ASSY) | 8,434 KB | **698 KB** |
| เฉลี่ยต่อผัง 1 ใบ | ~1,500 KB | **211 KB** |

**ตรวจ referential integrity หลังบีบ: ผัง 22 ไลน์ + ผังโรงงาน + ผังเครื่องจักร 3 โซน ชี้ไฟล์ที่มีอยู่จริงครบ 100%**

> ⚠️ **วันที่รันตัวบีบ ตัวเลข storage จะสูงผิดปกติ** — ตัวบีบต้องโหลด PNG เก่าลงมาก่อน (วัดได้ 35.2 MB
> ในชั่วโมงเดียว) = **ค่าใช้จ่ายครั้งเดียว** อย่าเอาวันนั้นไปเทียบ
>
> ⚠️ **ห้ามเทียบข้ามวันเสาร์-อาทิตย์** — 19/09 (เสาร์) app boots = **201** เทียบกับวันทำงาน ~4,000
> ⇒ ตัวเลขดูดีเกินจริงมาก (DR storage 9.1 MB · REST 14,387 req) **ไม่ใช่ผลของการแก้**

---

## 5c. รอบ 2 (21/09) — รูปจุดตรวจ PM เป็น WebP

หลังตัด `mtn_orders` ออก **ก้อนใหญ่สุดที่เหลือกลายเป็น Storage ฝั่ง DR**:

| โฟลเดอร์ | ครั้ง/วัน | เฉลี่ย/ใบ | รวม/วัน |
|---|---:|---:|---:|
| **`jig-images/jigs/`** (รูปจุดตรวจ PM + เฟรมหมุน) | 1,033 | 89 KB | **90.1 MB** |
| `mtn-images/after` + `before` (รูปซ่อม) | 241 | ~165 KB | 39 MB |

ของที่เก็บอยู่: **1,014 ไฟล์ = 102 MB** (JPEG 676 ใบ @111 KB · PNG 338 ใบ @88 KB)

**สิ่งที่ทำ:** `compressPhotoImage()` (WebP · 2000px · q0.85) ที่จุดอัปโหลดทั้ง 2 ใน `PMSetup.jsx`
(เฟรมหมุน + รูปอ้างอิงจุดตรวจ) และเพิ่มรูปกลุ่มนี้เข้าตัวบีบของเก่า

**⚠️ ต่างจากรูปผัง: รูปพวกนี้ถูกบีบมาแล้วรอบหนึ่ง** (JPEG คุณภาพต่ำ) ⇒ บีบ lossy ซ้ำ = เสียคุณภาพซ้ำซ้อน
จึง **ไม่ลดความละเอียดเพิ่ม** + quality สูง (0.85) — ขนาดที่ลดมาจากการเปลี่ยน**ฟอร์แมต** ไม่ใช่การทิ้งรายละเอียด

**⚠️ กับดักของกลุ่มนี้ (ไม่มีในรูปผัง):**
- **path เดียวถูกอ้างจาก 3 ตาราง** — `jig_images` · `jigs` · `jig_checkpoints`
  ⇒ ต้อง update ครบทั้ง 3 ก่อนลบไฟล์เก่า ไม่งั้นรูปหายจากจอใดจอหนึ่งแบบไม่รู้ตัว
- **มีเป็นพันไฟล์ ไม่ใช่ 20 ใบ** ⇒ ทำทีละ `RECOMPRESS_BATCH = 150` · คืน `remaining` ให้กดซ้ำ
- **กดซ้ำต้องปลอดภัย** ⇒ ไฟล์ที่เป็น `.webp` แล้วถูกข้าม**ก่อนโหลดลงมา** (ไม่เสีย egress ซ้ำ)

---

## 5d. ✅ ผลรอบ 2 + **แก้ตัวเลขที่ผมเคยประเมินผิด** (21/09)

### รูป PM — บีบแล้ว 80% ของไฟล์

| | ก่อนกด | หลังกด |
|---|---:|---:|
| WebP | 7 ไฟล์ | **816 ไฟล์** (เฉลี่ย 56 KB) |
| JPEG เหลือ | 676 | 69 |
| PNG เหลือ | 338 | 137 |
| **รวม `jig-images/jigs/`** | **102 MB** | **55.3 MB (−46%)** |

อัตราจริง: JPEG 111 KB → WebP 56 KB = **−50%**
**integrity ตรวจแล้ว: `jig_checkpoints` 639 · `jig_images` 369 · `jigs` 68 → ไฟล์หาย 0 ทุกตาราง**

### 🔴 บทเรียนวิธีวัด (รอบที่ 2 ที่ผมพลาดเรื่องเดิม)

§6 เวอร์ชันก่อนเขียนว่า `child_lot_requests` = **55 MB/วัน** — **ผิด**
ผมเอา **ไบต์ดิบ** (39.8 KB) × จำนวนครั้ง ทั้งที่ของจริงถูก**บีบก่อนส่ง**

วัดใหม่ด้วย `pg_column_size` (บีบแล้ว) ของวันที่ 21/09 ครึ่งวัน:

| คิวรี | ครั้ง (ครึ่งวัน) | บีบแล้ว/ครั้ง | รวม |
|---|---:|---:|---:|
| `child_lot_requests select=*` | 852 | 8.3 KB | **6.9 MB** |
| `production_sessions select=*` + join | 526 | 6.6 KB | 3.4 MB |
| `downtime_logs` รายการเปิดค้าง | 2,265 | 0.9 KB | 2.0 MB |

⇒ **ทั้งวันก็แค่ ~14 / 7 / 4 MB** — ไม่ใช่ 55 MB

**กฎเพิ่ม:** `octet_length()` = ไบต์ดิบ **ห้ามเอาไปคูณจำนวนครั้งตรงๆ**
ต้องผ่าน `pg_column_size()` (หรืออย่างน้อยหารด้วยอัตราบีบ ~4×) ก่อนเสมอ
— ใช้ "ไบต์ดิบ" ได้เฉพาะตอน**เทียบ 2 ทางเลือกของคิวรีเดียวกัน** (full vs slim) ซึ่งอัตราบีบใกล้กัน

### สถานะจริงหลังรอบ 2

**ไม่มีคิวรีตัวไหนเหลือที่ใหญ่พอจะเป็นเป้าอีกแล้ว** — ตัวโตสุด ~14 MB/วัน
ก้อนที่เหลือคือ **long tail + overhead ต่อ request** (~69,000 req/วัน × หัว ~1.5 KB ≈ 100-130 MB/วัน)

⚠️ **ห้ามไปไล่ตัด `select('*')` ของ `production_sessions` ใน Daily Report เพื่อประหยัด ~4 MB/วัน** —
40 คอลัมน์นั้นป้อน `computeOEE` + จอหลักของทั้งโรงงาน · **risk/reward ไม่คุ้ม** (ต่างจาก `mtn_orders`
ที่เป็น 116 คอลัมน์ × 628 MB/วัน)

---

## 5e. 📏 วัดเต็มวัน 21/09 (จันทร์ วันทำงานเต็มวันแรกที่ของแก้ครบทุกตัวขึ้น main)

**วิธีวัด — ใช้คิวรีชุดเดียวกันทั้ง 2 วัน** (เทียบข้ามวันด้วยคิวรีต่างสูตร = ตัวเลขหลอก):
`edge_logs` · ช่วงไทย 00:00-24:00 (= 17:00Z วันก่อน → 17:00Z) · ไบต์ storage จาก
`log_attributes['response.headers.content_length']`
⚠️ **คีย์คือ `response.headers.content_length` ไม่ใช่ `response.content_length`** — ใส่ผิดได้ 0 ทุกแถว
โดยไม่มี error (เสียเวลาไปรอบหนึ่งแล้ว) · REST ตอบแบบ chunked ⇒ **ไม่มีหัว content_length** ⇒ วัดไบต์ REST
จาก log ไม่ได้เลย นับได้แค่จำนวน request

| | 16/09 (พุธ · ก่อนแก้) | 21/09 (จันทร์ · หลังแก้) |
|---|---:|---:|
| app boot (`role_permissions` ฝั่ง Main) | 1,538 | 1,350 (−12%) |
| request รวม 2 project | 118,713 | 117,101 |
| REST ฝั่ง DR | 68,910 | 71,369 |
| **Storage ฝั่ง DR** | **135.7 MB** | 236.7 MB* |
| **Storage ฝั่ง Main** | **72.8 MB** | **58.6 MB** |

\* **ห้ามอ่านตัวเลขนี้ตรงๆ** — 21/09 เวลา 14:00-15:00 ไทยคือตอน user กดปุ่ม 🗜️ รอบ 2
(820 ครั้งที่เป็น POST/PUT · **138.9 MB**) = งานครั้งเดียวของตัวบีบเอง ไม่ใช่การใช้งานปกติ
หัก 138.9 ออก ⇒ **97.8 MB** และช่วงเช้า 08:00-11:00 ไทย (76 MB) ยังเสิร์ฟ **ไฟล์ก่อนบีบ**
เพราะรอบ 2 เพิ่งรันบ่ายวันเดียวกัน ⇒ steady state ที่คาดหลังบีบ ≈ 76 × 0.54 + ที่เหลือ ≈ **40-45 MB/วัน**

**สิ่งที่ตัวเลขนี้บอก (และไม่บอก):**
- ✅ ก้อนใหญ่หายจริง — ไม่มีคิวรีไหนเหลือเกิน ~14 MB/วัน แล้ว
- ⚠️ **จำนวน request ไม่ลด** (118.7k → 117.1k) เพราะงานที่ทำคือลด**ไบต์ต่อ request** ไม่ใช่จำนวนครั้ง
  ⇒ ต่อจากนี้ก้อนที่เหลือคือ **overhead ต่อ request** ซึ่งลดได้ด้วยการลดจำนวนครั้งเท่านั้น
- ⚠️ DR REST **เพิ่ม 3.6%** ทั้งที่ boot ลด 12% — ยังไม่ได้ไล่ว่าอะไรโต

---

## 5f. รอบ 3 (22/09) — รูปพนักงานเป็น WebP

ก้อนใหญ่สุดที่เหลือฝั่ง Main จากการวัด 21/09: **bucket `employee-photos` = 54.9 MB/วัน**
(1,391 ครั้ง × เฉลี่ย 40 KB · CF cache HIT 1,193 ครั้ง — **HIT ก็คิด egress** เพราะไบต์ยังออกไปหาเครื่องปลายทาง)
URL สะอาด ไม่มี query param ⇒ ไม่ใช่ปัญหา cache-bust · เป็นเรื่อง "ไฟล์ละ 40 KB × เครื่องเยอะ" ตรงๆ

ของจริงในถัง (`storage.objects`): `jpg` 231 ไฟล์ เฉลี่ย 66 KB · **`png` 44 ไฟล์ เฉลี่ย 628 KB** ·
`gif` 4 ไฟล์ เฉลี่ย **4.65 MB** (ไม่มีแถวไหนอ้างแล้ว = ขยะค้าง 18.2 MB)

ที่แก้:
1. `ImageCropModal` รับ prop **`webp`** (opt-in · default JPEG เหมือนเดิม) — เปิดที่ `operator.jsx` + `Register.jsx`
   · ตัวเขียนไฟล์ยุบเป็นฟังก์ชัน `emit()` จุดเดียว (เดิมซ้ำ 2 ที่ ลืมแก้ทีนึงได้ง่าย)
   · **นามสกุลไฟล์เอาจาก `blob.type` เท่านั้น** — จุดอัปโหลดทั้ง 2 หน้าอ่าน ext จาก `photo.name` อยู่แล้ว ⇒ ไหลตามเอง
2. `recompressLayouts.js` เพิ่มงานกลุ่ม **6) รูปพนักงาน** (`employees.image_url` · ไฟล์ราก bucket)
   — กดปุ่ม 🗜️ เดิมที่ `/linesetup` ได้เลย ไม่ต้องมีปุ่มใหม่ · ข้าม `layouts/`+`factory/` (กลุ่ม 1-2) และข้าม `.gif`
3. ด่านกันกลับมา `webp-tobiob-needs-type-check` — **`toBlob(cb,'image/webp')` บน Safari < 16.4 คืน PNG เงียบๆ**
   ⇒ ตั้งนามสกุล `.webp` เองแล้วได้ PNG ชื่อ .webp: ใหญ่กว่าเดิมและ Content-Type ผิด = งานลด egress
   กลายเป็นเพิ่ม egress โดยไม่มี error ให้จับ

คาดผล: 54.9 → ~25 MB/วัน (**−30 MB/วัน**) · ต้องกดปุ่ม 🗜️ ก่อนจะเห็นผลกับรูปเดิม 275 ใบ

---

## 5g. 🔴 ทำไมกดปุ่ม 🗜️ 3 ครั้งแล้วขนาดไม่ลด — คิวถูกอุด (22/09)

user กดปุ่มเพิ่ม 2-3 ครั้งแล้วแจ้งว่า "ลดมั้ย" · วัดจาก `storage.objects` จริง:

| bucket / โฟลเดอร์ | ไฟล์ | ขนาด | สถานะ |
|---|---:|---:|---|
| `jig-images/jigs/` webp | 816 | 44.6 MB | ✅ เสร็จ (เฉลี่ย 56 KB) |
| `jig-images/jigs/` jpg+png+jpeg | 206 | 10.7 MB | เล็กกว่าเกณฑ์ 40 KB อยู่แล้ว (เฉลี่ย 33-44 KB) |
| **`mtn-images/after/` jpg** | **324** | **60.6 MB** | ❌ **ไม่เคยถูกแตะ** (เฉลี่ย 191 KB) |
| **`mtn-images/before/` jpg** | **112** | **24.7 MB** | ❌ **ไม่เคยถูกแตะ** (เฉลี่ย 226 KB) |

`mtn_orders` มี 451 URL รูป (อยู่ใน bucket ครบ) — เป็น webp แค่ **11 ใบ** ⇒ ไม่ใช่เรื่องอ้างอิงพัง

**สาเหตุจริง — คิวงานถูกอุดด้วยงานที่ "ข้ามถาวร":**
ตัวปุ่มแบ่งล็อต 150 ใบ/รอบ แล้วมีตัวกันวนไม่รู้จบว่า *"ถ้าล็อตนี้ `done === 0` ให้หยุด"*
แต่รูปจุดตรวจ PM 198 ใบที่เล็กกว่าเกณฑ์อยู่แล้ว **ไม่เคยกลายเป็น .webp จึงไม่เคยหลุดออกจากลิสต์**
⇒ ถูกใส่คิวไว้หน้าสุด**ทุกรอบ** ⇒ ล็อตแรกเป็น skip ทั้งก้อน ⇒ `done === 0`
⇒ ตัวกันวนเข้าใจผิดว่า "ติดปัญหา" แล้วหยุด ⇒ **งานท้ายคิว (รูปซ่อม 85 MB) ไม่เคยถึงคิว**
ซ้ำร้าย 198 ใบนั้นถูก**ดาวน์โหลดเต็มก้อนทุกครั้งที่กด** (~9 MB/ครั้ง) เพื่อจะรู้ว่าไม่ต้องทำอะไร

**แก้ 3 จุด (22/09):**
1. `recompressOne` ถาม **HEAD ก่อนโหลดไฟล์** — รู้ขนาดโดยไม่กินแบนด์วิดท์ (ไม่มี HEAD/ไม่บอกขนาด = ถอยไปโหลดจริง)
2. กรอง `.webp` ออกตอน**สร้างคิว** (กลุ่มผังไลน์/ผังโรงงาน) ⇒ เลข "เหลือกี่ใบ" ตรงความจริง
3. จอส่ง **`limit: 0` รอบเดียวจบ** ไม่มีล็อต ไม่มีตัวกันวน `done === 0` ⇒ ไม่มีคิวถูกอุด
   → เขียนเป็น **กฎข้อ 5 หัวไฟล์ `recompressLayouts.js`** แล้ว (ห้ามกลับไปแบ่งล็อต)

**บทเรียนข้ามงาน:** ตัวกันวน (loop guard) ที่วัด "ความคืบหน้า" จากงานทั้งล็อต
จะตีความ "ล็อตนี้ไม่มีอะไรต้องทำ" เป็น "ระบบติดปัญหา" — ถ้าคิวมีงานที่ไม่มีวันเปลี่ยนสภาพปนอยู่
**ต้องเอางานที่ข้ามถาวรออกจากคิว ไม่ใช่ไปปรับตัวกันวน**

---

## 5h. รอบ 4 (24-25/09) — ลด "จำนวนครั้ง" แทน "ขนาดต่อครั้ง"

หลังบีบรูปจนก้อน storage ลงมาเหลือ ~75 MB/วัน ตัวที่เหลือคือจำนวน request ⇒ เปลี่ยนแนวมาไล่ตัวนี้

### 🔕 ตัวส่ง Push (24/09) — **ได้ผลจริง วัดแล้ว**

วัดหน้าต่างเวลาเดียวกันเป๊ะ (17:00Z→04:00Z · 12 ชม.) ก่อน/หลัง:

| endpoint | ก่อน | หลัง | |
|---|---:|---:|---|
| `push_subscriptions` | 727 | **113** | **−84%** |
| `notification_settings` | 731 | **429** | −41% |
| **รวม** | **1,458** | **542** | **−63%** |

ที่ `notification_settings` ไม่ลดเท่ากัน เพราะแยกดูแล้วเป็นคนละเจ้าของ:
`vapid_*` 274 ครั้ง (ตัวส่ง Push — จาก 3,816/วัน = **−86%**) + `bot_token` 155 ครั้ง (**ตัวส่ง Telegram
คนละฟังก์ชัน ไม่เกี่ยวกับที่แก้**) · ⚠️ ผมประเมินไว้ −72% **ได้จริง −63%** เพราะลืมคิดว่า VAPID
ไม่ลดตามสัดส่วนเดียวกับจำนวน invocation (cold isolate ต้องอ่านคีย์ใหม่ทุกครั้ง)
→ รายละเอียด/บทเรียน → `docs/modules/edge-functions.md`

### 🏭 `production_lines` (25/09) — ของกลางมีอยู่ แต่ถูกข้าม 107 จุด

วัด 23/09: **4,041 ครั้ง/วัน** · แยกตามชุดคอลัมน์:

| ชุดคอลัมน์ | ครั้ง/วัน |
|---|---:|
| `id,name,section,parent_line_name` | **1,761** |
| `id,name,parent_line_name,section,is_active` (= ตัว loader เอง) | 423 |
| `name,flow_mode,parallel_stations` | 263 |
| ที่เหลือ (อีก ~10 ชุด) | ~1,590 |

**ต้นเหตุเดียวกับ `storage_locations` เป๊ะ: loader กลางมีอยู่แล้ว แต่คอลัมน์ไม่ครบ
⇒ หน้าที่อยากได้ `cost_center`/`flow_mode`/`line_type`/กำลังคน ก็ยิงเอง** (`grep` เจอ **107 จุด**)

ที่แก้:
1. `LINE_COLUMNS` เป็น **superset 11 คอลัมน์** (จาก 5) + **bump คีย์เป็น `production_lines:v2`**
   · ตารางนี้ ~50 แถว/16 คอลัมน์ ⇒ ขอคอลัมน์เพิ่มถูกกว่าปล่อยให้ยิงเองเป็นพันครั้งมาก
2. แปลง **4 จุดที่ร้อนที่สุด** มาใช้ `loadProductionLines()`:
   `staleSessions.js` (โหลดทุกรอบที่ Daily Report รีเฟรช) · `ViewAsModal` · `WipBetweenSteps` · `QaCheckSheet`
   · ⚠️ `ViewAsModal` เรียงตาม section→name ⇒ ต้องเรียงเองฝั่งจอ (loader เรียงตามชื่ออย่างเดียว)
   · ⚠️ `staleSessions` คงกฎ "โหลดไม่สำเร็จต้องบอก" ไว้ — `cachedMaster` คืน `undefined` เมื่อโหลดพลาด
     และยังไม่เคยมี cache ซึ่ง**คนละความหมายกับ `[]` (ทะเบียนว่างจริง)**

> 🔑 **bump คีย์ ไม่ใช่ bump `CACHE_EPOCH`** — `CACHE_EPOCH` ล้าง cache **ของ master ทุกตัวทุกเครื่อง**
> พร้อมกัน = ระเบิด egress รอบใหม่ (บทเรียน 15/09 ที่เคยผูกกับ build id) · เปลี่ยนคีย์เฉพาะตัวที่ shape
> เปลี่ยนจริงก็พอ แล้วใส่คีย์เก่าไว้ใน `masterInvalidate` ด้วยเพื่อล้างของค้าง

#### 🔴 รอบเดียวกัน — ตรวจ log หลัง deploy แล้วเจอว่า "แก้ครึ่งเดียว = แย่กว่าเดิม"

ตรวจ querystring จริงหลังของขึ้น พบชุดใหม่ 11 คอลัมน์วิ่งอยู่จริง **แต่เจอ 2 อย่างที่ไม่ได้คิดไว้:**

1. **คอลัมน์ซ้ำใน querystring** — `…,std_night_shift,cost_center,flow_mode,…`
   เพราะมีจุดที่เขียน `` select(`${LINE_COLUMNS}, cost_center, flow_mode, …`) `` ต่อท้ายเอง
   พอ superset มีให้แล้วเลยซ้ำ (ไม่พัง แต่บอกว่ามีจุดที่ประกอบคอลัมน์เอง)
2. **🔴 ตัวใหญ่: `grep` เจอ ~30 จุดที่เขียน `supabase.from('production_lines').select(LINE_COLUMNS)` เอง**
   = audit 07/09 ทำให้ "คอลัมน์ตรงกัน" สำเร็จแล้วจริง **แต่ไม่เคยทำให้ "ยิงครั้งเดียว"**
   ⇒ **การขยาย LINE_COLUMNS ของผมทำให้ 30 จุดนั้นหนักขึ้นทุกจุด (5 → 11 คอลัมน์)**
   ทั้งที่ตั้งใจจะลด — **แก้ครึ่งเดียวแล้วหยุด = ทำให้แย่ลง**

⇒ กวาดต่อให้จบในวันเดียวกัน: **25 จุดสลับเป็น `loadLinesRes()`** (รูปแบบ `{ data, error }` สลับได้บรรทัดเดียว)
· ที่ลำดับต่างจาก `order by name` (ShiftOrganize=id · Checkin=section→name · ViewAsModal=section→name) **เรียงเองฝั่งจอ**
· เหลือยิงเอง **1 จุด**: `Report.jsx` ที่ต้องการ `head_name` (ไม่อยู่ใน LINE_COLUMNS เพราะไม่มี dropdown ไหนใช้)
· ถอด `import { LINE_COLUMNS }` ที่ไม่ได้ใช้แล้วออก 11 ไฟล์

**ด่านกันกลับมา: `lines-via-cached-loader`** — `select(LINE_COLUMNS)` นอก allow-list = build ล่ม

> 📌 **บทเรียนที่ใช้ได้กับ loader กลางทุกตัว:** การขยายชุดคอลัมน์ของ loader กลาง
> **ต้องมากับการกวาดจุดที่ยังยิงเองให้จบในคอมมิทเดียวกันเสมอ** ไม่งั้นจุดที่ยังไม่ย้ายจะจ่ายแพงขึ้น
> โดยที่ตัวเลขรวมดูเหมือนไม่ขยับ (แล้วคนสรุปว่า "แก้แล้วไม่ดีขึ้น")

#### รอบที่ 3 (25/09 บ่าย) — กวาดจุดที่ "ตั้งชุดคอลัมน์เอง" ให้หมด

ตรวจซ้ำหลังของขึ้น (24 ชม. ย้อนหลัง) พบว่าตัวเลขยังไม่ขยับจริง เพราะรอบก่อนกวาดแต่จุดที่ใช้ `LINE_COLUMNS`:

| ชุด | ครั้ง/วัน |
|---|---:|
| ผ่าน cache กลาง (11 คอลัมน์) | **424** |
| **ยังยิงเอง (ตั้งคอลัมน์เอง)** | **4,170** |

แยกตัวใหญ่: `id,name,section,parent_line_name` (ไม่มี `order`) **2,206** · ชุด 5 คอลัมน์เดิม 614+267 ·
`name,flow_mode,parallel_stations` **530** · `id,name` 115 · ถามทีละไลน์ `section,parent_line_name&name=eq.<ไลน์>` ~45

⇒ กวาด **48 จุดใน 45 ไฟล์** ให้ผ่าน cache กลางหมด · เคสที่ต้องคิดเป็นพิเศษ:
- **`monthlyReviewPptx.js` 2 จุด** ใช้ `loadProductionLines()` ตรงๆ ไม่ใช่ `loadLinesRes()` —
  สไลด์ต้อง**รู้ว่าอ่านทะเบียนไม่ติด**แล้วขึ้นคำเตือน (`undefined` = พัง · `[]` = ทะเบียนว่างจริง)
  ไม่งั้นค่าใช้จ่ายทั้งสไลด์กลายเป็น "ไม่มี cost center" โดยไม่มีใครรู้
- **`TvBoard` (`production_lines:scope`) + `FixtureRegistry` (`fx_lines`)** เคย cache ของตัวเองคนละคีย์
  = จอเดียวกันโหลดทะเบียนเดียวกัน 3 รอบ ⇒ ยุบเหลือคีย์เดียวทั้งแอป
  · **คีย์เก่ายังต้องอยู่ใน `masterInvalidate` ต่อไป** (เครื่องที่ยังไม่ได้ build ใหม่มีของค้าง)
- **`operator.jsx`** เคยถามชื่อไลน์ตัวเองทีละใบ (`.eq('id',…).single()`) → หาจาก cache แทน
- **`LineOeeBoard` / `OrderTrace`** เคยมี "ถอยชุดคอลัมน์ถ้า migration ยังไม่ลง" คนละท่า → ตัดทิ้ง
  (เรื่องชุดคอลัมน์เป็นหน้าที่ของ `useProductionLines.js` ที่เดียว)
- **`LineSetup` ยังยิงเอง ตั้งใจ** — หน้าแก้ทะเบียนไลน์ ต้องเห็นของสดทันทีหลังบันทึก + probe คอลัมน์

**ด่านขยายให้จับครบ:** `lines-via-cached-loader` เปลี่ยนจากจับแค่ `select(LINE_COLUMNS)`
เป็นจับ **`from('production_lines').select(`** ทุกแบบ (allow: loader · `Report.jsx` head_name · `LineSetup.jsx`)
— ด่านรอบแรกจับไม่ถึงตัวใหญ่ (4,170 จาก 4,594 ครั้ง/วัน) เพราะจุดพวกนั้นไม่ได้ใช้ค่าคงที่ตัวนั้นเลย

> 📌 **บทเรียน:** ด่านที่เขียนจาก "อาการที่เพิ่งเจอ" มักแคบเกินจริง — ถามต่อเสมอว่า
> **"เขียนแบบไหนได้อีกที่ทำผิดกฎเดียวกันแล้วด่านนี้ไม่เห็น"**

### 👤 `profiles` (3,814 ครั้ง/วัน) — **ตรวจแล้ว ไม่ใช่บั๊ก ไม่ต้องแก้**

ตัวใหญ่สุด `select=role,line_id,full_name,…&id=eq.<ตัวเอง>` = **2,568 ครั้ง/วัน** เทียบกับ
**app boot 3,358 ครั้ง/วัน** ⇒ ~0.76 ครั้งต่อการเปิดแอป 1 ครั้ง = **ยิงครั้งเดียวต่อ boot อยู่แล้ว**
ไม่ใช่การยิงซ้ำ · ทางลดคือ **ลดจำนวน boot (= ลดความถี่ deploy)** ไม่ใช่ cache
🔴 **ห้าม cache โปรไฟล์ตัวเอง** — role/สิทธิ์เปลี่ยนแล้วต้องมีผลทันที (cache 4 ชม. = คนถูกถอดสิทธิ์
ยังใช้ได้ต่ออีก 4 ชม.)

---

## 6. งานค้าง (เรียงตามผล)

1. **กดปุ่ม 🗜️ ที่หน้าใหม่ `/storage-maintenance`** (ย้ายจาก `/linesetup` — คำสั่ง user 22/09
   *"ที่จริงฟังก์ชันปุ่มบีบอัดรูปน่าจะอยู่ที่ setting"*) — หน้าบอกเองว่าเหลือกี่ใบแยกตามกลุ่ม
   ก่อนกด (โหมดสำรวจ อ่านแค่แถวใน DB ไม่โหลดรูป) · เป้าหลักที่เหลือ = **รูปซ่อม MO 85 MB**
   + รูปพนักงาน 275 ใบ · สิทธิ์ `storage_maintain:run` (migration `20260922_storage_maintenance_page_main.sql`)
2. **ลด "จำนวน request" ไม่ใช่ขนาด** — ก้อนที่ใหญ่ที่สุดที่เหลือแล้ว: ~117,000 req/วัน × หัว ~1.5 KB
   ≈ **170 MB/วัน** ⇒ **ตัวนี้คนเดียวก็เกินเพดาน Free แล้ว** · ทางลด = poll/realtime (ดู
   `docs/POLLING-AUDIT-2026-09-15.md` §6) ไม่ใช่การเลือกคอลัมน์
3. `dr_products?select=mat_no,pair_mat_no` — 629 ครั้ง/ครึ่งวัน จาก `ProdProgressStrip.jsx` +
   `ObeyaKpiBoard.jsx` ยิงตรงทั้งที่ `dr_products` มี cache หลายคีย์อยู่แล้ว (~7 MB/วัน)
   🔴 **ระวังเป็นพิเศษ:** กฎเหล็ก CLAUDE.md — "ลืม `select('pair_mat_no')` = pairMap ว่าง = OEE นับ 2 เท่าเงียบๆ"
   ⇒ ถ้าทำ cache แล้วชุดคอลัมน์ขาด จะทำตัวเลข OEE ทั้งโรงงานผิดโดยไม่มีใครรู้ · **bump คีย์ cache เสมอ**
4. `prod_orders?select=*` (33 คอลัมน์) ~1,800 ครั้ง/วัน · `downtime_logs?select=*,dr_downtime_types(...)` ~500 ครั้ง/วัน
5. รูปขยะค้างใน storage — `employee-photos/emp_*.gif` **4 ไฟล์ = 18.2 MB** (อ้างอิง 0 แถว ยืนยันแล้ว 22/09)
   + `jig-images/facility/*.png` 2 ใบ → `cleanup-orphan-photos` (กระทบเพดาน **พื้นที่เก็บ 1 GB** ไม่ใช่ egress)
6. **ห้ามแตะ** `production_sessions?select=*,dr_products(...)` (679 ครั้ง/วัน · 40 คอลัมน์) —
   ป้อน `computeOEE` + จอหลักหน้างาน · ได้ไม่คุ้มเสี่ยง (ตัดสินไว้แล้ว 21/09 ห้ามรื้อซ้ำ)

---

## 7. ประเมินว่าพอกลับ Free ไหม — **ยังไม่พอ** (22/09)

เพดานที่ต้องไปให้ถึง: **5 GB/เดือน ≈ 166 MB/วัน** (และต้องเผื่อเครื่องเพิ่มเป็น ~40 ตัว)

ประมาณการหลังของแก้ครบ + กดปุ่ม 🗜️ หมดแล้ว:

| ก้อน | MB/วัน | ฐานที่ใช้ประเมิน |
|---|---:|---|
| Storage DR (รูป PM/ซ่อม หลังบีบ) | ~40 | วัดจริง 76 MB ก่อนบีบ × อัตราลด 46% ที่วัดได้ |
| Storage Main (รูปพนักงาน หลังบีบ) | ~25 | วัดจริง 54.9 MB × อัตราลด ~50% ของ WebP |
| overhead ต่อ request | ~170 | 117,101 req × ~1.5 KB (**ยังไม่ได้แก้เลย**) |
| payload REST | ? | **วัดจาก log ไม่ได้** (chunked ไม่มี content_length) |
| **รวม** | **>235** | เทียบเพดาน **166** |

⇒ **ยังเกินอยู่ และก้อนที่เกินคือ "จำนวน request" ไม่ใช่ขนาดต่อ request อีกแล้ว**
งานที่เหลือจึงเปลี่ยนแนว: จากการเลือกคอลัมน์/บีบรูป → ไปลดรอบ poll + ใช้ realtime ให้ครบทุกจอ
(`useLiveBoard` · `makeIdleGate`) ตามของค้างใน `docs/POLLING-AUDIT-2026-09-15.md` §6

⚠️ **ห้ามยกเลิก Pro กลางรอบ** — GB ที่ใช้ไปแล้วเดือนนี้จะถูกนับกับเพดาน 5 GB ทันทีที่โควต้าลด = โดนล็อกซ้ำ
ให้ยกเลิกเมื่อ **วัดได้จริงว่าอยู่ต่ำกว่า ~166 MB/วัน ติดต่อกันหลายวันทำงาน** เท่านั้น

⚠️ **กับดักการเทียบวัน** (พลาดมาแล้ว 2 ครั้ง): เสาร์-อาทิตย์ boot ~200 เทียบกับวันทำงาน ~1,400
⇒ เห็นตัวเลขต่ำแล้วดีใจผิด · และช่วง log ที่ขอเลยเวลาปัจจุบันจะได้วันครึ่งๆ ดูเหมือนระบบล่ม
**ทุกครั้งที่เทียบ ต้องดู app boot กำกับเสมอ**
